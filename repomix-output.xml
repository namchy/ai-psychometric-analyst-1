This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
app/
  (protected)/
    dashboard/
      attempts/
        [attemptId]/
          run/
            page.tsx
          page.tsx
      page.tsx
  actions/
    assessment.ts
    auth.ts
  api/
    health/
      route.ts
  login/
    page.tsx
  globals.css
  layout.tsx
  page.tsx
components/
  assessment/
    assessment-form.tsx
    completed-assessment-summary.tsx
  auth/
    login-form.tsx
lib/
  assessment/
    completion-server.ts
    completion.ts
    display.ts
    report-config.ts
    report-provider-helpers.ts
    report-provider-mock.ts
    report-provider-openai.ts
    report-provider-registry.ts
    report-providers.ts
    reports.ts
    result-display.ts
    scoring.ts
    tests.ts
    types.ts
  auth/
    cookies.ts
    session.ts
  b2b/
    organizations.ts
  supabase/
    admin.ts
    client.ts
    env.ts
    server.ts
scripts/
  check-supabase.mjs
  verify-completion-flow.mjs
  verify-report-flow.mjs
  verify-resume-flow.mjs
  verify-scoring-flow.mjs
supabase/
  .branches/
    _current_branch
  .temp/
    cli-latest
  migrations/
    20260309122751_init_assessment_schema.sql
    20260309155744_enable_rls_initial.sql
    20260311120000_support_multiple_choice_responses.sql
    20260311121000_add_multiple_choice_and_text_seed_questions.sql
    20260312133500_harden_active_test_contract.sql
    20260312150000_add_attempt_reports.sql
    20260312173000_expand_attempt_report_generator_types.sql
    20260313103649_add_b2b_identity_foundation.sql
    20260313110000_add_attempt_report_status.sql
  snippets/
    Untitled query 162.sql
    Untitled query 233.sql
    Untitled query 398.sql
  config.toml
  seed.sql
.eslintrc.json
.gitignore
AGENTS.md
middleware.ts
next-env.d.ts
next.config.js
package.json
tsconfig.json
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="app/actions/auth.ts">
"use server";

import { redirect } from "next/navigation";
import { clearAuthSession, persistAuthSession } from "@/lib/auth/session";
import { createSupabaseAuthClient } from "@/lib/supabase/server";

export type LoginInput = {
  email: string;
  password: string;
};

export type LoginResult =
  | {
      ok: true;
    }
  | {
      ok: false;
      message: string;
    };

function normalizeCredential(value: string): string {
  return value.trim();
}

export async function loginWithPassword(input: LoginInput): Promise<LoginResult> {
  const email = normalizeCredential(input.email).toLowerCase();
  const password = normalizeCredential(input.password);

  if (!email || !password) {
    return {
      ok: false,
      message: "Email and password are required.",
    };
  }

  const supabase = createSupabaseAuthClient();
  const { data, error } = await supabase.auth.signInWithPassword({
    email,
    password,
  });

  if (error || !data.session) {
    return {
      ok: false,
      message: "Invalid email or password.",
    };
  }

  persistAuthSession(data.session);

  return { ok: true };
}

export async function logout() {
  clearAuthSession();
  redirect("/login");
}
</file>

<file path="app/api/health/route.ts">
import { NextResponse } from "next/server";

export function GET() {
  return NextResponse.json({ ok: true });
}
</file>

<file path="app/login/page.tsx">
import { LoginForm } from "@/components/auth/login-form";
import { redirectAuthenticatedUserToDashboard } from "@/lib/auth/session";

export const dynamic = "force-dynamic";

export default async function LoginPage() {
  await redirectAuthenticatedUserToDashboard();

  return (
    <main>
      <section className="card stack-md">
        <div className="stack-xs">
          <h1>Sign in</h1>
          <p>Use your Supabase Auth email/password credentials to access the B2B admin area.</p>
        </div>

        <LoginForm />
      </section>
    </main>
  );
}
</file>

<file path="app/layout.tsx">
import "./globals.css";
import type { Metadata } from "next";
import type { ReactNode } from "react";

export const metadata: Metadata = {
  title: "AI Psychometric Analyst",
  description: "MVP candidate assessment platform"
};

type RootLayoutProps = {
  children: ReactNode;
};

export default function RootLayout({ children }: RootLayoutProps) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
</file>

<file path="components/auth/login-form.tsx">
"use client";

import { useRouter } from "next/navigation";
import { useState, useTransition } from "react";
import { loginWithPassword } from "@/app/actions/auth";

export function LoginForm() {
  const router = useRouter();
  const [isPending, startTransition] = useTransition();
  const [message, setMessage] = useState<string | null>(null);

  return (
    <form
      className="stack-sm"
      onSubmit={(event) => {
        event.preventDefault();
        const formData = new FormData(event.currentTarget);
        const email = String(formData.get("email") ?? "");
        const password = String(formData.get("password") ?? "");

        startTransition(async () => {
          const result = await loginWithPassword({ email, password });

          if (!result.ok) {
            setMessage(result.message);
            return;
          }

          setMessage(null);
          router.push("/dashboard");
          router.refresh();
        });
      }}
    >
      <label className="stack-xs" htmlFor="email">
        <span>Email</span>
        <input id="email" name="email" type="email" autoComplete="email" required />
      </label>

      <label className="stack-xs" htmlFor="password">
        <span>Password</span>
        <input
          id="password"
          name="password"
          type="password"
          autoComplete="current-password"
          required
        />
      </label>

      <button type="submit" disabled={isPending}>
        {isPending ? "Signing in..." : "Sign in"}
      </button>

      {message ? <p>{message}</p> : null}
    </form>
  );
}
</file>

<file path="lib/assessment/completion-server.ts">
import {
  buildSelectionsFromResponses,
  getAssessmentCompletionState,
  type AssessmentCompletionState,
  type CompletionQuestion,
} from "@/lib/assessment/completion";
import { createSupabaseAdminClient } from "@/lib/supabase/admin";

type CompletionResponseRecord = {
  question_id: string;
  response_kind: CompletionQuestion["question_type"];
  answer_option_id: string | null;
  text_value: string | null;
  response_selections: Array<{
    answer_option_id: string;
  }> | null;
};

export async function loadAssessmentCompletionState(
  testId: string,
  attemptId: string,
): Promise<AssessmentCompletionState> {
  const supabase = createSupabaseAdminClient();
  const { data: questionsData, error: questionsError } = await supabase
    .from("questions")
    .select("id, text, question_type, is_required")
    .eq("test_id", testId)
    .eq("is_active", true)
    .order("question_order", { ascending: true });

  if (questionsError) {
    throw new Error(
      `Failed to load questions for completion validation: ${questionsError.message}`,
    );
  }

  const { data: responsesData, error: responsesError } = await supabase
    .from("responses")
    .select(
      "question_id, response_kind, answer_option_id, text_value, response_selections(answer_option_id)",
    )
    .eq("attempt_id", attemptId);

  if (responsesError) {
    throw new Error(
      `Failed to load responses for completion validation: ${responsesError.message}`,
    );
  }

  return getAssessmentCompletionState(
    (questionsData ?? []) as CompletionQuestion[],
    buildSelectionsFromResponses((responsesData ?? []) as CompletionResponseRecord[]),
  );
}
</file>

<file path="lib/assessment/completion.ts">
import type {
  AssessmentSelectionsInput,
  AssessmentSelectionValue,
  QuestionType,
} from "@/lib/assessment/types";

type CompletionResponseRecord = {
  question_id: string;
  response_kind: QuestionType;
  answer_option_id: string | null;
  text_value: string | null;
  response_selections: Array<{
    answer_option_id: string;
  }> | null;
};

export type CompletionQuestion = {
  id: string;
  text: string;
  question_type: QuestionType;
  is_required: boolean;
};

export type AssessmentCompletionState = {
  requiredQuestionCount: number;
  answeredRequiredQuestionCount: number;
  missingRequiredQuestionIds: string[];
  missingRequiredQuestionTexts: string[];
  isComplete: boolean;
};

function isNonEmptyString(value: string): boolean {
  return value.trim().length > 0;
}

export function isQuestionAnswered(
  questionType: QuestionType,
  selection: AssessmentSelectionValue | undefined,
): boolean {
  if (questionType === "text") {
    return typeof selection === "string" && isNonEmptyString(selection);
  }

  if (questionType === "single_choice") {
    return typeof selection === "string" && selection.length > 0;
  }

  return Array.isArray(selection) && selection.some((optionId) => optionId.length > 0);
}

export function getAssessmentCompletionState(
  questions: CompletionQuestion[],
  selections: AssessmentSelectionsInput,
): AssessmentCompletionState {
  const requiredQuestions = questions.filter((question) => question.is_required);
  const missingRequiredQuestions = requiredQuestions.filter(
    (question) => !isQuestionAnswered(question.question_type, selections[question.id]),
  );

  return {
    requiredQuestionCount: requiredQuestions.length,
    answeredRequiredQuestionCount: requiredQuestions.length - missingRequiredQuestions.length,
    missingRequiredQuestionIds: missingRequiredQuestions.map((question) => question.id),
    missingRequiredQuestionTexts: missingRequiredQuestions.map((question) => question.text),
    isComplete: missingRequiredQuestions.length === 0,
  };
}

export function buildSelectionsFromResponses(
  responses: CompletionResponseRecord[],
): AssessmentSelectionsInput {
  return responses.reduce<AssessmentSelectionsInput>((state, response) => {
    if (response.response_kind === "text") {
      if (response.text_value && isNonEmptyString(response.text_value)) {
        state[response.question_id] = response.text_value;
      }

      return state;
    }

    if (response.response_kind === "single_choice") {
      if (response.answer_option_id) {
        state[response.question_id] = response.answer_option_id;
      }

      return state;
    }

    const selectedOptionIds = (response.response_selections ?? []).map(
      (selection) => selection.answer_option_id,
    );

    if (selectedOptionIds.length > 0) {
      state[response.question_id] = selectedOptionIds;
    }

    return state;
  }, {});
}
</file>

<file path="lib/assessment/display.ts">
export function getAssessmentDisplayName(
  input: {
    name?: string | null;
    slug?: string | null;
  } | null,
): string {
  const normalizedName = input?.name?.trim().toLowerCase() ?? "";
  const normalizedSlug = input?.slug?.trim().toLowerCase() ?? "";

  if (normalizedName.includes("ipip-50") || normalizedSlug.includes("ipip-50")) {
    return "Big Five upitnik ličnosti (IPIP-50)";
  }

  return input?.name ?? input?.slug ?? "Procjena";
}
</file>

<file path="lib/assessment/scoring.ts">
import type { QuestionType, ScoringMethod } from "@/lib/assessment/types";
import { createSupabaseAdminClient } from "@/lib/supabase/admin";

type AttemptRecord = {
  id: string;
  status: "in_progress" | "completed" | "abandoned";
};

type TestRecord = {
  id: string;
  scoring_method: ScoringMethod;
};

type QuestionRecord = {
  id: string;
  code: string;
  text: string;
  dimension: string;
  question_type: QuestionType;
  question_order: number;
  reverse_scored: boolean;
  weight: number;
};

type AnswerOptionRecord = {
  id: string;
  question_id: string;
  value: number | null;
};

type ResponseRecord = {
  id: string;
  question_id: string;
  response_kind: QuestionType;
  answer_option_id: string | null;
  raw_value: number | null;
  scored_value: number | null;
};

export type CompletedAssessmentResultDimension = {
  dimension: string;
  rawScore: number;
  scoredQuestionCount: number;
};

export type CompletedAssessmentUnscoredResponse = {
  questionId: string;
  questionCode: string;
  questionText: string;
  questionType: QuestionType;
  reason: "question_type_not_scoreable" | "missing_numeric_option_value";
};

export type CompletedAssessmentResults = {
  attemptId: string;
  scoringMethod: ScoringMethod;
  dimensions: CompletedAssessmentResultDimension[];
  scoredResponseCount: number;
  unscoredResponses: CompletedAssessmentUnscoredResponse[];
};

type ComputedResponseScore = {
  responseId: string;
  rawValue: number | null;
  scoredValue: number | null;
};

type ComputedDimensionScore = {
  dimension: string;
  rawScore: number;
  scoredQuestionCount: number;
  sortOrder: number;
};

function roundScore(value: number): number {
  return Math.round(value * 100) / 100;
}

function valuesEqual(left: number | null, right: number | null): boolean {
  if (left === null || right === null) {
    return left === right;
  }

  return roundScore(left) === roundScore(right);
}

function getLikertScaleBounds(options: AnswerOptionRecord[]): { min: number; max: number } | null {
  const numericValues = options
    .map((option) => option.value)
    .filter((value): value is number => typeof value === "number");

  if (numericValues.length === 0) {
    return null;
  }

  return {
    min: Math.min(...numericValues),
    max: Math.max(...numericValues),
  };
}

function computeLikertResults(
  questions: QuestionRecord[],
  answerOptions: AnswerOptionRecord[],
  responses: ResponseRecord[],
): {
  responseScores: ComputedResponseScore[];
  dimensions: ComputedDimensionScore[];
  scoredResponseCount: number;
  unscoredResponses: CompletedAssessmentUnscoredResponse[];
} {
  const questionsById = new Map(questions.map((question) => [question.id, question]));
  const optionsById = new Map(answerOptions.map((option) => [option.id, option]));
  const answerOptionsByQuestionId = answerOptions.reduce<Map<string, AnswerOptionRecord[]>>(
    (groupedOptions, option) => {
      const questionOptions = groupedOptions.get(option.question_id) ?? [];
      questionOptions.push(option);
      groupedOptions.set(option.question_id, questionOptions);
      return groupedOptions;
    },
    new Map<string, AnswerOptionRecord[]>(),
  );

  const dimensionsByName = new Map<string, ComputedDimensionScore>();
  const responseScores: ComputedResponseScore[] = [];
  const unscoredResponses: CompletedAssessmentUnscoredResponse[] = [];
  let scoredResponseCount = 0;

  for (const response of responses) {
    const question = questionsById.get(response.question_id);

    if (!question) {
      continue;
    }

    if (response.response_kind !== "single_choice") {
      responseScores.push({
        responseId: response.id,
        rawValue: null,
        scoredValue: null,
      });
      unscoredResponses.push({
        questionId: question.id,
        questionCode: question.code,
        questionText: question.text,
        questionType: question.question_type,
        reason: "question_type_not_scoreable",
      });
      continue;
    }

    const selectedOption = response.answer_option_id
      ? optionsById.get(response.answer_option_id)
      : null;
    const scaleBounds = getLikertScaleBounds(answerOptionsByQuestionId.get(question.id) ?? []);

    if (!selectedOption || selectedOption.value === null || !scaleBounds) {
      responseScores.push({
        responseId: response.id,
        rawValue: null,
        scoredValue: null,
      });
      unscoredResponses.push({
        questionId: question.id,
        questionCode: question.code,
        questionText: question.text,
        questionType: question.question_type,
        reason: "missing_numeric_option_value",
      });
      continue;
    }

    const rawValue = selectedOption.value;
    const scoredValue = question.reverse_scored
      ? scaleBounds.min + scaleBounds.max - rawValue
      : rawValue;

    responseScores.push({
      responseId: response.id,
      rawValue,
      scoredValue,
    });

    const dimensionScore = dimensionsByName.get(question.dimension) ?? {
      dimension: question.dimension,
      rawScore: 0,
      scoredQuestionCount: 0,
      sortOrder: question.question_order,
    };

    dimensionScore.rawScore = roundScore(dimensionScore.rawScore + scoredValue * question.weight);
    dimensionScore.scoredQuestionCount += 1;
    dimensionScore.sortOrder = Math.min(dimensionScore.sortOrder, question.question_order);
    dimensionsByName.set(question.dimension, dimensionScore);
    scoredResponseCount += 1;
  }

  return {
    responseScores,
    dimensions: [...dimensionsByName.values()].sort((left, right) => left.sortOrder - right.sortOrder),
    scoredResponseCount,
    unscoredResponses,
  };
}

async function loadScoringInputs(testId: string, attemptId: string) {
  const supabase = createSupabaseAdminClient();

  const { data: attemptData, error: attemptError } = await supabase
    .from("attempts")
    .select("id, status")
    .eq("id", attemptId)
    .eq("test_id", testId)
    .maybeSingle();

  if (attemptError) {
    throw new Error(`Failed to load attempt for scoring: ${attemptError.message}`);
  }

  const attempt = attemptData as AttemptRecord | null;

  if (!attempt || attempt.status !== "completed") {
    return null;
  }

  const { data: testData, error: testError } = await supabase
    .from("tests")
    .select("id, scoring_method")
    .eq("id", testId)
    .single();

  if (testError || !testData) {
    throw new Error(
      `Failed to load test scoring configuration: ${testError?.message ?? "Unknown error"}`,
    );
  }

  const { data: questionsData, error: questionsError } = await supabase
    .from("questions")
    .select("id, code, text, dimension, question_type, question_order, reverse_scored, weight")
    .eq("test_id", testId)
    .order("question_order", { ascending: true });

  if (questionsError) {
    throw new Error(`Failed to load questions for scoring: ${questionsError.message}`);
  }

  const questions = (questionsData ?? []) as QuestionRecord[];
  const questionIds = questions.map((question) => question.id);

  const { data: answerOptionsData, error: answerOptionsError } = await supabase
    .from("answer_options")
    .select("id, question_id, value")
    .in("question_id", questionIds);

  if (answerOptionsError) {
    throw new Error(`Failed to load answer options for scoring: ${answerOptionsError.message}`);
  }

  const { data: responsesData, error: responsesError } = await supabase
    .from("responses")
    .select("id, question_id, response_kind, answer_option_id, raw_value, scored_value")
    .eq("attempt_id", attemptId);

  if (responsesError) {
    throw new Error(`Failed to load responses for scoring: ${responsesError.message}`);
  }

  return {
    supabase,
    test: testData as TestRecord,
    questions,
    answerOptions: (answerOptionsData ?? []) as AnswerOptionRecord[],
    responses: (responsesData ?? []) as ResponseRecord[],
  };
}

export async function calculateCompletedAssessmentResults(
  testId: string,
  attemptId: string,
): Promise<CompletedAssessmentResults | null> {
  const scoringInputs = await loadScoringInputs(testId, attemptId);

  if (!scoringInputs) {
    return null;
  }

  if (scoringInputs.test.scoring_method !== "likert_sum") {
    throw new Error(
      `Unsupported scoring method for MVP results flow: ${scoringInputs.test.scoring_method}`,
    );
  }

  const computedResults = computeLikertResults(
    scoringInputs.questions,
    scoringInputs.answerOptions,
    scoringInputs.responses,
  );

  return {
    attemptId,
    scoringMethod: scoringInputs.test.scoring_method,
    dimensions: computedResults.dimensions.map((dimension) => ({
      dimension: dimension.dimension,
      rawScore: dimension.rawScore,
      scoredQuestionCount: dimension.scoredQuestionCount,
    })),
    scoredResponseCount: computedResults.scoredResponseCount,
    unscoredResponses: computedResults.unscoredResponses,
  };
}

export async function persistCompletedAssessmentResults(
  testId: string,
  attemptId: string,
): Promise<CompletedAssessmentResults | null> {
  const scoringInputs = await loadScoringInputs(testId, attemptId);

  if (!scoringInputs) {
    return null;
  }

  if (scoringInputs.test.scoring_method !== "likert_sum") {
    throw new Error(
      `Unsupported scoring method for MVP results flow: ${scoringInputs.test.scoring_method}`,
    );
  }

  const computedResults = computeLikertResults(
    scoringInputs.questions,
    scoringInputs.answerOptions,
    scoringInputs.responses,
  );

  const responsesById = new Map(scoringInputs.responses.map((response) => [response.id, response]));
  const responseUpdates = computedResults.responseScores.filter((score) => {
    const existingResponse = responsesById.get(score.responseId);

    if (!existingResponse) {
      return false;
    }

    return (
      !valuesEqual(existingResponse.raw_value, score.rawValue) ||
      !valuesEqual(existingResponse.scored_value, score.scoredValue)
    );
  });

  for (const responseUpdate of responseUpdates) {
    const { error: responseUpdateError } = await scoringInputs.supabase
      .from("responses")
      .update({
        raw_value: responseUpdate.rawValue,
        scored_value: responseUpdate.scoredValue,
      })
      .eq("id", responseUpdate.responseId);

    if (responseUpdateError) {
      throw new Error(`Failed to persist response score: ${responseUpdateError.message}`);
    }
  }

  const { error: deleteScoresError } = await scoringInputs.supabase
    .from("dimension_scores")
    .delete()
    .eq("attempt_id", attemptId);

  if (deleteScoresError) {
    throw new Error(`Failed to replace dimension scores: ${deleteScoresError.message}`);
  }

  if (computedResults.dimensions.length > 0) {
    const { error: insertScoresError } = await scoringInputs.supabase
      .from("dimension_scores")
      .insert(
        computedResults.dimensions.map((dimension) => ({
          attempt_id: attemptId,
          dimension: dimension.dimension,
          raw_score: dimension.rawScore,
          normalized_score: null,
          percentile_score: null,
          interpretation: null,
        })),
      );

    if (insertScoresError) {
      throw new Error(`Failed to persist dimension scores: ${insertScoresError.message}`);
    }
  }

  return {
    attemptId,
    scoringMethod: scoringInputs.test.scoring_method,
    dimensions: computedResults.dimensions.map((dimension) => ({
      dimension: dimension.dimension,
      rawScore: dimension.rawScore,
      scoredQuestionCount: dimension.scoredQuestionCount,
    })),
    scoredResponseCount: computedResults.scoredResponseCount,
    unscoredResponses: computedResults.unscoredResponses,
  };
}
</file>

<file path="lib/auth/cookies.ts">
export const SUPABASE_ACCESS_TOKEN_COOKIE_NAME = "sb-access-token";
export const SUPABASE_REFRESH_TOKEN_COOKIE_NAME = "sb-refresh-token";
export const DEFAULT_REFRESH_COOKIE_MAX_AGE_SECONDS = 60 * 60 * 24 * 7;

export type AuthCookieName =
  | typeof SUPABASE_ACCESS_TOKEN_COOKIE_NAME
  | typeof SUPABASE_REFRESH_TOKEN_COOKIE_NAME;

export function getAuthCookieOptions(maxAge: number) {
  return {
    httpOnly: true,
    sameSite: "lax" as const,
    secure: process.env.NODE_ENV === "production",
    path: "/",
    maxAge,
  };
}
</file>

<file path="lib/supabase/admin.ts">
import "server-only";

import { createClient } from "@supabase/supabase-js";

function getRequiredServerEnvVar(value: string | undefined, name: string): string {
  if (!value) {
    throw new Error(`Missing required env var: ${name}`);
  }

  return value;
}

export function createSupabaseAdminClient() {
  const url = getRequiredServerEnvVar(
    process.env.NEXT_PUBLIC_SUPABASE_URL,
    "NEXT_PUBLIC_SUPABASE_URL",
  );
  const serviceRoleKey = getRequiredServerEnvVar(
    process.env.SUPABASE_SERVICE_ROLE_KEY,
    "SUPABASE_SERVICE_ROLE_KEY",
  );

  return createClient(url, serviceRoleKey, {
    auth: {
      autoRefreshToken: false,
      persistSession: false,
    },
  });
}
</file>

<file path="lib/supabase/client.ts">
import { createClient } from "@supabase/supabase-js";
import { getSupabaseEnv } from "./env";

let browserClient: ReturnType<typeof createClient> | null = null;

export function getSupabaseBrowserClient() {
  if (!browserClient) {
    const { url, publishableKey } = getSupabaseEnv();
    browserClient = createClient(url, publishableKey);
  }
  return browserClient;
}
</file>

<file path="supabase/.branches/_current_branch">
main
</file>

<file path="supabase/.temp/cli-latest">
v2.75.0
</file>

<file path="supabase/migrations/20260309122751_init_assessment_schema.sql">
create extension if not exists pgcrypto;

create table public.tests (
  id uuid primary key default gen_random_uuid(),
  slug text not null unique,
  name text not null,
  category text not null check (category in ('personality', 'behavioral', 'cognitive')),
  description text,
  status text not null default 'draft' check (status in ('draft', 'active', 'archived')),
  scoring_method text not null default 'likert_sum' check (scoring_method in ('likert_sum', 'correct_answers', 'weighted_correct')),
  duration_minutes integer,
  is_active boolean not null default true,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);

create table public.questions (
  id uuid primary key default gen_random_uuid(),
  test_id uuid not null references public.tests(id) on delete cascade,
  code text not null,
  text text not null,
  help_text text,
  dimension text not null,
  question_type text not null default 'single_choice' check (question_type in ('single_choice', 'multiple_choice', 'text')),
  question_order integer not null,
  reverse_scored boolean not null default false,
  difficulty text check (difficulty in ('easy', 'medium', 'hard')),
  weight numeric(6,2) not null default 1.00,
  is_required boolean not null default true,
  is_active boolean not null default true,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  unique (test_id, code),
  unique (test_id, question_order)
);

create table public.answer_options (
  id uuid primary key default gen_random_uuid(),
  question_id uuid not null references public.questions(id) on delete cascade,
  code text,
  label text not null,
  value integer,
  option_order integer not null,
  is_correct boolean,
  created_at timestamptz not null default now(),
  unique (question_id, option_order)
);

create table public.attempts (
  id uuid primary key default gen_random_uuid(),
  user_id uuid,
  test_id uuid not null references public.tests(id) on delete restrict,
  status text not null default 'in_progress' check (status in ('in_progress', 'completed', 'abandoned')),
  started_at timestamptz not null default now(),
  completed_at timestamptz,
  total_time_seconds integer,
  metadata jsonb not null default '{}'::jsonb
);

create table public.responses (
  id uuid primary key default gen_random_uuid(),
  attempt_id uuid not null references public.attempts(id) on delete cascade,
  question_id uuid not null references public.questions(id) on delete cascade,
  answer_option_id uuid references public.answer_options(id) on delete restrict,
  raw_value integer,
  scored_value numeric(8,2),
  text_value text,
  answered_at timestamptz not null default now(),
  unique (attempt_id, question_id)
);

create table public.dimension_scores (
  id uuid primary key default gen_random_uuid(),
  attempt_id uuid not null references public.attempts(id) on delete cascade,
  dimension text not null,
  raw_score numeric(10,2) not null,
  normalized_score numeric(10,2),
  percentile_score numeric(10,2),
  interpretation text,
  created_at timestamptz not null default now(),
  unique (attempt_id, dimension)
);

create index idx_questions_test_id on public.questions(test_id);
create index idx_questions_dimension on public.questions(dimension);
create index idx_answer_options_question_id on public.answer_options(question_id);
create index idx_attempts_test_id on public.attempts(test_id);
create index idx_attempts_user_id on public.attempts(user_id);
create index idx_responses_attempt_id on public.responses(attempt_id);
create index idx_responses_question_id on public.responses(question_id);
create index idx_dimension_scores_attempt_id on public.dimension_scores(attempt_id);
</file>

<file path="supabase/migrations/20260309155744_enable_rls_initial.sql">
-- Enable RLS on all core tables

alter table public.tests enable row level security;
alter table public.questions enable row level security;
alter table public.answer_options enable row level security;
alter table public.attempts enable row level security;
alter table public.responses enable row level security;
alter table public.dimension_scores enable row level security;

-- Clean re-run safety: drop policies if they already exist

drop policy if exists "tests_read_public" on public.tests;
drop policy if exists "questions_read_public" on public.questions;
drop policy if exists "answer_options_read_public" on public.answer_options;

drop policy if exists "attempts_read_own" on public.attempts;
drop policy if exists "responses_read_own" on public.responses;
drop policy if exists "dimension_scores_read_own" on public.dimension_scores;

-- PUBLIC READ POLICIES
-- For MVP, expose only active records.
-- Important: this assumes your seeded test has is_active = true.

create policy "tests_read_public"
on public.tests
for select
to anon, authenticated
using (
  is_active = true
);

create policy "questions_read_public"
on public.questions
for select
to anon, authenticated
using (
  is_active = true
  and exists (
    select 1
    from public.tests t
    where t.id = questions.test_id
      and t.is_active = true
  )
);

create policy "answer_options_read_public"
on public.answer_options
for select
to anon, authenticated
using (
  exists (
    select 1
    from public.questions q
    join public.tests t on t.id = q.test_id
    where q.id = answer_options.question_id
      and q.is_active = true
      and t.is_active = true
  )
);

-- PRIVATE USER DATA POLICIES
-- Only authenticated users can read their own attempts and related data.
-- This assumes attempts.user_id will store auth.uid().

create policy "attempts_read_own"
on public.attempts
for select
to authenticated
using (
  user_id = auth.uid()
);

create policy "responses_read_own"
on public.responses
for select
to authenticated
using (
  exists (
    select 1
    from public.attempts a
    where a.id = responses.attempt_id
      and a.user_id = auth.uid()
  )
);

create policy "dimension_scores_read_own"
on public.dimension_scores
for select
to authenticated
using (
  exists (
    select 1
    from public.attempts a
    where a.id = dimension_scores.attempt_id
      and a.user_id = auth.uid()
  )
);
</file>

<file path="supabase/migrations/20260311120000_support_multiple_choice_responses.sql">
do $$
begin
  if not exists (
    select 1
    from pg_constraint
    where conname = 'answer_options_id_question_id_unique'
      and conrelid = 'public.answer_options'::regclass
  ) then
    alter table public.answer_options
      add constraint answer_options_id_question_id_unique unique (id, question_id);
  end if;
end $$;

alter table public.responses
  add column if not exists response_kind text;

update public.responses r
set response_kind = case
  when q.question_type = 'text' then 'text'
  when q.question_type = 'multiple_choice' then 'multiple_choice'
  else 'single_choice'
end
from public.questions q
where q.id = r.question_id
  and r.response_kind is null;

drop index if exists idx_responses_text_once_per_question;
drop index if exists idx_responses_selected_option_once_per_question;

do $$
begin
  if not exists (
    select 1
    from pg_constraint
    where conname = 'responses_id_question_id_unique'
      and conrelid = 'public.responses'::regclass
  ) then
    alter table public.responses
      add constraint responses_id_question_id_unique unique (id, question_id);
  end if;
end $$;

do $$
begin
  if not exists (
    select 1
    from pg_constraint
    where conname = 'responses_attempt_id_question_id_key'
      and conrelid = 'public.responses'::regclass
  ) then
    alter table public.responses
      add constraint responses_attempt_id_question_id_key unique (attempt_id, question_id);
  end if;
end $$;

alter table public.responses
  drop constraint if exists responses_answer_option_id_fkey;

alter table public.responses
  drop constraint if exists responses_value_shape_check;

alter table public.responses
  drop constraint if exists responses_response_kind_check;

create table if not exists public.response_selections (
  response_id uuid not null,
  question_id uuid not null,
  answer_option_id uuid not null,
  created_at timestamptz not null default now(),
  primary key (response_id, answer_option_id)
);

do $$
begin
  if not exists (
    select 1
    from pg_constraint
    where conname = 'response_selections_response_fk'
      and conrelid = 'public.response_selections'::regclass
  ) then
    alter table public.response_selections
      add constraint response_selections_response_fk
        foreign key (response_id, question_id)
        references public.responses (id, question_id)
        on delete cascade;
  end if;
end $$;

do $$
begin
  if not exists (
    select 1
    from pg_constraint
    where conname = 'response_selections_answer_option_fk'
      and conrelid = 'public.response_selections'::regclass
  ) then
    alter table public.response_selections
      add constraint response_selections_answer_option_fk
        foreign key (answer_option_id, question_id)
        references public.answer_options (id, question_id)
        on delete restrict;
  end if;
end $$;

with canonical_multiple_choice_responses as (
  select
    r.attempt_id,
    r.question_id,
    min(r.id::text)::uuid as response_id
  from public.responses r
  join public.questions q
    on q.id = r.question_id
  where q.question_type = 'multiple_choice'
  group by r.attempt_id, r.question_id
)
insert into public.response_selections (response_id, question_id, answer_option_id)
select
  canonical.response_id,
  r.question_id,
  r.answer_option_id
from public.responses r
join public.questions q
  on q.id = r.question_id
join canonical_multiple_choice_responses canonical
  on canonical.attempt_id = r.attempt_id
  and canonical.question_id = r.question_id
where q.question_type = 'multiple_choice'
  and r.answer_option_id is not null
on conflict (response_id, answer_option_id) do nothing;

with canonical_multiple_choice_responses as (
  select
    r.attempt_id,
    r.question_id,
    min(r.id::text)::uuid as response_id
  from public.responses r
  join public.questions q
    on q.id = r.question_id
  where q.question_type = 'multiple_choice'
  group by r.attempt_id, r.question_id
)
update public.responses r
set
  answer_option_id = null,
  text_value = null
from canonical_multiple_choice_responses canonical
where r.id = canonical.response_id;

with canonical_multiple_choice_responses as (
  select
    r.attempt_id,
    r.question_id,
    min(r.id::text)::uuid as response_id
  from public.responses r
  join public.questions q
    on q.id = r.question_id
  where q.question_type = 'multiple_choice'
  group by r.attempt_id, r.question_id
)
delete from public.responses r
using canonical_multiple_choice_responses canonical, public.questions q
where q.id = r.question_id
  and q.question_type = 'multiple_choice'
  and r.attempt_id = canonical.attempt_id
  and r.question_id = canonical.question_id
  and r.id <> canonical.response_id;

alter table public.responses
  add constraint responses_response_kind_check
    check (response_kind in ('single_choice', 'multiple_choice', 'text')),
  add constraint responses_value_shape_check
    check (
      (response_kind = 'single_choice' and answer_option_id is not null and text_value is null)
      or (response_kind = 'multiple_choice' and answer_option_id is null and text_value is null)
      or (response_kind = 'text' and answer_option_id is null and text_value is not null)
    ),
  add constraint responses_answer_option_matches_question_fk
    foreign key (answer_option_id, question_id)
    references public.answer_options (id, question_id)
    on delete restrict;

alter table public.responses
  alter column response_kind set default 'single_choice',
  alter column response_kind set not null;

create index if not exists idx_response_selections_answer_option_id
  on public.response_selections (answer_option_id);

create index if not exists idx_response_selections_question_id
  on public.response_selections (question_id);

alter table public.response_selections enable row level security;

drop policy if exists "response_selections_read_own" on public.response_selections;

create policy "response_selections_read_own"
on public.response_selections
for select
using (
  exists (
    select 1
    from public.responses r
    join public.attempts a
      on a.id = r.attempt_id
    where r.id = response_selections.response_id
      and a.user_id = auth.uid()
  )
);
</file>

<file path="supabase/migrations/20260311121000_add_multiple_choice_and_text_seed_questions.sql">
insert into public.questions (
  id,
  test_id,
  code,
  text,
  dimension,
  question_type,
  question_order,
  reverse_scored,
  is_active
)
select
  '25555555-1111-1111-1111-111111111111',
  t.id,
  'A1',
  'Koje aktivnosti ti prijaju u timu?',
  'agreeableness',
  'multiple_choice',
  5,
  false,
  true
from public.tests t
where t.id = '11111111-1111-1111-1111-111111111111'
  and not exists (
    select 1
    from public.questions q
    where q.id = '25555555-1111-1111-1111-111111111111'
  );

insert into public.questions (
  id,
  test_id,
  code,
  text,
  dimension,
  question_type,
  question_order,
  reverse_scored,
  is_active
)
select
  '26666666-1111-1111-1111-111111111111',
  t.id,
  'O1',
  'U nekoliko rečenica opiši kako pristupaš novim idejama.',
  'openness',
  'text',
  6,
  false,
  true
from public.tests t
where t.id = '11111111-1111-1111-1111-111111111111'
  and not exists (
    select 1
    from public.questions q
    where q.id = '26666666-1111-1111-1111-111111111111'
  );

insert into public.answer_options (question_id, code, label, value, option_order)
select
  '25555555-1111-1111-1111-111111111111',
  seeded_option.code,
  seeded_option.label,
  seeded_option.value,
  seeded_option.option_order
from (
  values
    ('collaboration', 'Zajedničko planiranje', null::integer, 1),
    ('support', 'Pomaganje kolegama', null::integer, 2),
    ('facilitation', 'Moderiranje diskusije', null::integer, 3),
    ('delivery', 'Završavanje dogovorenih zadataka', null::integer, 4)
) as seeded_option(code, label, value, option_order)
where exists (
  select 1
  from public.questions q
  where q.id = '25555555-1111-1111-1111-111111111111'
)
  and not exists (
    select 1
    from public.answer_options ao
    where ao.question_id = '25555555-1111-1111-1111-111111111111'
      and ao.option_order = seeded_option.option_order
  );
</file>

<file path="supabase/migrations/20260312133500_harden_active_test_contract.sql">
with preferred_active_test as (
  select id
  from public.tests
  order by
    case when slug = 'ipip50-hr-v1' then 0 else 1 end,
    created_at asc,
    id asc
  limit 1
)
update public.tests t
set
  is_active = (t.id = preferred_active_test.id),
  status = case
    when t.id = preferred_active_test.id then 'active'
    when t.status = 'archived' then 'archived'
    else 'draft'
  end,
  updated_at = now()
from preferred_active_test;

alter table public.tests
  alter column is_active set default false;

alter table public.tests
  drop constraint if exists tests_status_check;

alter table public.tests
  add constraint tests_status_check
  check (status in ('draft', 'active', 'archived'));

alter table public.tests
  drop constraint if exists tests_status_is_active_check;

alter table public.tests
  add constraint tests_status_is_active_check
  check (
    (is_active = true and status = 'active')
    or (is_active = false and status in ('draft', 'archived'))
  );

create unique index if not exists tests_one_active_test_idx
  on public.tests (is_active)
  where is_active = true;
</file>

<file path="supabase/migrations/20260312150000_add_attempt_reports.sql">
create table public.attempt_reports (
  id uuid primary key default gen_random_uuid(),
  attempt_id uuid not null unique references public.attempts(id) on delete cascade,
  test_slug text not null,
  generator_type text not null check (generator_type in ('mock')),
  generated_at timestamptz not null default now(),
  report_snapshot jsonb not null
);

create index idx_attempt_reports_attempt_id on public.attempt_reports(attempt_id);

alter table public.attempt_reports enable row level security;

drop policy if exists "attempt_reports_read_own" on public.attempt_reports;

create policy "attempt_reports_read_own"
on public.attempt_reports
for select
to authenticated
using (
  exists (
    select 1
    from public.attempts a
    where a.id = attempt_reports.attempt_id
      and a.user_id = auth.uid()
  )
);
</file>

<file path="supabase/migrations/20260312173000_expand_attempt_report_generator_types.sql">
alter table public.attempt_reports
drop constraint if exists attempt_reports_generator_type_check;

alter table public.attempt_reports
add constraint attempt_reports_generator_type_check
check (generator_type in ('mock', 'openai'));
</file>

<file path="supabase/migrations/20260313103649_add_b2b_identity_foundation.sql">
create table public.organizations (
  id uuid primary key default gen_random_uuid(),
  name text not null,
  slug text not null unique,
  status text not null default 'active' check (status in ('active', 'inactive')),
  created_at timestamptz not null default now()
);

create table public.organization_memberships (
  id uuid primary key default gen_random_uuid(),
  organization_id uuid not null references public.organizations(id) on delete cascade,
  user_id uuid not null references auth.users(id) on delete cascade,
  role text not null check (role in ('org_owner', 'hr_admin', 'manager')),
  status text not null default 'active' check (status in ('active', 'invited', 'disabled')),
  created_at timestamptz not null default now(),
  unique (organization_id, user_id)
);

create table public.participants (
  id uuid primary key default gen_random_uuid(),
  organization_id uuid not null references public.organizations(id) on delete cascade,
  user_id uuid references auth.users(id) on delete set null,
  email text not null,
  full_name text not null,
  participant_type text not null check (participant_type in ('employee', 'candidate')),
  status text not null default 'active' check (status in ('active', 'inactive')),
  created_at timestamptz not null default now()
);

create unique index idx_organizations_slug on public.organizations(slug);
create index idx_organization_memberships_user_id on public.organization_memberships(user_id);
create index idx_organization_memberships_organization_id on public.organization_memberships(organization_id);
create index idx_participants_organization_id on public.participants(organization_id);
create index idx_participants_user_id on public.participants(user_id);
create unique index idx_participants_org_email on public.participants(organization_id, lower(email));

alter table public.attempts
  add column if not exists organization_id uuid references public.organizations(id) on delete set null,
  add column if not exists participant_id uuid references public.participants(id) on delete set null;

create index if not exists idx_attempts_organization_id on public.attempts(organization_id);
create index if not exists idx_attempts_participant_id on public.attempts(participant_id);

alter table public.organizations enable row level security;
alter table public.organization_memberships enable row level security;
alter table public.participants enable row level security;

drop policy if exists "organizations_read_member" on public.organizations;
drop policy if exists "organization_memberships_read_own" on public.organization_memberships;
drop policy if exists "participants_read_member" on public.participants;

create policy "organization_memberships_read_own"
on public.organization_memberships
for select
to authenticated
using (
  user_id = auth.uid()
);

create policy "organizations_read_member"
on public.organizations
for select
to authenticated
using (
  exists (
    select 1
    from public.organization_memberships membership
    where membership.organization_id = organizations.id
      and membership.user_id = auth.uid()
      and membership.status = 'active'
  )
);

create policy "participants_read_member"
on public.participants
for select
to authenticated
using (
  exists (
    select 1
    from public.organization_memberships membership
    where membership.organization_id = participants.organization_id
      and membership.user_id = auth.uid()
      and membership.status = 'active'
  )
);

drop policy if exists "attempts_read_own" on public.attempts;
drop policy if exists "responses_read_own" on public.responses;
drop policy if exists "dimension_scores_read_own" on public.dimension_scores;
drop policy if exists "response_selections_read_own" on public.response_selections;
drop policy if exists "attempt_reports_read_own" on public.attempt_reports;

create policy "attempts_read_own"
on public.attempts
for select
to authenticated
using (
  user_id = auth.uid()
  or exists (
    select 1
    from public.participants participant
    where participant.id = attempts.participant_id
      and participant.user_id = auth.uid()
  )
  or exists (
    select 1
    from public.organization_memberships membership
    where membership.organization_id = attempts.organization_id
      and membership.user_id = auth.uid()
      and membership.status = 'active'
  )
);

create policy "responses_read_own"
on public.responses
for select
to authenticated
using (
  exists (
    select 1
    from public.attempts attempt
    left join public.participants participant
      on participant.id = attempt.participant_id
    left join public.organization_memberships membership
      on membership.organization_id = attempt.organization_id
      and membership.user_id = auth.uid()
      and membership.status = 'active'
    where attempt.id = responses.attempt_id
      and (
        attempt.user_id = auth.uid()
        or participant.user_id = auth.uid()
        or membership.id is not null
      )
  )
);

create policy "dimension_scores_read_own"
on public.dimension_scores
for select
to authenticated
using (
  exists (
    select 1
    from public.attempts attempt
    left join public.participants participant
      on participant.id = attempt.participant_id
    left join public.organization_memberships membership
      on membership.organization_id = attempt.organization_id
      and membership.user_id = auth.uid()
      and membership.status = 'active'
    where attempt.id = dimension_scores.attempt_id
      and (
        attempt.user_id = auth.uid()
        or participant.user_id = auth.uid()
        or membership.id is not null
      )
  )
);

create policy "response_selections_read_own"
on public.response_selections
for select
to authenticated
using (
  exists (
    select 1
    from public.responses response
    join public.attempts attempt
      on attempt.id = response.attempt_id
    left join public.participants participant
      on participant.id = attempt.participant_id
    left join public.organization_memberships membership
      on membership.organization_id = attempt.organization_id
      and membership.user_id = auth.uid()
      and membership.status = 'active'
    where response.id = response_selections.response_id
      and (
        attempt.user_id = auth.uid()
        or participant.user_id = auth.uid()
        or membership.id is not null
      )
  )
);

create policy "attempt_reports_read_own"
on public.attempt_reports
for select
to authenticated
using (
  exists (
    select 1
    from public.attempts attempt
    left join public.participants participant
      on participant.id = attempt.participant_id
    left join public.organization_memberships membership
      on membership.organization_id = attempt.organization_id
      and membership.user_id = auth.uid()
      and membership.status = 'active'
    where attempt.id = attempt_reports.attempt_id
      and (
        attempt.user_id = auth.uid()
        or participant.user_id = auth.uid()
        or membership.id is not null
      )
  )
);
</file>

<file path="supabase/migrations/20260313110000_add_attempt_report_status.sql">
alter table public.attempt_reports
add column report_status text not null default 'ready',
add column failure_code text,
add column failure_reason text;

alter table public.attempt_reports
alter column report_snapshot drop not null;

alter table public.attempt_reports
add constraint attempt_reports_report_status_check
check (report_status in ('ready', 'unavailable'));

alter table public.attempt_reports
add constraint attempt_reports_snapshot_consistency_check
check (
  (report_status = 'ready' and report_snapshot is not null and failure_code is null and failure_reason is null)
  or (report_status = 'unavailable' and report_snapshot is null)
);
</file>

<file path="supabase/snippets/Untitled query 162.sql">
select
  rs.response_id,
  rs.question_id,
  q.code as question_code,
  ao.id as answer_option_id,
  ao.code as option_code,
  ao.label as option_label,
  rs.created_at
from public.response_selections rs
join public.questions q
  on q.id = rs.question_id
join public.answer_options ao
  on ao.id = rs.answer_option_id
where rs.response_id in (
  select r.id
  from public.responses r
  where r.attempt_id = '7041cd69-4a8b-4d70-8536-a2f0757a3838'
)
order by q.question_order, ao.option_order;
</file>

<file path="supabase/snippets/Untitled query 233.sql">
select
  r.id,
  r.attempt_id,
  r.question_id,
  q.code as question_code,
  q.question_type,
  r.response_kind,
  r.answer_option_id,
  r.text_value,
  r.answered_at
from public.responses r
join public.questions q
  on q.id = r.question_id
where r.attempt_id = '7041cd69-4a8b-4d70-8536-a2f0757a3838'
order by q.question_order;
</file>

<file path="supabase/config.toml">
# For detailed configuration reference documentation, visit:
# https://supabase.com/docs/guides/local-development/cli/config
# A string used to distinguish different Supabase projects on the same host. Defaults to the
# working directory name when running `supabase init`.
project_id = "AI_psychometric_analyst_for_HR_v1"

[api]
enabled = true
# Port to use for the API URL.
port = 54321
# Schemas to expose in your API. Tables, views and stored procedures in this schema will get API
# endpoints. `public` and `graphql_public` schemas are included by default.
schemas = ["public", "graphql_public"]
# Extra schemas to add to the search_path of every request.
extra_search_path = ["public", "extensions"]
# The maximum number of rows returns from a view, table, or stored procedure. Limits payload size
# for accidental or malicious requests.
max_rows = 1000

[api.tls]
# Enable HTTPS endpoints locally using a self-signed certificate.
enabled = false
# Paths to self-signed certificate pair.
# cert_path = "../certs/my-cert.pem"
# key_path = "../certs/my-key.pem"

[db]
# Port to use for the local database URL.
port = 54322
# Port used by db diff command to initialize the shadow database.
shadow_port = 54320
# Maximum amount of time to wait for health check when starting the local database.
health_timeout = "2m"
# The database major version to use. This has to be the same as your remote database's. Run `SHOW
# server_version;` on the remote database to check.
major_version = 17

[db.pooler]
enabled = false
# Port to use for the local connection pooler.
port = 54329
# Specifies when a server connection can be reused by other clients.
# Configure one of the supported pooler modes: `transaction`, `session`.
pool_mode = "transaction"
# How many server connections to allow per user/database pair.
default_pool_size = 20
# Maximum number of client connections allowed.
max_client_conn = 100

# [db.vault]
# secret_key = "env(SECRET_VALUE)"

[db.migrations]
# If disabled, migrations will be skipped during a db push or reset.
enabled = true
# Specifies an ordered list of schema files that describe your database.
# Supports glob patterns relative to supabase directory: "./schemas/*.sql"
schema_paths = []

[db.seed]
# If enabled, seeds the database after migrations during a db reset.
enabled = true
# Specifies an ordered list of seed files to load during db reset.
# Supports glob patterns relative to supabase directory: "./seeds/*.sql"
sql_paths = ["./seed.sql"]

[db.network_restrictions]
# Enable management of network restrictions.
enabled = false
# List of IPv4 CIDR blocks allowed to connect to the database.
# Defaults to allow all IPv4 connections. Set empty array to block all IPs.
allowed_cidrs = ["0.0.0.0/0"]
# List of IPv6 CIDR blocks allowed to connect to the database.
# Defaults to allow all IPv6 connections. Set empty array to block all IPs.
allowed_cidrs_v6 = ["::/0"]

# Uncomment to reject non-secure connections to the database.
# [db.ssl_enforcement]
# enabled = true

[realtime]
enabled = true
# Bind realtime via either IPv4 or IPv6. (default: IPv4)
# ip_version = "IPv6"
# The maximum length in bytes of HTTP request headers. (default: 4096)
# max_header_length = 4096

[studio]
enabled = true
# Port to use for Supabase Studio.
port = 54323
# External URL of the API server that frontend connects to.
api_url = "http://127.0.0.1"
# OpenAI API Key to use for Supabase AI in the Supabase Studio.
openai_api_key = "env(OPENAI_API_KEY)"

# Email testing server. Emails sent with the local dev setup are not actually sent - rather, they
# are monitored, and you can view the emails that would have been sent from the web interface.
[inbucket]
enabled = true
# Port to use for the email testing server web interface.
port = 54324
# Uncomment to expose additional ports for testing user applications that send emails.
# smtp_port = 54325
# pop3_port = 54326
# admin_email = "admin@email.com"
# sender_name = "Admin"

[storage]
enabled = true
# The maximum file size allowed (e.g. "5MB", "500KB").
file_size_limit = "50MiB"

# Uncomment to configure local storage buckets
# [storage.buckets.images]
# public = false
# file_size_limit = "50MiB"
# allowed_mime_types = ["image/png", "image/jpeg"]
# objects_path = "./images"

# Allow connections via S3 compatible clients
[storage.s3_protocol]
enabled = true

# Image transformation API is available to Supabase Pro plan.
# [storage.image_transformation]
# enabled = true

# Store analytical data in S3 for running ETL jobs over Iceberg Catalog
# This feature is only available on the hosted platform.
[storage.analytics]
enabled = false
max_namespaces = 5
max_tables = 10
max_catalogs = 2

# Analytics Buckets is available to Supabase Pro plan.
# [storage.analytics.buckets.my-warehouse]

# Store vector embeddings in S3 for large and durable datasets
# This feature is only available on the hosted platform.
[storage.vector]
enabled = false
max_buckets = 10
max_indexes = 5

# Vector Buckets is available to Supabase Pro plan.
# [storage.vector.buckets.documents-openai]

[auth]
enabled = true
# The base URL of your website. Used as an allow-list for redirects and for constructing URLs used
# in emails.
site_url = "http://127.0.0.1:3000"
# A list of *exact* URLs that auth providers are permitted to redirect to post authentication.
additional_redirect_urls = ["https://127.0.0.1:3000"]
# How long tokens are valid for, in seconds. Defaults to 3600 (1 hour), maximum 604,800 (1 week).
jwt_expiry = 3600
# JWT issuer URL. If not set, defaults to the local API URL (http://127.0.0.1:<port>/auth/v1).
# jwt_issuer = ""
# Path to JWT signing key. DO NOT commit your signing keys file to git.
# signing_keys_path = "./signing_keys.json"
# If disabled, the refresh token will never expire.
enable_refresh_token_rotation = true
# Allows refresh tokens to be reused after expiry, up to the specified interval in seconds.
# Requires enable_refresh_token_rotation = true.
refresh_token_reuse_interval = 10
# Allow/disallow new user signups to your project.
enable_signup = true
# Allow/disallow anonymous sign-ins to your project.
enable_anonymous_sign_ins = false
# Allow/disallow testing manual linking of accounts
enable_manual_linking = false
# Passwords shorter than this value will be rejected as weak. Minimum 6, recommended 8 or more.
minimum_password_length = 6
# Passwords that do not meet the following requirements will be rejected as weak. Supported values
# are: `letters_digits`, `lower_upper_letters_digits`, `lower_upper_letters_digits_symbols`
password_requirements = ""

[auth.rate_limit]
# Number of emails that can be sent per hour. Requires auth.email.smtp to be enabled.
email_sent = 2
# Number of SMS messages that can be sent per hour. Requires auth.sms to be enabled.
sms_sent = 30
# Number of anonymous sign-ins that can be made per hour per IP address. Requires enable_anonymous_sign_ins = true.
anonymous_users = 30
# Number of sessions that can be refreshed in a 5 minute interval per IP address.
token_refresh = 150
# Number of sign up and sign-in requests that can be made in a 5 minute interval per IP address (excludes anonymous users).
sign_in_sign_ups = 30
# Number of OTP / Magic link verifications that can be made in a 5 minute interval per IP address.
token_verifications = 30
# Number of Web3 logins that can be made in a 5 minute interval per IP address.
web3 = 30

# Configure one of the supported captcha providers: `hcaptcha`, `turnstile`.
# [auth.captcha]
# enabled = true
# provider = "hcaptcha"
# secret = ""

[auth.email]
# Allow/disallow new user signups via email to your project.
enable_signup = true
# If enabled, a user will be required to confirm any email change on both the old, and new email
# addresses. If disabled, only the new email is required to confirm.
double_confirm_changes = true
# If enabled, users need to confirm their email address before signing in.
enable_confirmations = false
# If enabled, users will need to reauthenticate or have logged in recently to change their password.
secure_password_change = false
# Controls the minimum amount of time that must pass before sending another signup confirmation or password reset email.
max_frequency = "1s"
# Number of characters used in the email OTP.
otp_length = 6
# Number of seconds before the email OTP expires (defaults to 1 hour).
otp_expiry = 3600

# Use a production-ready SMTP server
# [auth.email.smtp]
# enabled = true
# host = "smtp.sendgrid.net"
# port = 587
# user = "apikey"
# pass = "env(SENDGRID_API_KEY)"
# admin_email = "admin@email.com"
# sender_name = "Admin"

# Uncomment to customize email template
# [auth.email.template.invite]
# subject = "You have been invited"
# content_path = "./supabase/templates/invite.html"

# Uncomment to customize notification email template
# [auth.email.notification.password_changed]
# enabled = true
# subject = "Your password has been changed"
# content_path = "./templates/password_changed_notification.html"

[auth.sms]
# Allow/disallow new user signups via SMS to your project.
enable_signup = false
# If enabled, users need to confirm their phone number before signing in.
enable_confirmations = false
# Template for sending OTP to users
template = "Your code is {{ .Code }}"
# Controls the minimum amount of time that must pass before sending another sms otp.
max_frequency = "5s"

# Use pre-defined map of phone number to OTP for testing.
# [auth.sms.test_otp]
# 4152127777 = "123456"

# Configure logged in session timeouts.
# [auth.sessions]
# Force log out after the specified duration.
# timebox = "24h"
# Force log out if the user has been inactive longer than the specified duration.
# inactivity_timeout = "8h"

# This hook runs before a new user is created and allows developers to reject the request based on the incoming user object.
# [auth.hook.before_user_created]
# enabled = true
# uri = "pg-functions://postgres/auth/before-user-created-hook"

# This hook runs before a token is issued and allows you to add additional claims based on the authentication method used.
# [auth.hook.custom_access_token]
# enabled = true
# uri = "pg-functions://<database>/<schema>/<hook_name>"

# Configure one of the supported SMS providers: `twilio`, `twilio_verify`, `messagebird`, `textlocal`, `vonage`.
[auth.sms.twilio]
enabled = false
account_sid = ""
message_service_sid = ""
# DO NOT commit your Twilio auth token to git. Use environment variable substitution instead:
auth_token = "env(SUPABASE_AUTH_SMS_TWILIO_AUTH_TOKEN)"

# Multi-factor-authentication is available to Supabase Pro plan.
[auth.mfa]
# Control how many MFA factors can be enrolled at once per user.
max_enrolled_factors = 10

# Control MFA via App Authenticator (TOTP)
[auth.mfa.totp]
enroll_enabled = false
verify_enabled = false

# Configure MFA via Phone Messaging
[auth.mfa.phone]
enroll_enabled = false
verify_enabled = false
otp_length = 6
template = "Your code is {{ .Code }}"
max_frequency = "5s"

# Configure MFA via WebAuthn
# [auth.mfa.web_authn]
# enroll_enabled = true
# verify_enabled = true

# Use an external OAuth provider. The full list of providers are: `apple`, `azure`, `bitbucket`,
# `discord`, `facebook`, `github`, `gitlab`, `google`, `keycloak`, `linkedin_oidc`, `notion`, `twitch`,
# `twitter`, `x`, `slack`, `spotify`, `workos`, `zoom`.
[auth.external.apple]
enabled = false
client_id = ""
# DO NOT commit your OAuth provider secret to git. Use environment variable substitution instead:
secret = "env(SUPABASE_AUTH_EXTERNAL_APPLE_SECRET)"
# Overrides the default auth redirectUrl.
redirect_uri = ""
# Overrides the default auth provider URL. Used to support self-hosted gitlab, single-tenant Azure,
# or any other third-party OIDC providers.
url = ""
# If enabled, the nonce check will be skipped. Required for local sign in with Google auth.
skip_nonce_check = false
# If enabled, it will allow the user to successfully authenticate when the provider does not return an email address.
email_optional = false

# Allow Solana wallet holders to sign in to your project via the Sign in with Solana (SIWS, EIP-4361) standard.
# You can configure "web3" rate limit in the [auth.rate_limit] section and set up [auth.captcha] if self-hosting.
[auth.web3.solana]
enabled = false

# Use Firebase Auth as a third-party provider alongside Supabase Auth.
[auth.third_party.firebase]
enabled = false
# project_id = "my-firebase-project"

# Use Auth0 as a third-party provider alongside Supabase Auth.
[auth.third_party.auth0]
enabled = false
# tenant = "my-auth0-tenant"
# tenant_region = "us"

# Use AWS Cognito (Amplify) as a third-party provider alongside Supabase Auth.
[auth.third_party.aws_cognito]
enabled = false
# user_pool_id = "my-user-pool-id"
# user_pool_region = "us-east-1"

# Use Clerk as a third-party provider alongside Supabase Auth.
[auth.third_party.clerk]
enabled = false
# Obtain from https://clerk.com/setup/supabase
# domain = "example.clerk.accounts.dev"

# OAuth server configuration
[auth.oauth_server]
# Enable OAuth server functionality
enabled = false
# Path for OAuth consent flow UI
authorization_url_path = "/oauth/consent"
# Allow dynamic client registration
allow_dynamic_registration = false

[edge_runtime]
enabled = true
# Supported request policies: `oneshot`, `per_worker`.
# `per_worker` (default) — enables hot reload during local development.
# `oneshot` — fallback mode if hot reload causes issues (e.g. in large repos or with symlinks).
policy = "per_worker"
# Port to attach the Chrome inspector for debugging edge functions.
inspector_port = 8083
# The Deno major version to use.
deno_version = 2

# [edge_runtime.secrets]
# secret_key = "env(SECRET_VALUE)"

[analytics]
enabled = true
port = 54327
# Configure one of the supported backends: `postgres`, `bigquery`.
backend = "postgres"

# Experimental features may be deprecated any time
[experimental]
# Configures Postgres storage engine to use OrioleDB (S3)
orioledb_version = ""
# Configures S3 bucket URL, eg. <bucket_name>.s3-<region>.amazonaws.com
s3_host = "env(S3_HOST)"
# Configures S3 bucket region, eg. us-east-1
s3_region = "env(S3_REGION)"
# Configures AWS_ACCESS_KEY_ID for S3 bucket
s3_access_key = "env(S3_ACCESS_KEY)"
# Configures AWS_SECRET_ACCESS_KEY for S3 bucket
s3_secret_key = "env(S3_SECRET_KEY)"
</file>

<file path=".eslintrc.json">
{
  "extends": [
    "next/core-web-vitals"
  ]
}
</file>

<file path=".gitignore">
node_modules
.next
out
dist
coverage
.DS_Store
*.log
.env.local

tsconfig.tsbuildinfo
</file>

<file path="AGENTS.md">
\# AGENTS.md



\## Project purpose

This repository is an MVP for a B2B candidate assessment platform.

The product will support structured assessments such as Big Five, DISC-like behavioral tests, and cognitive tests.

The current phase focuses only on the first Big Five MVP slice.



\## Current stack

\- Next.js

\- TypeScript

\- Supabase

\- Local Supabase for development

\- Vercel-ready deployment later



\## Current database reality

The local database already has:

\- tests

\- questions

\- answer\_options

\- attempts

\- responses

\- dimension\_scores



RLS is enabled.

Public read access is intended only for:

\- tests

\- questions

\- answer\_options



Write operations for attempts, responses, and dimension\_scores should be implemented server-side, not directly from the browser.



\## Engineering rules

\- Use App Router.

\- Use TypeScript.

\- Keep implementation simple and MVP-friendly.

\- Do not add dependencies unless clearly necessary.

\- Do not refactor unrelated code.

\- Do not hardcode test questions in React components.

\- Treat Supabase as the source of truth for runtime data.

\- Respect existing RLS rules.

\- Never expose secret keys in client-side code.

\- Prefer server-side data fetching when reasonable.

\- Prefer narrow, reviewable diffs.



\## Product constraints

\- No AI-generated report in the current phase.

\- No hiring recommendation logic.

\- No psychometric over-engineering yet.

\- Focus on one working vertical slice at a time.



\## Task priorities right now

1\. Read active test from Supabase

2\. Render questions and answer options

3\. Add server-side submission flow

4\. Add scoring logic

5\. Add AI reporting later



\## Validation

Before finishing any task:

\- run relevant validation commands

\- report what was changed

\- report assumptions

\- report remaining next step



\## Communication style

\- Be concise

\- State assumptions clearly

\- Keep changes easy to review
</file>

<file path="middleware.ts">
import { NextResponse, type NextRequest } from "next/server";
import { createClient } from "@supabase/supabase-js";
import {
  DEFAULT_REFRESH_COOKIE_MAX_AGE_SECONDS,
  SUPABASE_ACCESS_TOKEN_COOKIE_NAME,
  SUPABASE_REFRESH_TOKEN_COOKIE_NAME,
  getAuthCookieOptions,
} from "@/lib/auth/cookies";
import { getSupabaseEnv } from "@/lib/supabase/env";

function createSupabaseMiddlewareClient() {
  const { url, publishableKey } = getSupabaseEnv();

  return createClient(url, publishableKey, {
    auth: {
      autoRefreshToken: false,
      persistSession: false,
    },
  });
}

function persistSessionCookies(response: NextResponse, session: {
  access_token: string;
  refresh_token: string;
  expires_in?: number;
}) {
  const accessTokenMaxAge = session.expires_in ?? DEFAULT_REFRESH_COOKIE_MAX_AGE_SECONDS;

  response.cookies.set(
    SUPABASE_ACCESS_TOKEN_COOKIE_NAME,
    session.access_token,
    getAuthCookieOptions(accessTokenMaxAge),
  );
  response.cookies.set(
    SUPABASE_REFRESH_TOKEN_COOKIE_NAME,
    session.refresh_token,
    getAuthCookieOptions(DEFAULT_REFRESH_COOKIE_MAX_AGE_SECONDS),
  );
}

function clearSessionCookies(response: NextResponse) {
  response.cookies.set(SUPABASE_ACCESS_TOKEN_COOKIE_NAME, "", getAuthCookieOptions(0));
  response.cookies.set(SUPABASE_REFRESH_TOKEN_COOKIE_NAME, "", getAuthCookieOptions(0));
}

async function getUserFromAccessToken(accessToken: string) {
  const supabase = createSupabaseMiddlewareClient();
  return supabase.auth.getUser(accessToken);
}

async function refreshSession(refreshToken: string) {
  const supabase = createSupabaseMiddlewareClient();
  return supabase.auth.refreshSession({ refresh_token: refreshToken });
}

function getLoginUrl(request: NextRequest) {
  return new URL("/login", request.url);
}

function getDashboardUrl(request: NextRequest) {
  return new URL("/dashboard", request.url);
}

export async function middleware(request: NextRequest) {
  const accessToken = request.cookies.get(SUPABASE_ACCESS_TOKEN_COOKIE_NAME)?.value ?? null;
  const refreshToken = request.cookies.get(SUPABASE_REFRESH_TOKEN_COOKIE_NAME)?.value ?? null;
  const pathname = request.nextUrl.pathname;
  const isProtectedRoute = pathname.startsWith("/dashboard");
  const isLoginRoute = pathname === "/login";

  if (!isProtectedRoute && !isLoginRoute) {
    return NextResponse.next();
  }

  if (!accessToken && !refreshToken) {
    if (isProtectedRoute) {
      return NextResponse.redirect(getLoginUrl(request));
    }

    return NextResponse.next();
  }

  if (accessToken) {
    const { data, error } = await getUserFromAccessToken(accessToken);

    if (!error && data.user) {
      if (isLoginRoute) {
        return NextResponse.redirect(getDashboardUrl(request));
      }

      return NextResponse.next();
    }
  }

  if (!refreshToken) {
    const response = isProtectedRoute
      ? NextResponse.redirect(getLoginUrl(request))
      : NextResponse.next();
    clearSessionCookies(response);
    return response;
  }

  const { data, error } = await refreshSession(refreshToken);

  if (error || !data.session || !data.user) {
    const response = isProtectedRoute
      ? NextResponse.redirect(getLoginUrl(request))
      : NextResponse.next();
    clearSessionCookies(response);
    return response;
  }

  const response = isLoginRoute
    ? NextResponse.redirect(getDashboardUrl(request))
    : NextResponse.next();

  persistSessionCookies(response, data.session);
  return response;
}

export const config = {
  matcher: ["/dashboard/:path*", "/login"],
};
</file>

<file path="next-env.d.ts">
/// <reference types="next" />
/// <reference types="next/image-types/global" />

// NOTE: This file should not be edited
// see https://nextjs.org/docs/app/building-your-application/configuring/typescript for more information.
</file>

<file path="next.config.js">
/** @type {import('next').NextConfig} */
const nextConfig = {};

module.exports = nextConfig;
</file>

<file path="tsconfig.json">
{
  "compilerOptions": {
    "target": "ES2022",
    "lib": [
      "dom",
      "dom.iterable",
      "es2022"
    ],
    "allowJs": false,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "incremental": true,
    "plugins": [
      {
        "name": "next"
      }
    ],
    "baseUrl": ".",
    "paths": {
      "@/*": [
        "./*"
      ]
    }
  },
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts"
  ],
  "exclude": [
    "node_modules"
  ]
}
</file>

<file path="lib/assessment/report-config.ts">
import "server-only";

import type { ReportGeneratorType } from "@/lib/assessment/report-providers";

export type AiReportConfig = {
  provider: ReportGeneratorType;
  model: string | null;
  promptVersion: string;
  fallbackToMock: boolean;
  openAiApiKey: string | null;
  openAiTimeoutMs: number;
};

function normalizeBooleanEnv(value: string | undefined, defaultValue: boolean): boolean {
  if (!value) {
    return defaultValue;
  }

  return value.toLowerCase() === "true";
}

function normalizeTimeoutEnv(value: string | undefined, defaultValue: number): number {
  if (!value) {
    return defaultValue;
  }

  const parsed = Number.parseInt(value, 10);

  if (!Number.isFinite(parsed) || parsed <= 0) {
    return defaultValue;
  }

  return parsed;
}

export function getAiReportConfig(): AiReportConfig {
  const provider = (process.env.AI_REPORT_PROVIDER ?? "mock").toLowerCase();

  return {
    provider: provider === "openai" ? "openai" : "mock",
    model: process.env.AI_REPORT_MODEL ?? null,
    promptVersion: process.env.AI_REPORT_PROMPT_VERSION ?? "v1",
    fallbackToMock: normalizeBooleanEnv(process.env.AI_REPORT_FALLBACK_TO_MOCK, true),
    openAiApiKey: process.env.OPENAI_API_KEY ?? null,
    openAiTimeoutMs: normalizeTimeoutEnv(process.env.AI_REPORT_OPENAI_TIMEOUT_MS, 120000),
  };
}
</file>

<file path="lib/assessment/report-provider-helpers.ts">
import "server-only";

export { formatDimensionLabel } from "@/lib/assessment/result-display";
import type {
  AiReportPromptInput,
  CompletedAssessmentReportRequest,
  PreparedReportGenerationInput,
} from "@/lib/assessment/report-providers";

function roundScore(value: number): number {
  return Math.round(value * 100) / 100;
}

export function getAverageScore(rawScore: number, scoredQuestionCount: number): number {
  if (scoredQuestionCount === 0) {
    return 0;
  }

  return roundScore(rawScore / scoredQuestionCount);
}

function getRankedDimensions(input: CompletedAssessmentReportRequest) {
  return [...input.results.dimensions]
    .map((dimension) => ({
      ...dimension,
      averageScore: getAverageScore(dimension.rawScore, dimension.scoredQuestionCount),
    }))
    .sort(
      (left, right) =>
        right.averageScore - left.averageScore || left.dimension.localeCompare(right.dimension),
    );
}

export function buildAiReportPromptInput(
  input: CompletedAssessmentReportRequest,
): AiReportPromptInput {
  const rankedDimensions = getRankedDimensions(input);

  return {
    attempt_id: input.attemptId,
    test_slug: input.testSlug,
    scoring_method: input.scoringMethod,
    prompt_version: input.promptVersion,
    scored_response_count: input.results.scoredResponseCount,
    dimension_scores: rankedDimensions.map((dimension) => ({
      dimension_key: dimension.dimension,
      raw_score: dimension.rawScore,
      scored_question_count: dimension.scoredQuestionCount,
    })),
    deterministic_summary: {
      highest_dimension: rankedDimensions[0]?.dimension ?? null,
      lowest_dimension: rankedDimensions[rankedDimensions.length - 1]?.dimension ?? null,
      dimensions_ranked: rankedDimensions.map((dimension) => dimension.dimension),
    },
  };
}

export function buildPreparedReportGenerationInput(
  input: CompletedAssessmentReportRequest,
): PreparedReportGenerationInput {
  return {
    attemptId: input.attemptId,
    testSlug: input.testSlug,
    promptVersion: input.promptVersion,
    promptInput: buildAiReportPromptInput(input),
  };
}
</file>

<file path="lib/assessment/report-provider-mock.ts">
import "server-only";

import type {
  CompletedAssessmentReport,
  PreparedReportGenerationInput,
  ReportProvider,
} from "@/lib/assessment/report-providers";
import {
  formatDimensionLabel,
  getAverageScore,
} from "@/lib/assessment/report-provider-helpers";

function getScoreBand(score: number): "high" | "mid" | "low" {
  if (score >= 3.67) {
    return "high";
  }

  if (score >= 2.34) {
    return "mid";
  }

  return "low";
}

function getDimensionInterpretation(dimensionKey: string, averageScore: number): string {
  const band = getScoreBand(averageScore);

  const interpretationsByDimension: Record<string, Record<"high" | "mid" | "low", string>> = {
    extraversion: {
      high: "Često djeluje energizirano kroz socijalni kontakt i vidljivo uključivanje.",
      mid: "Pokazuje uravnotežen spoj otvorenog angažmana i promišljenijeg tempa.",
      low: "Može preferirati mirnije okruženje i odmjereniji interpersonalni ritam.",
    },
    agreeableness: {
      high: "Naglašava saradnju, taktičnost i kvalitet međuljudskih odnosa.",
      mid: "Može dobro balansirati iskrenost i saradnju, zavisno od konteksta.",
      low: "Može češće birati direktan izazov umjesto prilagođavanja ili konsenzusa.",
    },
    conscientiousness: {
      high: "Vjerovatno vrednuje strukturu, dosljednost i pouzdanu realizaciju.",
      mid: "Može se prilagođavati između planiranja i fleksibilnosti kako se zahtjevi mijenjaju.",
      low: "Može raditi spontanije i imati korist od jasnije vanjske strukture.",
    },
    emotional_stability: {
      high: "Vjerovatno zadržava stabilnost i pod uobičajenim pritiskom.",
      mid: "Pokazuje mješovit profil nošenja sa stresom koji može varirati po opterećenju ili kontekstu.",
      low: "Može intenzivnije doživljavati pritisak i imati korist od stabilnijih navika oporavka.",
    },
    intellect: {
      high: "Često je usmjeren prema idejama, istraživanju i konceptualnoj raznolikosti.",
      mid: "Može prihvatati nove ideje uz zadržavanje vrijednosti poznatih pristupa.",
      low: "Može preferirati praktičnu jasnoću umjesto apstraktnog istraživanja.",
    },
  };

  const fallback = {
    high: "Ova oblast je relativno izražena u trenutnom obrascu odgovora.",
    mid: "Ova oblast je u srednjem rasponu u trenutnom obrascu odgovora.",
    low: "Ova oblast je relativno niže izražena u trenutnom obrascu odgovora.",
  };

  return (interpretationsByDimension[dimensionKey] ?? fallback)[band];
}

function buildMockReport(input: PreparedReportGenerationInput): CompletedAssessmentReport {
  const { promptInput } = input;
  const primaryDimension = promptInput.deterministic_summary.highest_dimension;
  const lowestDimension = promptInput.deterministic_summary.lowest_dimension;
  const secondaryDimension = promptInput.deterministic_summary.dimensions_ranked[1] ?? null;

  const dimensions = promptInput.dimension_scores
    .map((dimension) => ({
      dimension_key: dimension.dimension_key,
      score: dimension.raw_score,
      short_interpretation: getDimensionInterpretation(
        dimension.dimension_key,
        getAverageScore(dimension.raw_score, dimension.scored_question_count),
      ),
    }))
    .sort(
      (left, right) =>
        right.score - left.score || left.dimension_key.localeCompare(right.dimension_key),
    );

  const summaryParts = [
    `Izvještaj za ${input.testSlug} zasnovan je na pohranjenim skorovima metodologije ${promptInput.scoring_method}.`,
    primaryDimension
      ? `${formatDimensionLabel(primaryDimension)} je najuočljiviji signal u ovom pokušaju.`
      : "Za ovaj pokušaj nisu bili dostupni skorovi po dimenzijama.",
    lowestDimension && lowestDimension !== primaryDimension
      ? `${formatDimensionLabel(lowestDimension)} je komparativno niže izražena i treba je čitati kao razvojnu oblast, a ne kao nedostatak.`
      : null,
  ].filter((part): part is string => Boolean(part));

  const strengths = promptInput.deterministic_summary.dimensions_ranked
    .slice(0, 2)
    .map((dimensionKey) => {
      const matchingDimension = promptInput.dimension_scores.find(
        (dimension) => dimension.dimension_key === dimensionKey,
      );
      const averageScore = matchingDimension
        ? getAverageScore(matchingDimension.raw_score, matchingDimension.scored_question_count)
        : 0;

      return `${formatDimensionLabel(dimensionKey)}: ${getDimensionInterpretation(dimensionKey, averageScore)}`;
    });

  const blindSpots = lowestDimension
    ? [
        `${formatDimensionLabel(lowestDimension)} je niže izražena u ovom obrascu odgovora, pa osoba može rjeđe koristiti ponašanja vezana za tu oblast u pojedinim situacijama.`,
        "Narativne zaključke treba provjeravati kroz stvarno ponašanje, kontekst uloge i praktična opažanja.",
      ]
    : ["Tumačenje potencijalnih slijepih tačaka nije dostupno jer nisu pronađene bodovane dimenzije."];

  const workStyle = [
    primaryDimension
      ? `Vjerovatno glavno uporište radnog stila: teme povezane sa dimenzijom ${formatDimensionLabel(primaryDimension).toLowerCase()} najizraženije su u ovom završenom pokušaju.`
      : "Glavno uporište radnog stila nije moguće procijeniti bez bodovanih dimenzija.",
    secondaryDimension
      ? `Sekundarni signal: ${formatDimensionLabel(secondaryDimension).toLowerCase()} također značajno oblikuje način na koji se ukupni profil može ispoljavati iz dana u dan.`
      : "Sekundarni signal radnog stila nije bio dostupan iz trenutnog skupa skorova.",
  ];

  const developmentRecommendations = lowestDimension
    ? [
        `Uvedi jednu namjernu razvojnu naviku povezanu s ponašanjima iz oblasti ${formatDimensionLabel(lowestDimension).toLowerCase()} u sedmične radne rutine.`,
        primaryDimension
          ? `Iskoristi izraženiji obrazac u oblasti ${formatDimensionLabel(primaryDimension).toLowerCase()} kao oslonac dok se gradi veći raspon ponašanja u niže bodovanim oblastima.`
          : "Koristi redovnu refleksiju o obrascima skorova kako bi se rezultati pretvorili u konkretne bihevioralne eksperimente.",
      ]
    : ["Prikupi potpuniji bodovani pokušaj prije donošenja preporuka za razvoj."];

  return {
    attempt_id: input.attemptId,
    test_slug: input.testSlug,
    generated_at: new Date().toISOString(),
    generator_type: "mock",
    summary: summaryParts.join(" "),
    dimensions,
    strengths,
    blind_spots: blindSpots,
    work_style: workStyle,
    development_recommendations: developmentRecommendations,
    disclaimer:
      "Ovaj izvještaj je zasnovan na determinističkim scoring podacima. Opisuje tendencije i razvojne teme, a ne dijagnozu, kliničku procjenu niti preporuku za zapošljavanje.",
  };
}

export const mockReportProvider: ReportProvider = {
  type: "mock",
  async generateReport(input) {
    return {
      ok: true,
      report: buildMockReport(input),
    };
  },
};
</file>

<file path="lib/assessment/report-provider-registry.ts">
import "server-only";

import { getAiReportConfig } from "@/lib/assessment/report-config";
import { mockReportProvider } from "@/lib/assessment/report-provider-mock";
import { createOpenAiReportProvider } from "@/lib/assessment/report-provider-openai";
import type { ReportProvider } from "@/lib/assessment/report-providers";

export function createSelectedReportProvider(): ReportProvider {
  const config = getAiReportConfig();

  if (config.provider === "openai") {
    return createOpenAiReportProvider({
      apiKey: config.openAiApiKey,
      model: config.model,
      timeoutMs: config.openAiTimeoutMs,
    });
  }

  return mockReportProvider;
}
</file>

<file path="lib/assessment/report-providers.ts">
import "server-only";

import type { CompletedAssessmentResults } from "@/lib/assessment/scoring";
import type { ScoringMethod } from "@/lib/assessment/types";

export type ReportGeneratorType = "mock" | "openai";
export type AttemptReportStatus = "ready" | "unavailable";

export type CompletedAssessmentReportDimension = {
  dimension_key: string;
  score: number;
  short_interpretation: string;
};

export type CompletedAssessmentReport = {
  attempt_id: string;
  test_slug: string;
  generated_at: string;
  generator_type: ReportGeneratorType;
  summary: string;
  dimensions: CompletedAssessmentReportDimension[];
  strengths: string[];
  blind_spots: string[];
  work_style: string[];
  development_recommendations: string[];
  disclaimer: string;
};

export type CompletedAssessmentReportRequest = {
  attemptId: string;
  testSlug: string;
  scoringMethod: ScoringMethod;
  promptVersion: string;
  results: CompletedAssessmentResults;
};

export type AiReportDimensionInput = {
  dimension_key: string;
  raw_score: number;
  scored_question_count: number;
};

export type AiReportPromptInput = {
  attempt_id: string;
  test_slug: string;
  scoring_method: ScoringMethod;
  prompt_version: string;
  scored_response_count: number;
  dimension_scores: AiReportDimensionInput[];
  deterministic_summary: {
    highest_dimension: string | null;
    lowest_dimension: string | null;
    dimensions_ranked: string[];
  };
};

export type PreparedReportGenerationInput = {
  attemptId: string;
  testSlug: string;
  promptVersion: string;
  promptInput: AiReportPromptInput;
};

export type ReportProviderResult =
  | {
      ok: true;
      report: CompletedAssessmentReport;
    }
  | {
      ok: false;
      reason: string;
    };

export type ReportProvider = {
  type: ReportGeneratorType;
  generateReport: (input: PreparedReportGenerationInput) => Promise<ReportProviderResult>;
};

function isStringArray(value: unknown): value is string[] {
  return Array.isArray(value) && value.every((item) => typeof item === "string");
}

function isReportDimension(value: unknown): value is CompletedAssessmentReportDimension {
  if (!value || typeof value !== "object") {
    return false;
  }

  const dimension = value as Record<string, unknown>;

  return (
    typeof dimension.dimension_key === "string" &&
    typeof dimension.score === "number" &&
    typeof dimension.short_interpretation === "string"
  );
}

export function isCompletedAssessmentReport(value: unknown): value is CompletedAssessmentReport {
  if (!value || typeof value !== "object") {
    return false;
  }

  const report = value as Record<string, unknown>;
  const generatorType = report.generator_type;

  return (
    typeof report.attempt_id === "string" &&
    typeof report.test_slug === "string" &&
    typeof report.generated_at === "string" &&
    (generatorType === "mock" || generatorType === "openai") &&
    typeof report.summary === "string" &&
    Array.isArray(report.dimensions) &&
    report.dimensions.every(isReportDimension) &&
    isStringArray(report.strengths) &&
    isStringArray(report.blind_spots) &&
    isStringArray(report.work_style) &&
    isStringArray(report.development_recommendations) &&
    typeof report.disclaimer === "string"
  );
}

export function isAttemptReportStatus(value: unknown): value is AttemptReportStatus {
  return value === "ready" || value === "unavailable";
}
</file>

<file path="lib/assessment/result-display.ts">
const DIMENSION_DISPLAY_LABELS: Record<string, string> = {
  extraversion: "Ekstraverzija",
  agreeableness: "Kooperativnost",
  conscientiousness: "Savjesnost",
  emotional_stability: "Emocionalna stabilnost",
  neuroticism: "Emocionalna stabilnost",
  intellect: "Otvorenost prema iskustvu",
  openness: "Otvorenost prema iskustvu",
  openness_to_experience: "Otvorenost prema iskustvu",
};

const DIMENSION_HELPER_LABELS: Record<string, string> = {
  ekstraverzija: "socijalna energija i izražavanje",
  kooperativnost: "saradnja, obzirnost i odnos prema drugima",
  savjesnost: "organizacija, disciplina i odgovornost",
  "emocionalna stabilnost": "reakcija na stres, pritisak i neizvjesnost",
  "otvorenost prema iskustvu": "odnos prema novim idejama, učenju i promjenama",
};

export function formatDimensionLabel(dimensionKey: string): string {
  const normalizedKey = dimensionKey.trim().toLowerCase();

  if (DIMENSION_DISPLAY_LABELS[normalizedKey]) {
    return DIMENSION_DISPLAY_LABELS[normalizedKey];
  }

  return dimensionKey
    .split(/[_-]+/)
    .map((part) => part.charAt(0).toUpperCase() + part.slice(1))
    .join(" ");
}

export function formatScoreLabel(score: number): string {
  const absoluteScore = Math.abs(score);
  const lastTwoDigits = absoluteScore % 100;
  const lastDigit = absoluteScore % 10;

  if (lastTwoDigits < 11 || lastTwoDigits > 14) {
    if (lastDigit === 1) {
      return `${score} bod`;
    }

    if (lastDigit >= 2 && lastDigit <= 4) {
      return `${score} boda`;
    }
  }

  return `${score} bodova`;
}

export function getDimensionHelperLabel(dimensionKey: string): string | null {
  const label = formatDimensionLabel(dimensionKey).trim().toLowerCase();
  return DIMENSION_HELPER_LABELS[label] ?? null;
}
</file>

<file path="lib/b2b/organizations.ts">
import "server-only";

import { createSupabaseAdminClient } from "@/lib/supabase/admin";

export type OrganizationSummary = {
  id: string;
  name: string;
  slug: string;
  status: "active" | "inactive";
};

export type MembershipSummary = {
  id: string;
  role: "org_owner" | "hr_admin" | "manager";
  status: "active" | "invited" | "disabled";
  created_at: string;
  organization: OrganizationSummary | null;
};

export type ParticipantSummary = {
  id: string;
  organization_id: string;
  user_id: string | null;
  email: string;
  full_name: string;
  participant_type: "employee" | "candidate";
  status: "active" | "inactive";
  created_at: string;
};

export type OrganizationScopedAttemptSummary = {
  id: string;
  test_id: string;
  user_id: string | null;
  organization_id: string | null;
  participant_id: string | null;
  status: "in_progress" | "completed" | "abandoned";
  started_at: string;
  completed_at: string | null;
  tests: {
    slug: string;
    name: string;
  } | null;
  participants: {
    id: string;
    organization_id: string;
    full_name: string;
    email: string;
  } | null;
  organizations: {
    name: string;
    slug: string;
  } | null;
};

type AttemptRelation<T> = T | T[] | null;

type MembershipRow = {
  id: string;
  role: MembershipSummary["role"];
  status: MembershipSummary["status"];
  created_at: string;
  organizations:
    | OrganizationSummary
    | OrganizationSummary[]
    | null;
};

type AttemptRow = {
  id: string;
  test_id: string;
  user_id: string | null;
  organization_id: string | null;
  participant_id: string | null;
  status: OrganizationScopedAttemptSummary["status"];
  started_at: string;
  completed_at: string | null;
  tests: AttemptRelation<OrganizationScopedAttemptSummary["tests"] extends infer T ? NonNullable<T> : never>;
  participants: AttemptRelation<
    OrganizationScopedAttemptSummary["participants"] extends infer T ? NonNullable<T> : never
  >;
  organizations: AttemptRelation<
    OrganizationScopedAttemptSummary["organizations"] extends infer T ? NonNullable<T> : never
  >;
};

function normalizeOrganization(
  value: MembershipRow["organizations"],
): OrganizationSummary | null {
  if (!value) {
    return null;
  }

  return Array.isArray(value) ? value[0] ?? null : value;
}

function normalizeAttemptRelation<T>(value: AttemptRelation<T>): T | null {
  if (!value) {
    return null;
  }

  return Array.isArray(value) ? value[0] ?? null : value;
}

export async function getMembershipsForUser(userId: string): Promise<MembershipSummary[]> {
  const supabase = createSupabaseAdminClient();
  const { data, error } = await supabase
    .from("organization_memberships")
    .select("id, role, status, created_at, organizations(id, name, slug, status)")
    .eq("user_id", userId)
    .order("created_at", { ascending: true })
    .order("id", { ascending: true });

  if (error) {
    throw new Error(`Failed to load organization memberships: ${error.message}`);
  }

  return ((data ?? []) as MembershipRow[]).map((membership) => ({
    id: membership.id,
    role: membership.role,
    status: membership.status,
    created_at: membership.created_at,
    organization: normalizeOrganization(membership.organizations),
  }));
}

export async function getActiveOrganizationForUser(
  userId: string,
): Promise<OrganizationSummary | null> {
  const memberships = await getMembershipsForUser(userId);

  const activeMembership = memberships.find(
    (membership) =>
      membership.status === "active" && membership.organization?.status === "active",
  );

  return activeMembership?.organization ?? null;
}

export async function getParticipantsForOrganization(
  organizationId: string,
): Promise<ParticipantSummary[]> {
  const supabase = createSupabaseAdminClient();
  const { data, error } = await supabase
    .from("participants")
    .select("id, organization_id, user_id, email, full_name, participant_type, status, created_at")
    .eq("organization_id", organizationId)
    .eq("status", "active")
    .order("created_at", { ascending: true })
    .order("id", { ascending: true });

  if (error) {
    throw new Error(`Failed to load organization participants: ${error.message}`);
  }

  return (data ?? []) as ParticipantSummary[];
}

export async function getParticipantForOrganization(
  organizationId: string,
  participantId: string,
): Promise<ParticipantSummary | null> {
  const supabase = createSupabaseAdminClient();
  const { data, error } = await supabase
    .from("participants")
    .select("id, organization_id, user_id, email, full_name, participant_type, status, created_at")
    .eq("organization_id", organizationId)
    .eq("id", participantId)
    .eq("status", "active")
    .maybeSingle();

  if (error) {
    throw new Error(`Failed to load organization participant: ${error.message}`);
  }

  return (data as ParticipantSummary | null) ?? null;
}

export async function getAttemptForOrganization(
  organizationId: string,
  attemptId: string,
): Promise<OrganizationScopedAttemptSummary | null> {
  const supabase = createSupabaseAdminClient();
  const { data, error } = await supabase
    .from("attempts")
    .select(
      "id, test_id, user_id, organization_id, participant_id, status, started_at, completed_at, tests(slug, name), participants(id, organization_id, full_name, email), organizations(name, slug)",
    )
    .eq("id", attemptId)
    .eq("organization_id", organizationId)
    .maybeSingle();

  if (error) {
    throw new Error(`Failed to load organization attempt: ${error.message}`);
  }

  const attemptRow = (data as AttemptRow | null) ?? null;

  if (!attemptRow) {
    return null;
  }

  const attempt: OrganizationScopedAttemptSummary = {
    ...attemptRow,
    tests: normalizeAttemptRelation(attemptRow.tests),
    participants: normalizeAttemptRelation(attemptRow.participants),
    organizations: normalizeAttemptRelation(attemptRow.organizations),
  };

  if (
    attempt.participants &&
    attempt.participants.organization_id !== organizationId
  ) {
    return null;
  }

  return attempt;
}

export async function getAttemptsForOrganization(
  organizationId: string,
): Promise<OrganizationScopedAttemptSummary[]> {
  const supabase = createSupabaseAdminClient();
  const { data, error } = await supabase
    .from("attempts")
    .select(
      "id, test_id, user_id, organization_id, participant_id, status, started_at, completed_at, tests(slug, name), participants(id, organization_id, full_name, email), organizations(name, slug)",
    )
    .eq("organization_id", organizationId)
    .order("started_at", { ascending: false })
    .order("id", { ascending: false });

  if (error) {
    throw new Error(`Failed to load organization attempts: ${error.message}`);
  }

  return ((data ?? []) as AttemptRow[])
    .map((attempt) => ({
      ...attempt,
      tests: normalizeAttemptRelation(attempt.tests),
      participants: normalizeAttemptRelation(attempt.participants),
      organizations: normalizeAttemptRelation(attempt.organizations),
    }))
    .filter(
      (attempt) =>
        !attempt.participants || attempt.participants.organization_id === organizationId,
    )
    .sort((left, right) => {
      const leftPriority = left.status === "in_progress" ? 0 : 1;
      const rightPriority = right.status === "in_progress" ? 0 : 1;

      if (leftPriority !== rightPriority) {
        return leftPriority - rightPriority;
      }

      return Date.parse(right.started_at) - Date.parse(left.started_at);
    });
}
</file>

<file path="lib/supabase/env.ts">
function getRequiredEnvVar(value: string | undefined, name: string): string {
  if (!value) {
    throw new Error(`Missing required env var: ${name}`);
  }

  return value;
}

export function getSupabaseEnv() {
  return {
    url: getRequiredEnvVar(
      process.env.NEXT_PUBLIC_SUPABASE_URL,
      "NEXT_PUBLIC_SUPABASE_URL",
    ),
    publishableKey: getRequiredEnvVar(
      process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY,
      "NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY",
    ),
  };
}
</file>

<file path="lib/supabase/server.ts">
import { createClient } from "@supabase/supabase-js";
import { getSupabaseEnv } from "./env";

export function createSupabaseServerClient() {
  const { url, publishableKey } = getSupabaseEnv();
  return createClient(url, publishableKey);
}

export function createSupabaseAuthClient() {
  const { url, publishableKey } = getSupabaseEnv();

  return createClient(url, publishableKey, {
    auth: {
      autoRefreshToken: false,
      persistSession: false,
    },
  });
}
</file>

<file path="supabase/snippets/Untitled query 398.sql">
select id, test_id, status, started_at, completed_at
from public.attempts
order by started_at desc
limit 5;
</file>

<file path="package.json">
{
  "name": "ai-psychometric-analyst-hr-v1",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "typecheck": "tsc --noEmit",
    "check:supabase": "node --env-file=.env.local scripts/check-supabase.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.57.4",
    "next": "^14.2.5",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.11",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "eslint": "^8.57.0",
    "eslint-config-next": "^14.2.5",
    "supabase": "^2.76.8",
    "typescript": "^5.5.4"
  }
}
</file>

<file path="app/(protected)/dashboard/attempts/[attemptId]/run/page.tsx">
import { notFound, redirect } from "next/navigation";
import { AssessmentForm } from "@/components/assessment/assessment-form";
import {
  getActiveOrganizationForUser,
  getAttemptForOrganization,
} from "@/lib/b2b/organizations";
import { requireAuthenticatedUser } from "@/lib/auth/session";
import {
  getAnswerOptionsForQuestions,
  getAssessmentResumeState,
  getCompletedAssessmentReportSnapshot,
  getCompletedAssessmentResults,
  getQuestionsForTest,
} from "@/lib/assessment/tests";
import { getAssessmentDisplayName } from "@/lib/assessment/display";

type ProtectedAttemptRunPageProps = {
  params: {
    attemptId: string;
  };
};

export const dynamic = "force-dynamic";

export default async function ProtectedAttemptRunPage({
  params,
}: ProtectedAttemptRunPageProps) {
  const user = await requireAuthenticatedUser();
  const organization = await getActiveOrganizationForUser(user.id);

  if (!organization) {
    notFound();
  }

  const attempt = await getAttemptForOrganization(organization.id, params.attemptId);

  if (!attempt) {
    notFound();
  }

  if (attempt.status === "completed") {
    redirect(`/dashboard/attempts/${attempt.id}`);
  }

  const questions = await getQuestionsForTest(attempt.test_id);

  if (questions.length === 0) {
    return (
      <main className="stack-md">
        <section className="card stack-sm">
          <h1>{attempt.tests?.name ?? attempt.tests?.slug ?? "Assessment"}</h1>
          <p>No questions are available for this test yet.</p>
        </section>
      </main>
    );
  }

  const answerOptionsByQuestionId = await getAnswerOptionsForQuestions(
    questions.map((question) => question.id),
  );
  const resumeState = await getAssessmentResumeState(attempt.test_id, attempt.id);
  const results = await getCompletedAssessmentResults(attempt.test_id, attempt.id);
  const report = await getCompletedAssessmentReportSnapshot(attempt.test_id, attempt.id);
  const assessmentName = getAssessmentDisplayName({
    name: attempt.tests?.name,
    slug: attempt.tests?.slug,
  });
  const participantName = attempt.participants?.full_name ?? null;

  return (
    <main className="assessment-run-page stack-md">
      <section className="assessment-run-shell">
        <AssessmentForm
          executionMode="protected"
          layoutMode="step"
          completionRedirectPath={`/dashboard/attempts/${attempt.id}`}
          assessmentDisplayName={assessmentName}
          participantDisplayName={participantName}
          testId={attempt.test_id}
          questions={questions}
          answerOptionsByQuestionId={answerOptionsByQuestionId}
          initialAttemptId={resumeState.attemptId}
          initialAttemptStatus={resumeState.attemptStatus}
          initialCompletedAt={resumeState.completedAt}
          initialSelections={resumeState.selections}
          initialResults={results}
          initialReport={report}
        />
      </section>
    </main>
  );
}
</file>

<file path="app/(protected)/dashboard/page.tsx">
import Link from "next/link";
import { createB2BAttempt } from "@/app/actions/assessment";
import { logout } from "@/app/actions/auth";
import {
  getActiveOrganizationForUser,
  getAttemptsForOrganization,
  getMembershipsForUser,
  getParticipantsForOrganization,
  type MembershipSummary,
} from "@/lib/b2b/organizations";
import { requireAuthenticatedUser } from "@/lib/auth/session";

const DEFAULT_B2B_TEST_SLUG = "ipip50-hr-v1";

type DashboardPageProps = {
  searchParams?: Record<string, string | string[] | undefined>;
};

function getOrganizationName(membership: MembershipSummary): string {
  return membership.organization?.name ?? "Unknown organization";
}

function getDashboardMessage(rawError: string | string[] | undefined): string | null {
  const error = Array.isArray(rawError) ? rawError[0] : rawError;

  switch (error) {
    case "missing-participant":
      return "Select a participant before creating an attempt.";
    case "no-active-organization":
      return "No active organization is available for this user.";
    case "participant-not-found":
      return "The selected participant was not found in the active organization.";
    case "test-not-found":
      return "The configured test slug could not be resolved.";
    case "create-attempt-failed":
      return "Unable to create the attempt right now. Please try again.";
    default:
      return null;
  }
}

function formatTimestamp(value: string | null): string {
  if (!value) {
    return "N/A";
  }

  return new Date(value).toLocaleString();
}

function getAttemptLabel(attemptId: string): string {
  return attemptId.length > 8 ? `${attemptId.slice(0, 8)}...` : attemptId;
}

export const dynamic = "force-dynamic";

export default async function DashboardPage({ searchParams }: DashboardPageProps) {
  const user = await requireAuthenticatedUser();
  const [memberships, activeOrganization] = await Promise.all([
    getMembershipsForUser(user.id),
    getActiveOrganizationForUser(user.id),
  ]);
  const [participants, attempts] = activeOrganization
    ? await Promise.all([
        getParticipantsForOrganization(activeOrganization.id),
        getAttemptsForOrganization(activeOrganization.id),
      ])
    : [[], []];
  const message = getDashboardMessage(searchParams?.error);

  return (
    <main className="stack-md">
      <section className="card stack-sm">
        <div className="stack-xs">
          <h1>Dashboard</h1>
          <p>Signed in as {user.email ?? user.id}</p>
          <p>
            Active organization: {activeOrganization ? activeOrganization.name : "No active organization"}
          </p>
          {activeOrganization ? (
            <p>
              Current B2B test: <code>{DEFAULT_B2B_TEST_SLUG}</code>
            </p>
          ) : null}
          {memberships.length > 1 ? (
            <p>TODO: add an explicit organization switcher when multi-org selection becomes necessary.</p>
          ) : null}
          {message ? <p>{message}</p> : null}
        </div>

        <form action={logout}>
          <button type="submit">Sign out</button>
        </form>
      </section>

      <section className="card stack-sm">
        <h2>Memberships</h2>

        {memberships.length === 0 ? (
          <p>No organization memberships found for this user yet.</p>
        ) : (
          <ul>
            {memberships.map((membership) => (
              <li key={membership.id}>
                {getOrganizationName(membership)} ({membership.role}, {membership.status})
              </li>
            ))}
          </ul>
        )}
      </section>

      <section className="card stack-sm">
        <h2>Participants</h2>

        {!activeOrganization ? (
          <p>This user does not have an active organization yet, so no B2B attempts can be created.</p>
        ) : participants.length === 0 ? (
          <p>No active participants found for {activeOrganization.name}.</p>
        ) : (
          <ul>
            {participants.map((participant) => (
              <li key={participant.id}>
                <div className="stack-xs">
                  <p>
                    <strong>{participant.full_name}</strong> ({participant.email})
                  </p>
                  <p>
                    {participant.participant_type} {participant.user_id ? "· linked user" : "· no linked user"}
                  </p>
                  <form action={createB2BAttempt}>
                    <input type="hidden" name="participantId" value={participant.id} />
                    <input type="hidden" name="testSlug" value={DEFAULT_B2B_TEST_SLUG} />
                    <button type="submit">Create IPIP-50 attempt</button>
                  </form>
                </div>
              </li>
            ))}
          </ul>
        )}
      </section>

      <section className="card stack-sm">
        <h2>Attempts</h2>

        {!activeOrganization ? (
          <p>This user does not have an active organization yet, so no organization-scoped attempts are available.</p>
        ) : attempts.length === 0 ? (
          <div className="stack-xs">
            <p>No attempts exist for {activeOrganization.name} yet.</p>
            <p>Create the first attempt for a participant above to start the protected assessment flow.</p>
          </div>
        ) : (
          <ul>
            {attempts.map((attempt) => {
              const detailHref = `/dashboard/attempts/${attempt.id}`;
              const continueHref = `${detailHref}/run`;
              const participantName = attempt.participants?.full_name ?? attempt.participant_id ?? "Unknown participant";
              const participantEmail = attempt.participants?.email ?? "N/A";
              const testLabel = attempt.tests?.name ?? attempt.tests?.slug ?? "Unknown test";
              const activityLabel =
                attempt.status === "completed"
                  ? `Completed ${formatTimestamp(attempt.completed_at)}`
                  : `Started ${formatTimestamp(attempt.started_at)}`;

              return (
                <li key={attempt.id}>
                  <div className="stack-xs">
                    <p>
                      <strong>{participantName}</strong> ({participantEmail})
                    </p>
                    <p>
                      Attempt {getAttemptLabel(attempt.id)} · {testLabel} · {attempt.status}
                    </p>
                    <p>{activityLabel}</p>
                    {attempt.user_id ? <p>Owner: {attempt.user_id}</p> : null}
                    <p>
                      <Link href={detailHref}>Open attempt</Link>
                      {" · "}
                      {attempt.status === "completed" ? (
                        <Link href={detailHref}>View results</Link>
                      ) : (
                        <Link href={continueHref}>Continue assessment</Link>
                      )}
                    </p>
                  </div>
                </li>
              );
            })}
          </ul>
        )}
      </section>
    </main>
  );
}
</file>

<file path="lib/assessment/report-provider-openai.ts">
import "server-only";

import { formatDimensionLabel } from "@/lib/assessment/report-provider-helpers";
import type {
  CompletedAssessmentReport,
  CompletedAssessmentReportDimension,
  PreparedReportGenerationInput,
  ReportProvider,
} from "@/lib/assessment/report-providers";

type OpenAiProviderOptions = {
  apiKey: string | null;
  model: string | null;
  timeoutMs?: number;
};

type OpenAiStructuredReport = {
  summary: string;
  dimensions: CompletedAssessmentReportDimension[];
  strengths: string[];
  blind_spots: string[];
  work_style: string[];
  development_recommendations: string[];
  disclaimer: string;
};

type ErrorWithCause = Error & {
  cause?: unknown;
};

const OPENAI_REPORT_SCHEMA = {
  name: "assessment_report_v1",
  strict: true,
  schema: {
    type: "object",
    additionalProperties: false,
    properties: {
      summary: { type: "string" },
      dimensions: {
        type: "array",
        items: {
          type: "object",
          additionalProperties: false,
          properties: {
            dimension_key: { type: "string" },
            score: { type: "number" },
            short_interpretation: { type: "string" },
          },
          required: ["dimension_key", "score", "short_interpretation"],
        },
      },
      strengths: { type: "array", items: { type: "string" } },
      blind_spots: { type: "array", items: { type: "string" } },
      work_style: { type: "array", items: { type: "string" } },
      development_recommendations: { type: "array", items: { type: "string" } },
      disclaimer: { type: "string" },
    },
    required: [
      "summary",
      "dimensions",
      "strengths",
      "blind_spots",
      "work_style",
      "development_recommendations",
      "disclaimer",
    ],
  },
} as const;

function isStructuredReport(value: unknown): value is OpenAiStructuredReport {
  if (!value || typeof value !== "object") {
    return false;
  }

  const report = value as Record<string, unknown>;

  return (
    typeof report.summary === "string" &&
    Array.isArray(report.dimensions) &&
    report.dimensions.every((dimension) => {
      if (!dimension || typeof dimension !== "object") {
        return false;
      }

      const item = dimension as Record<string, unknown>;
      return (
        typeof item.dimension_key === "string" &&
        typeof item.score === "number" &&
        typeof item.short_interpretation === "string"
      );
    }) &&
    Array.isArray(report.strengths) &&
    report.strengths.every((item) => typeof item === "string") &&
    Array.isArray(report.blind_spots) &&
    report.blind_spots.every((item) => typeof item === "string") &&
    Array.isArray(report.work_style) &&
    report.work_style.every((item) => typeof item === "string") &&
    Array.isArray(report.development_recommendations) &&
    report.development_recommendations.every((item) => typeof item === "string") &&
    typeof report.disclaimer === "string"
  );
}

function buildSystemPrompt(promptVersion: string): string {
  return [
    `Generišeš strukturirane Big Five assessment izvještaje. Verzija prompta: ${promptVersion}.`,
    "Cjelokupan narativni sadržaj mora biti isključivo na bosanskom jeziku, ijekavica, latinica.",
    "Ne miješaj engleski sa bosanskim u naslovima, opisima, preporukama ni disclaimeru.",
    "Koristi isključivo dostavljeni deterministički scoring kontekst.",
    "Ne zaključuj niti računaj skorove iz sirovih odgovora.",
    "Ne mijenjaj dostavljene dimension keys niti score vrijednosti; vrati ih tačno kako su zadani.",
    "Opisuj tendencije i vjerovatne obrasce ponašanja, ne fiksne osobine niti sigurnost.",
    "Ne navodi dijagnoze, kliničke tvrdnje, zaključke o zaštićenim kategorijama ni savjete o tretmanu.",
    "Ne daj preporuke za zaposliti/ne zaposliti niti selekcijske odluke.",
    "Ton mora biti profesionalan, jasan, prirodan i upotrebljiv za HR/B2B izvještaj.",
    "Vrati isključivo JSON koji odgovara dostavljenoj shemi.",
  ].join(" ");
}

function buildUserPrompt(input: PreparedReportGenerationInput): string {
  const { promptInput } = input;
  const dimensionHints = promptInput.dimension_scores
    .map(
      (dimension) =>
        `${formatDimensionLabel(dimension.dimension_key)}: sirovi skor ${dimension.raw_score} kroz ${dimension.scored_question_count} bodovanih pitanja.`,
    )
    .join(" ");

  return JSON.stringify({
    instructions: {
      report_goal:
        "Napiši sažet, strukturiran assessment izvještaj zasnovan isključivo na dostavljenim determinističkim skorovima.",
      guardrails: [
        "Sav tekst mora biti na bosanskom jeziku, ijekavica, latinica.",
        "Skorovi su već izračunati i jedini su kvantitativni izvor istine.",
        "Ne spekuliraj iz sirovih odgovora niti iz nedostajućih podataka.",
        "Ne prenaglašavaj sigurnost zaključaka.",
        "Koristi razvojni jezik i izbjegavaj dijagnostičke ili hiring zaključke.",
        "Ako koristiš stručni termin, formuliraj ga prirodno za poslovni i HR kontekst na bosanskom jeziku.",
      ],
      dimension_hint_text: dimensionHints,
    },
    input: promptInput,
  });
}

function containsEnglishLeakage(value: string): boolean {
  const normalized = value.toLowerCase();
  const englishMarkers = [
    "report",
    "summary",
    "strengths",
    "blind spots",
    "work style",
    "development recommendations",
    "candidate",
    "score pattern",
    "raw score",
    "scored questions",
    "likely",
    "attempt",
    "completed",
    "hiring advice",
    "diagnosis",
  ];

  return englishMarkers.some((marker) => normalized.includes(marker));
}

function assertBosnianNarrative(report: OpenAiStructuredReport): void {
  const narrativeFields = [
    report.summary,
    report.disclaimer,
    ...report.dimensions.map((dimension) => dimension.short_interpretation),
    ...report.strengths,
    ...report.blind_spots,
    ...report.work_style,
    ...report.development_recommendations,
  ];

  if (narrativeFields.some((field) => containsEnglishLeakage(field))) {
    throw new Error("OpenAI report narrative did not pass the Bosnian-language consistency check.");
  }
}

function normalizeStructuredReport(
  input: PreparedReportGenerationInput,
  report: OpenAiStructuredReport,
): CompletedAssessmentReport {
  const interpretationsByDimension = new Map(
    report.dimensions.map((dimension) => [dimension.dimension_key, dimension.short_interpretation]),
  );

  return {
    attempt_id: input.attemptId,
    test_slug: input.testSlug,
    generated_at: new Date().toISOString(),
    generator_type: "openai",
    summary: report.summary,
    dimensions: input.promptInput.dimension_scores.map((dimension) => ({
      dimension_key: dimension.dimension_key,
      score: dimension.raw_score,
      short_interpretation:
        interpretationsByDimension.get(dimension.dimension_key) ??
        `Obrazac skora za ${formatDimensionLabel(dimension.dimension_key).toLowerCase()} ukazuje na uočljivu tendenciju ponašanja u ovom pokušaju.`,
    })),
    strengths: report.strengths,
    blind_spots: report.blind_spots,
    work_style: report.work_style,
    development_recommendations: report.development_recommendations,
    disclaimer: report.disclaimer,
  };
}

async function requestOpenAiReport(
  input: PreparedReportGenerationInput,
  options: OpenAiProviderOptions,
): Promise<CompletedAssessmentReport> {
  if (!options.apiKey) {
    throw new Error("Missing required env var: OPENAI_API_KEY");
  }

  if (!options.model) {
    throw new Error("Missing required env var: AI_REPORT_MODEL");
  }

  const timeoutMs = options.timeoutMs ?? 120000;
  const controller = new AbortController();
  const timeout = setTimeout(() => controller.abort(new Error(`OpenAI report generation timed out after ${timeoutMs}ms.`)), timeoutMs);

  console.info("OpenAI report generation started", {
    attemptId: input.attemptId,
    testSlug: input.testSlug,
    model: options.model,
    promptVersion: input.promptVersion,
    timeoutMs,
  });

  try {
    const response = await fetch("https://api.openai.com/v1/chat/completions", {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        Authorization: `Bearer ${options.apiKey}`,
      },
      body: JSON.stringify({
        model: options.model,
        temperature: 0.2,
        response_format: {
          type: "json_schema",
          json_schema: OPENAI_REPORT_SCHEMA,
        },
        messages: [
          {
            role: "system",
            content: buildSystemPrompt(input.promptVersion),
          },
          {
            role: "user",
            content: buildUserPrompt(input),
          },
        ],
      }),
      signal: controller.signal,
      cache: "no-store",
    });

    if (!response.ok) {
      const errorText = await response.text();
      throw new Error(`OpenAI request failed with status ${response.status}: ${errorText}`);
    }

    const payload = (await response.json()) as {
      choices?: Array<{
        message?: {
          content?: string;
        };
      }>;
    };

    const content = payload.choices?.[0]?.message?.content;

    if (typeof content !== "string") {
      throw new Error("OpenAI response did not contain structured content.");
    }

    const parsed = JSON.parse(content) as unknown;

    if (!isStructuredReport(parsed)) {
      throw new Error("OpenAI response JSON did not match the expected report contract.");
    }

    assertBosnianNarrative(parsed);

    const report = normalizeStructuredReport(input, parsed);

    console.info("OpenAI report generation succeeded", {
      attemptId: input.attemptId,
      testSlug: input.testSlug,
      model: options.model,
      timeoutMs,
      dimensionCount: report.dimensions.length,
    });

    return report;
  } catch (error) {
    const normalizedError = error instanceof Error ? (error as ErrorWithCause) : null;

    console.error("OpenAI report generation failed", {
      attemptId: input.attemptId,
      testSlug: input.testSlug,
      model: options.model,
      timeoutMs,
      signalAborted: controller.signal.aborted,
      errorName: normalizedError?.name ?? typeof error,
      errorMessage: normalizedError?.message ?? String(error),
      errorStack: normalizedError?.stack ?? null,
      errorCause: normalizedError?.cause ?? null,
    });

    throw error;
  } finally {
    clearTimeout(timeout);
  }
}

export function createOpenAiReportProvider(options: OpenAiProviderOptions): ReportProvider {
  return {
    type: "openai",
    async generateReport(input) {
      try {
        const report = await requestOpenAiReport(input, options);
        return {
          ok: true,
          report,
        };
      } catch (error) {
        return {
          ok: false,
          reason: error instanceof Error ? error.message : "Unknown OpenAI provider error.",
        };
      }
    },
  };
}
</file>

<file path="lib/assessment/types.ts">
export type TestCategory = "personality" | "behavioral" | "cognitive";
export type TestStatus = "draft" | "active" | "archived";
export type ScoringMethod = "likert_sum" | "correct_answers" | "weighted_correct";

export type QuestionType = "single_choice" | "multiple_choice" | "text";
export type Difficulty = "easy" | "medium" | "hard";
export type AttemptStatus = "in_progress" | "completed" | "abandoned";

export type JsonObject = Record<string, unknown>;

export type Test = {
  id: string;
  slug: string;
  name: string;
  category: TestCategory;
  description: string | null;
  status: TestStatus;
  scoring_method: ScoringMethod;
  duration_minutes: number | null;
  is_active: boolean;
  created_at: string;
  updated_at: string;
};

export type Question = {
  id: string;
  test_id: string;
  code: string;
  text: string;
  help_text: string | null;
  dimension: string;
  question_type: QuestionType;
  question_order: number;
  reverse_scored: boolean;
  difficulty: Difficulty | null;
  weight: number;
  is_required: boolean;
  is_active: boolean;
  created_at: string;
  updated_at: string;
};

export type AnswerOption = {
  id: string;
  question_id: string;
  code: string | null;
  label: string;
  value: number | null;
  option_order: number;
  is_correct: boolean | null;
  created_at: string;
};

export type Attempt = {
  id: string;
  user_id: string | null;
  organization_id: string | null;
  participant_id: string | null;
  test_id: string;
  status: AttemptStatus;
  started_at: string;
  completed_at: string | null;
  total_time_seconds: number | null;
  metadata: JsonObject;
};

export type Response = {
  id: string;
  attempt_id: string;
  question_id: string;
  response_kind: QuestionType;
  answer_option_id: string | null;
  raw_value: number | null;
  scored_value: number | null;
  text_value: string | null;
  answered_at: string;
};

export type ResponseSelection = {
  response_id: string;
  question_id: string;
  answer_option_id: string;
  created_at: string;
};

export type AssessmentSelectionValue = string | string[];
export type AssessmentSelectionsInput = Record<string, AssessmentSelectionValue>;

export type AttemptOwnershipContext = {
  userId?: string | null;
  organizationId?: string | null;
  participantId?: string | null;
};
</file>

<file path="lib/auth/session.ts">
import "server-only";

import { cookies } from "next/headers";
import { redirect } from "next/navigation";
import type { User } from "@supabase/supabase-js";
import {
  DEFAULT_REFRESH_COOKIE_MAX_AGE_SECONDS,
  type AuthCookieName,
  SUPABASE_ACCESS_TOKEN_COOKIE_NAME,
  SUPABASE_REFRESH_TOKEN_COOKIE_NAME,
  getAuthCookieOptions,
} from "@/lib/auth/cookies";
import { createSupabaseAuthClient } from "@/lib/supabase/server";

export class AuthenticationRequiredError extends Error {
  constructor(message = "Authentication required.") {
    super(message);
    this.name = "AuthenticationRequiredError";
  }
}

function getTokenCookieValue(name: AuthCookieName): string | null {
  return cookies().get(name)?.value ?? null;
}

export function persistAuthSession(session: {
  access_token: string;
  refresh_token: string;
  expires_in?: number;
}) {
  const maxAge = session.expires_in ?? DEFAULT_REFRESH_COOKIE_MAX_AGE_SECONDS;

  cookies().set(
    SUPABASE_ACCESS_TOKEN_COOKIE_NAME,
    session.access_token,
    getAuthCookieOptions(maxAge),
  );
  cookies().set(
    SUPABASE_REFRESH_TOKEN_COOKIE_NAME,
    session.refresh_token,
    getAuthCookieOptions(DEFAULT_REFRESH_COOKIE_MAX_AGE_SECONDS),
  );
}

export function clearAuthSession() {
  cookies().set(SUPABASE_ACCESS_TOKEN_COOKIE_NAME, "", getAuthCookieOptions(0));
  cookies().set(SUPABASE_REFRESH_TOKEN_COOKIE_NAME, "", getAuthCookieOptions(0));
}

export async function getAuthenticatedUser(): Promise<User | null> {
  const accessToken = getTokenCookieValue(SUPABASE_ACCESS_TOKEN_COOKIE_NAME);

  if (!accessToken) {
    return null;
  }

  const supabase = createSupabaseAuthClient();
  const { data, error } = await supabase.auth.getUser(accessToken);

  if (error) {
    return null;
  }

  return data.user ?? null;
}

export async function requireAuthenticatedUser(): Promise<User> {
  const user = await getAuthenticatedUser();

  if (!user) {
    redirect("/login");
  }

  return user;
}

export async function requireAuthenticatedUserForAction(): Promise<User> {
  const user = await getAuthenticatedUser();

  if (!user) {
    throw new AuthenticationRequiredError();
  }

  return user;
}

export async function redirectAuthenticatedUserToDashboard() {
  const user = await getAuthenticatedUser();

  if (user) {
    redirect("/dashboard");
  }
}
</file>

<file path="scripts/verify-resume-flow.mjs">
import process from "node:process";
import { setTimeout as delay } from "node:timers/promises";
import { createClient } from "@supabase/supabase-js";

const APP_URL = process.env.APP_URL ?? "http://localhost:3100";
const ATTEMPT_COOKIE_NAME = "assessment_attempt_id";
const HEALTH_URL = `${APP_URL}/api/health`;
const EXPECTED_ACTIVE_TEST_SLUG = "ipip50-hr-v1";
const TEMP_TEST_ID = "39999999-1111-1111-1111-111111111111";
const REQUIRED_CODES = ["E01", "A01", "O01"];

function fail(message) {
  throw new Error(message);
}

function assertIncludes(haystack, needle, message) {
  if (!haystack.includes(needle)) {
    fail(message);
  }
}

function assertNotIncludes(haystack, needle, message) {
  if (haystack.includes(needle)) {
    fail(message);
  }
}

function assertResumeAttemptId(html, attemptId) {
  const expectedSnippet = attemptId === null
    ? 'initialAttemptId\\":null'
    : `initialAttemptId\\":\\"${attemptId}\\"`;

  assertIncludes(html, expectedSnippet, `Expected resume payload to contain ${expectedSnippet}.`);
}

function assertSerializedSelection(html, questionId, answerOptionId) {
  assertIncludes(
    html,
    `\\"${questionId}\\":\\"${answerOptionId}\\"`,
    `Expected resume payload to contain selection ${answerOptionId} for ${questionId}.`,
  );
}

function assertSelectionsEmpty(html) {
  assertIncludes(
    html,
    'initialSelections\\":{}',
    "Expected resume payload to contain an empty initialSelections object.",
  );
}

async function fetchAssessmentPage(attemptId) {
  const response = await fetch(APP_URL, {
    headers: {
      cookie: `${ATTEMPT_COOKIE_NAME}=${attemptId}`,
    },
  });

  if (!response.ok) {
    fail(`Page request failed with status ${response.status}.`);
  }

  return response.text();
}

async function waitForServer() {
  let lastDetail = `No response from ${HEALTH_URL}.`;

  for (let attempt = 0; attempt < 120; attempt += 1) {
    try {
      const response = await fetch(HEALTH_URL);

      if (response.ok) {
        return;
      }

      lastDetail = `HTTP ${response.status} from ${HEALTH_URL}.`;
    } catch (error) {
      lastDetail = error instanceof Error ? error.message : String(error);
    }

    await delay(1000);
  }

  fail(`Next.js server did not become ready at ${HEALTH_URL}. Last check: ${lastDetail}`);
}

async function saveSelections(supabase, testId, attemptId, selectionsByQuestionId) {
  let nextAttemptId = attemptId;

  if (!nextAttemptId) {
    const { data: attemptData, error: attemptError } = await supabase
      .from("attempts")
      .insert({ test_id: testId })
      .select("id")
      .single();

    if (attemptError || !attemptData) {
      fail(`Unable to create attempt: ${attemptError?.message ?? "Unknown error"}`);
    }

    nextAttemptId = attemptData.id;
  }

  const questionIds = Object.keys(selectionsByQuestionId);
  const { error: deleteResponsesError } = await supabase
    .from("responses")
    .delete()
    .eq("attempt_id", nextAttemptId)
    .in("question_id", questionIds);

  if (deleteResponsesError) {
    fail(`Unable to replace responses: ${deleteResponsesError.message}`);
  }

  const responseRows = Object.entries(selectionsByQuestionId)
    .filter(([, answerOptionId]) => Boolean(answerOptionId))
    .map(([questionId, answerOptionId]) => ({
      attempt_id: nextAttemptId,
      question_id: questionId,
      response_kind: "single_choice",
      answer_option_id: answerOptionId,
    }));

  const { error: insertResponsesError } = await supabase
    .from("responses")
    .insert(responseRows);

  if (insertResponsesError) {
    fail(`Unable to insert responses: ${insertResponsesError.message}`);
  }

  return nextAttemptId;
}

async function getResponseSnapshot(supabase, attemptId) {
  const { data, error } = await supabase
    .from("responses")
    .select("id, question_id, response_kind, answer_option_id")
    .eq("attempt_id", attemptId)
    .order("question_id", { ascending: true });

  if (error) {
    fail(`Unable to load response snapshot: ${error.message}`);
  }

  return data ?? [];
}

async function ensureTempOtherTest(supabase) {
  const { error } = await supabase.from("tests").upsert({
    id: TEMP_TEST_ID,
    slug: "verification-other-test",
    name: "Verification Other Test",
    category: "personality",
    description: null,
    status: "draft",
    scoring_method: "likert_sum",
    duration_minutes: null,
    is_active: false,
  });

  if (error) {
    fail(`Unable to create temp test: ${error.message}`);
  }
}

async function main() {
  const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL;
  const serviceRoleKey = process.env.SUPABASE_SERVICE_ROLE_KEY;

  if (!supabaseUrl || !serviceRoleKey) {
    fail("Missing required Supabase env vars.");
  }

  const supabase = createClient(supabaseUrl, serviceRoleKey, {
    auth: {
      autoRefreshToken: false,
      persistSession: false,
    },
  });

  await waitForServer();

  const { data: activeTest, error: activeTestError } = await supabase
    .from("tests")
    .select("id, slug")
    .eq("is_active", true)
    .maybeSingle();

  if (activeTestError || !activeTest) {
    fail(`Unable to load active test: ${activeTestError?.message ?? "Unknown error"}`);
  }

  if (activeTest.slug !== EXPECTED_ACTIVE_TEST_SLUG) {
    fail(`Expected active test ${EXPECTED_ACTIVE_TEST_SLUG}, received ${activeTest.slug}.`);
  }

  const { data: questions, error: questionsError } = await supabase
    .from("questions")
    .select("id, code, question_type")
    .eq("test_id", activeTest.id)
    .eq("is_active", true)
    .order("question_order", { ascending: true });

  if (questionsError || !questions) {
    fail(`Unable to load questions: ${questionsError?.message ?? "Unknown error"}`);
  }

  if (!questions.every((question) => question.question_type === "single_choice")) {
    fail("Resume verification expects the active test to contain only single_choice questions.");
  }

  const questionByCode = new Map(questions.map((question) => [question.code, question]));

  for (const code of REQUIRED_CODES) {
    if (!questionByCode.has(code)) {
      fail(`Expected seeded question ${code} was not found.`);
    }
  }

  const targetQuestions = REQUIRED_CODES.map((code) => questionByCode.get(code));
  const { data: answerOptions, error: answerOptionsError } = await supabase
    .from("answer_options")
    .select("id, question_id, option_order")
    .in("question_id", targetQuestions.map((question) => question.id))
    .order("question_id", { ascending: true })
    .order("option_order", { ascending: true });

  if (answerOptionsError || !answerOptions) {
    fail(`Unable to load answer options: ${answerOptionsError?.message ?? "Unknown error"}`);
  }

  const answerOptionsByQuestionId = answerOptions.reduce((groupedOptions, option) => {
    const questionOptions = groupedOptions.get(option.question_id) ?? [];
    questionOptions.push(option);
    groupedOptions.set(option.question_id, questionOptions);
    return groupedOptions;
  }, new Map());

  for (const question of targetQuestions) {
    const options = answerOptionsByQuestionId.get(question.id) ?? [];

    if (options.length !== 5) {
      fail(`Expected 5 answer options for ${question.code}, received ${options.length}.`);
    }
  }

  const initialSelections = {
    [targetQuestions[0].id]: answerOptionsByQuestionId.get(targetQuestions[0].id)[1].id,
    [targetQuestions[1].id]: answerOptionsByQuestionId.get(targetQuestions[1].id)[2].id,
    [targetQuestions[2].id]: answerOptionsByQuestionId.get(targetQuestions[2].id)[3].id,
  };
  const updatedSelections = {
    [targetQuestions[0].id]: answerOptionsByQuestionId.get(targetQuestions[0].id)[4].id,
    [targetQuestions[1].id]: answerOptionsByQuestionId.get(targetQuestions[1].id)[0].id,
    [targetQuestions[2].id]: answerOptionsByQuestionId.get(targetQuestions[2].id)[1].id,
  };

  const attemptId = await saveSelections(supabase, activeTest.id, null, initialSelections);

  const initialSnapshot = await getResponseSnapshot(supabase, attemptId);
  if (initialSnapshot.length !== REQUIRED_CODES.length) {
    fail(`Expected ${REQUIRED_CODES.length} saved responses, received ${initialSnapshot.length}.`);
  }

  if (!initialSnapshot.every((response) => response.response_kind === "single_choice")) {
    fail("Initial save did not persist only single_choice response rows.");
  }

  const initialResponseIdByQuestionId = new Map(
    initialSnapshot.map((response) => [response.question_id, response.id]),
  );

  const initialHtml = await fetchAssessmentPage(attemptId);
  assertResumeAttemptId(initialHtml, attemptId);
  for (const [questionId, answerOptionId] of Object.entries(initialSelections)) {
    assertSerializedSelection(initialHtml, questionId, answerOptionId);
  }
  for (const [questionId, answerOptionId] of Object.entries(updatedSelections)) {
    assertNotIncludes(
      initialHtml,
      `\\"${questionId}\\":\\"${answerOptionId}\\"`,
      `Initial resume payload still contained the edited option ${answerOptionId}.`,
    );
  }

  await saveSelections(supabase, activeTest.id, attemptId, updatedSelections);

  const updatedSnapshot = await getResponseSnapshot(supabase, attemptId);
  if (updatedSnapshot.length !== REQUIRED_CODES.length) {
    fail(`Expected ${REQUIRED_CODES.length} updated responses, received ${updatedSnapshot.length}.`);
  }

  for (const response of updatedSnapshot) {
    if (response.answer_option_id !== updatedSelections[response.question_id]) {
      fail(`Updated response for ${response.question_id} did not persist the expected option id.`);
    }

    if (response.id === initialResponseIdByQuestionId.get(response.question_id)) {
      fail(`Expected response row for ${response.question_id} to be replaced on re-save.`);
    }
  }

  const updatedHtml = await fetchAssessmentPage(attemptId);
  assertResumeAttemptId(updatedHtml, attemptId);
  for (const [questionId, answerOptionId] of Object.entries(updatedSelections)) {
    assertSerializedSelection(updatedHtml, questionId, answerOptionId);
  }
  for (const [questionId, answerOptionId] of Object.entries(initialSelections)) {
    assertNotIncludes(
      updatedHtml,
      `\\"${questionId}\\":\\"${answerOptionId}\\"`,
      `Updated resume payload still contained the stale option ${answerOptionId}.`,
    );
  }

  const { error: deleteAttemptError } = await supabase
    .from("attempts")
    .delete()
    .eq("id", attemptId);

  if (deleteAttemptError) {
    fail(`Unable to delete attempt for stale-cookie verification: ${deleteAttemptError.message}`);
  }

  const deletedAttemptHtml = await fetchAssessmentPage(attemptId);
  assertResumeAttemptId(deletedAttemptHtml, null);
  assertSelectionsEmpty(deletedAttemptHtml);

  await ensureTempOtherTest(supabase);

  const { data: wrongAttempt, error: wrongAttemptError } = await supabase
    .from("attempts")
    .insert({ test_id: TEMP_TEST_ID })
    .select("id")
    .single();

  if (wrongAttemptError || !wrongAttempt) {
    fail(`Unable to create wrong-test attempt: ${wrongAttemptError?.message ?? "Unknown error"}`);
  }

  const wrongTestHtml = await fetchAssessmentPage(wrongAttempt.id);
  assertResumeAttemptId(wrongTestHtml, null);
  assertSelectionsEmpty(wrongTestHtml);

  console.log("Resume flow verification passed.");
  console.log(`Verified attempt id: ${attemptId}`);
  console.log("Verified behaviors:");
  console.log("- initial save DB truth for single_choice responses only");
  console.log("- reload response payload carried the saved resume state into the client form");
  console.log("- edit + re-save replaced DB truth correctly for the same question ids");
  console.log("- deleted-attempt and wrong-test cookies fall back to empty resume state");
}

main().catch((error) => {
  console.error(error instanceof Error ? error.message : String(error));
  process.exitCode = 1;
});
</file>

<file path="scripts/check-supabase.mjs">
import { createClient } from "@supabase/supabase-js";

const EXPECTED_ACTIVE_TEST_SLUG = "ipip50-hr-v1";
const EXPECTED_QUESTION_COUNT = 50;
const EXPECTED_OPTION_VALUES = [1, 2, 3, 4, 5];
const SUPPORTED_SCORING_METHODS = new Set(["likert_sum"]);

function fail(message) {
  throw new Error(message);
}

function assert(condition, message) {
  if (!condition) {
    fail(message);
  }
}

try {
  const url = process.env.NEXT_PUBLIC_SUPABASE_URL;
  const publishableKey = process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY;
  const serviceRoleKey = process.env.SUPABASE_SERVICE_ROLE_KEY;

  if (!url || !publishableKey) {
    fail("Missing NEXT_PUBLIC_SUPABASE_URL or NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY in .env.local");
  }

  const supabase = createClient(url, publishableKey);
  const { data: activeTests, error: activeTestError } = await supabase
    .from("tests")
    .select("id, slug, name, scoring_method")
    .eq("is_active", true);

  if (activeTestError) {
    fail(`Supabase query failed: ${activeTestError.message}`);
  }

  assert(
    (activeTests?.length ?? 0) === 1,
    `Expected exactly 1 active test, received ${activeTests?.length ?? 0}.`,
  );

  const [activeTest] = activeTests ?? [];

  assert(Boolean(activeTest), "No active test found.");
  assert(
    activeTest.slug === EXPECTED_ACTIVE_TEST_SLUG,
    `Expected active test slug ${EXPECTED_ACTIVE_TEST_SLUG}, received ${activeTest.slug}.`,
  );
  assert(
    SUPPORTED_SCORING_METHODS.has(activeTest.scoring_method),
    `Unsupported active test scoring method: ${activeTest.scoring_method}.`,
  );

  const { data: questions, error: questionsError } = await supabase
    .from("questions")
    .select("id, code, question_type")
    .eq("test_id", activeTest.id)
    .eq("is_active", true)
    .order("question_order", { ascending: true });

  if (questionsError) {
    fail(`Question verification failed: ${questionsError.message}`);
  }

  assert((questions?.length ?? 0) > 0, "Active test has no active questions.");
  assert(
    (questions?.length ?? 0) === EXPECTED_QUESTION_COUNT,
    `Expected ${EXPECTED_QUESTION_COUNT} active questions, received ${questions?.length ?? 0}.`,
  );
  assert(
    (questions ?? []).every((question) => question.question_type === "single_choice"),
    "Expected all active questions to be single_choice for ipip50-hr-v1.",
  );

  const questionIds = (questions ?? []).map((question) => question.id);
  const { data: answerOptions, error: answerOptionsError } = await supabase
    .from("answer_options")
    .select("question_id, value, option_order")
    .in("question_id", questionIds)
    .order("question_id", { ascending: true })
    .order("option_order", { ascending: true });

  if (answerOptionsError) {
    fail(`Answer option verification failed: ${answerOptionsError.message}`);
  }

  assert((answerOptions?.length ?? 0) > 0, "Active test has no answer options.");

  const optionsByQuestionId = (answerOptions ?? []).reduce((groupedOptions, option) => {
    const questionOptions = groupedOptions.get(option.question_id) ?? [];
    questionOptions.push(option);
    groupedOptions.set(option.question_id, questionOptions);
    return groupedOptions;
  }, new Map());

  for (const question of questions ?? []) {
    const questionOptions = optionsByQuestionId.get(question.id) ?? [];
    assert(
      questionOptions.length === EXPECTED_OPTION_VALUES.length,
      `Expected ${EXPECTED_OPTION_VALUES.length} options for ${question.code}, received ${questionOptions.length}.`,
    );

    const numericValues = questionOptions.map((option) => option.value);
    assert(
      numericValues.join(",") === EXPECTED_OPTION_VALUES.join(","),
      `Expected option values ${EXPECTED_OPTION_VALUES.join(",")} for ${question.code}, received ${numericValues.join(",")}.`,
    );
  }

  console.log("Supabase read checks passed.");
  console.log("Active test:", activeTest.slug);
  console.log("Active test scoring method:", activeTest.scoring_method);
  console.log("Verified question count:", questions.length);

  if (!serviceRoleKey) {
    fail("Missing SUPABASE_SERVICE_ROLE_KEY in the server environment");
  }

  const adminSupabase = createClient(url, serviceRoleKey, {
    auth: {
      autoRefreshToken: false,
      persistSession: false,
    },
  });

  const { data: attemptData, error: attemptError } = await adminSupabase
    .from("attempts")
    .insert({ test_id: activeTest.id })
    .select("id")
    .single();

  if (attemptError || !attemptData) {
    fail(`Supabase admin write check failed: ${attemptError?.message ?? "Unknown error"}`);
  }

  const { error: cleanupError } = await adminSupabase
    .from("attempts")
    .delete()
    .eq("id", attemptData.id);

  if (cleanupError) {
    fail(`Supabase admin cleanup failed: ${cleanupError.message}`);
  }

  console.log("Supabase admin write check passed.");
} catch (error) {
  console.error(error instanceof Error ? error.message : String(error));
  process.exitCode = 1;
}
</file>

<file path="scripts/verify-scoring-flow.mjs">
import process from "node:process";
import { setTimeout as delay } from "node:timers/promises";
import { createClient } from "@supabase/supabase-js";

const APP_URL = process.env.APP_URL ?? "http://localhost:3100";
const ATTEMPT_COOKIE_NAME = "assessment_attempt_id";
const HEALTH_URL = `${APP_URL}/api/health`;
const EXPECTED_ACTIVE_TEST_SLUG = "ipip50-hr-v1";
const SCORING_CASES = [
  { code: "E01", optionIndex: 4, expectedRaw: 5, expectedScored: 5, dimension: "Ekstraverzija" },
  { code: "A01", optionIndex: 3, expectedRaw: 4, expectedScored: 2, dimension: "Kooperativnost" },
  { code: "C01", optionIndex: 2, expectedRaw: 3, expectedScored: 3, dimension: "Savjesnost" },
  { code: "ES01", optionIndex: 1, expectedRaw: 2, expectedScored: 4, dimension: "Emocionalna stabilnost" },
  { code: "O01", optionIndex: 0, expectedRaw: 1, expectedScored: 1, dimension: "Otvorenost prema iskustvu" },
];

function fail(message) {
  throw new Error(message);
}

function assertIncludes(haystack, needle, message) {
  if (!haystack.includes(needle)) {
    fail(message);
  }
}

function assertNotIncludes(haystack, needle, message) {
  if (haystack.includes(needle)) {
    fail(message);
  }
}

async function fetchAssessmentPage(attemptId) {
  const response = await fetch(APP_URL, {
    headers: {
      cookie: `${ATTEMPT_COOKIE_NAME}=${attemptId}`,
    },
  });

  if (!response.ok) {
    fail(`Page request failed with status ${response.status}.`);
  }

  return response.text();
}

async function waitForServer() {
  let lastDetail = `No response from ${HEALTH_URL}.`;

  for (let attempt = 0; attempt < 120; attempt += 1) {
    try {
      const response = await fetch(HEALTH_URL);

      if (response.ok) {
        return;
      }

      lastDetail = `HTTP ${response.status} from ${HEALTH_URL}.`;
    } catch (error) {
      lastDetail = error instanceof Error ? error.message : String(error);
    }

    await delay(1000);
  }

  fail(`Next.js server did not become ready at ${HEALTH_URL}. Last check: ${lastDetail}`);
}

async function main() {
  const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL;
  const serviceRoleKey = process.env.SUPABASE_SERVICE_ROLE_KEY;

  if (!supabaseUrl || !serviceRoleKey) {
    fail("Missing required Supabase env vars.");
  }

  const supabase = createClient(supabaseUrl, serviceRoleKey, {
    auth: {
      autoRefreshToken: false,
      persistSession: false,
    },
  });

  await waitForServer();

  const { data: activeTest, error: activeTestError } = await supabase
    .from("tests")
    .select("id, slug")
    .eq("is_active", true)
    .maybeSingle();

  if (activeTestError || !activeTest) {
    fail(`Unable to load active test: ${activeTestError?.message ?? "Unknown error"}`);
  }

  if (activeTest.slug !== EXPECTED_ACTIVE_TEST_SLUG) {
    fail(`Expected active test ${EXPECTED_ACTIVE_TEST_SLUG}, received ${activeTest.slug}.`);
  }

  const { data: questions, error: questionsError } = await supabase
    .from("questions")
    .select("id, code, question_type")
    .eq("test_id", activeTest.id)
    .eq("is_active", true)
    .order("question_order", { ascending: true });

  if (questionsError || !questions) {
    fail(`Unable to load questions: ${questionsError?.message ?? "Unknown error"}`);
  }

  if (!questions.every((question) => question.question_type === "single_choice")) {
    fail("Scoring verification expects the active test to contain only single_choice questions.");
  }

  const questionByCode = new Map(questions.map((question) => [question.code, question]));

  for (const scoringCase of SCORING_CASES) {
    if (!questionByCode.has(scoringCase.code)) {
      fail(`Expected seeded question ${scoringCase.code} was not found.`);
    }
  }

  const { data: answerOptions, error: answerOptionsError } = await supabase
    .from("answer_options")
    .select("id, question_id, option_order")
    .in("question_id", SCORING_CASES.map((scoringCase) => questionByCode.get(scoringCase.code).id))
    .order("question_id", { ascending: true })
    .order("option_order", { ascending: true });

  if (answerOptionsError || !answerOptions) {
    fail(`Unable to load answer options: ${answerOptionsError?.message ?? "Unknown error"}`);
  }

  const answerOptionsByQuestionId = answerOptions.reduce((groupedOptions, option) => {
    const questionOptions = groupedOptions.get(option.question_id) ?? [];
    questionOptions.push(option);
    groupedOptions.set(option.question_id, questionOptions);
    return groupedOptions;
  }, new Map());

  const { data: attemptData, error: attemptError } = await supabase
    .from("attempts")
    .insert({ test_id: activeTest.id, status: "completed", completed_at: new Date().toISOString() })
    .select("id")
    .single();

  if (attemptError || !attemptData) {
    fail(`Unable to create completed attempt: ${attemptError?.message ?? "Unknown error"}`);
  }

  const attemptId = attemptData.id;
  const responseRows = [];

  for (const scoringCase of SCORING_CASES) {
    const question = questionByCode.get(scoringCase.code);
    const options = answerOptionsByQuestionId.get(question.id) ?? [];
    const selectedOption = options[scoringCase.optionIndex];

    if (!selectedOption) {
      fail(`Expected option index ${scoringCase.optionIndex} for ${scoringCase.code}.`);
    }

    responseRows.push({
      attempt_id: attemptId,
      question_id: question.id,
      response_kind: "single_choice",
      answer_option_id: selectedOption.id,
    });
  }

  const { error: responseError } = await supabase.from("responses").insert(responseRows);
  if (responseError) {
    fail(`Unable to create responses: ${responseError.message}`);
  }

  const html = await fetchAssessmentPage(attemptId);
  assertIncludes(html, "Rezultati", "Expected results section to render for a completed attempt.");
  assertIncludes(html, "Broj bodovanih odgovora", "Expected scored response summary to render.");
  for (const scoringCase of SCORING_CASES) {
    assertIncludes(html, scoringCase.dimension, `Expected ${scoringCase.dimension} result to render.`);
  }
  assertNotIncludes(
    html,
    "Zabilježeni, ali nebodovani odgovori",
    "Did not expect unscored response messaging for an all-single_choice test.",
  );

  const { data: scoredResponses, error: scoredResponsesError } = await supabase
    .from("responses")
    .select("question_id, raw_value, scored_value")
    .eq("attempt_id", attemptId);

  if (scoredResponsesError || !scoredResponses) {
    fail(`Unable to reload scored responses: ${scoredResponsesError?.message ?? "Unknown error"}`);
  }

  const scoredResponseByQuestionId = new Map(
    scoredResponses.map((response) => [response.question_id, response]),
  );

  for (const scoringCase of SCORING_CASES) {
    const questionId = questionByCode.get(scoringCase.code).id;
    const response = scoredResponseByQuestionId.get(questionId);

    if (!response) {
      fail(`Missing scored response for question ${scoringCase.code}.`);
    }

    if (response.raw_value !== scoringCase.expectedRaw || response.scored_value !== scoringCase.expectedScored) {
      fail(`Unexpected stored response score for question ${scoringCase.code}.`);
    }
  }

  const { data: dimensionScores, error: dimensionScoresError } = await supabase
    .from("dimension_scores")
    .select("dimension, raw_score")
    .eq("attempt_id", attemptId)
    .order("dimension", { ascending: true });

  if (dimensionScoresError || !dimensionScores) {
    fail(`Unable to load dimension scores: ${dimensionScoresError?.message ?? "Unknown error"}`);
  }

  if (dimensionScores.length !== SCORING_CASES.length) {
    fail(`Expected ${SCORING_CASES.length} persisted dimension scores, received ${dimensionScores.length}.`);
  }

  const dimensionScoreByName = new Map(
    dimensionScores.map((dimensionScore) => [dimensionScore.dimension, Number(dimensionScore.raw_score)]),
  );
  const expectedDimensionScores = new Map([
    ["extraversion", 5],
    ["agreeableness", 2],
    ["conscientiousness", 3],
    ["emotional_stability", 4],
    ["intellect", 1],
  ]);

  for (const [dimension, expectedRawScore] of expectedDimensionScores.entries()) {
    if (dimensionScoreByName.get(dimension) !== expectedRawScore) {
      fail(`Expected ${dimension} raw score to persist as ${expectedRawScore}.`);
    }
  }

  const reloadHtml = await fetchAssessmentPage(attemptId);
  assertIncludes(reloadHtml, "Rezultati", "Expected results section to remain on reload.");
  assertIncludes(reloadHtml, "Broj bodovanih odgovora", "Expected persisted results summary to remain on reload.");

  console.log("Scoring flow verification passed.");
  console.log(`Verified attempt id: ${attemptId}`);
  console.log("Verified behaviors:");
  console.log("- completed attempt results are derived server-side from persisted single_choice responses");
  console.log("- response raw_value and scored_value are persisted for likert_sum items");
  console.log("- dimension_scores are persisted for completed attempts and remain stable on reload");
  console.log("- no unscored response branch is exercised for the active ipip50-hr-v1 dataset");
}

main().catch((error) => {
  console.error(error instanceof Error ? error.message : String(error));
  process.exitCode = 1;
});
</file>

<file path="supabase/seed.sql">
insert into public.tests (id, slug, name, category, description, status, scoring_method, duration_minutes, is_active)
values
  ('5c0bbefb-0a61-47d8-8973-2e0e4817720c', 'ipip50-hr-v1', 'IPIP-50 Hrvatski v1', 'personality', '50-Item Lexical Big-Five Factor Markers for Self-Report, hrvatski prijevod Boris Mlačić. Namjena: samorefleksija i razvoj, ne dijagnostika.', 'active', 'likert_sum', null, true);

insert into public.questions (id, test_id, code, text, dimension, question_type, question_order, reverse_scored, weight, is_active)
values
  ('b647058d-52de-48ff-9eb6-6f48399d7e92', '5c0bbefb-0a61-47d8-8973-2e0e4817720c', 'E01', 'Unosim živost u neku zabavu.', 'extraversion', 'single_choice', 1, false, 1, true),
  ('388fba00-4a39-4753-83a5-3d0d94fbde82', '5c0bbefb-0a61-47d8-8973-2e0e4817720c', 'A01', 'Ne brinem se puno za druge ljude.', 'agreeableness', 'single_choice', 2, true, 1, true),
  ('dc77a29c-5067-4613-80de-2f8a4f9466c2', '5c0bbefb-0a61-47d8-8973-2e0e4817720c', 'C01', 'Uvijek sam spreman (spremna).', 'conscientiousness', 'single_choice', 3, false, 1, true),
  ('bd6b54c1-ee7d-4326-bde6-c2d299a5aa15', '5c0bbefb-0a61-47d8-8973-2e0e4817720c', 'ES01', 'Lako podliježem stresu.', 'emotional_stability', 'single_choice', 4, true, 1, true),
  ('c80e9f1f-e9ab-4bb8-bc6a-487828637b95', '5c0bbefb-0a61-47d8-8973-2e0e4817720c', 'O01', 'Imam bogat rječnik.', 'intellect', 'single_choice', 5, false, 1, true),
  ('b8432a96-e967-466b-909c-b25a6532ab15', '5c0bbefb-0a61-47d8-8973-2e0e4817720c', 'E02', 'Ne pričam puno.', 'extraversion', 'single_choice', 6, true, 1, true),
  ('1cf3ac40-1a1b-4970-a4c2-c27f11f273b7', '5c0bbefb-0a61-47d8-8973-2e0e4817720c', 'A02', 'Zanimaju me drugi ljudi.', 'agreeableness', 'single_choice', 7, false, 1, true),
  ('ed1da360-f266-4f4c-8eff-6eeaed780405', '5c0bbefb-0a61-47d8-8973-2e0e4817720c', 'C02', 'Ostavljam svoje stvari posvuda.', 'conscientiousness', 'single_choice', 8, true, 1, true),
  ('22c3fc2f-6b05-4962-82b4-2eac067a1b40', '5c0bbefb-0a61-47d8-8973-2e0e4817720c', 'ES02', 'Uglavnom se osjećam opušteno.', 'emotional_stability', 'single_choice', 9, false, 1, true),
  ('18e0e7c7-c6eb-4ffb-bd6d-0b42b4e18ba8', '5c0bbefb-0a61-47d8-8973-2e0e4817720c', 'O02', 'Teško razumijem apstraktne ideje.', 'intellect', 'single_choice', 10, true, 1, true),
  ('21ce6020-b834-4c01-9fd1-cee910c89270', '5c0bbefb-0a61-47d8-8973-2e0e4817720c', 'E03', 'Osjećam se ugodno u društvu.', 'extraversion', 'single_choice', 11, false, 1, true),
  ('b7323a4c-d883-4ee5-8ece-9f10cd0236fd', '5c0bbefb-0a61-47d8-8973-2e0e4817720c', 'A03', 'Vrijeđam ljude.', 'agreeableness', 'single_choice', 12, true, 1, true),
  ('60b66e4d-503a-479f-a68e-337f9eafc4c2', '5c0bbefb-0a61-47d8-8973-2e0e4817720c', 'C03', 'Obraćam pažnju na detalje.', 'conscientiousness', 'single_choice', 13, false, 1, true),
  ('7e586c56-ad74-40a2-85b0-768c481cdd31', '5c0bbefb-0a61-47d8-8973-2e0e4817720c', 'ES03', 'Često sam zabrinut (zabrinuta).', 'emotional_stability', 'single_choice', 14, true, 1, true),
  ('630d9f53-c823-4bb1-a141-e40d829a8e86', '5c0bbefb-0a61-47d8-8973-2e0e4817720c', 'O03', 'Imam bujnu maštu.', 'intellect', 'single_choice', 15, false, 1, true),
  ('221860da-63e7-4c35-a9c0-868672a1d0fa', '5c0bbefb-0a61-47d8-8973-2e0e4817720c', 'E04', 'Držim se po strani.', 'extraversion', 'single_choice', 16, true, 1, true),
  ('eae42e44-8459-413a-87a1-285bf2cfd024', '5c0bbefb-0a61-47d8-8973-2e0e4817720c', 'A04', 'Suosjećam s drugima.', 'agreeableness', 'single_choice', 17, false, 1, true),
  ('058ad608-5cd3-424b-baaa-61b708883504', '5c0bbefb-0a61-47d8-8973-2e0e4817720c', 'C04', 'Pravim nered.', 'conscientiousness', 'single_choice', 18, true, 1, true),
  ('f3a01f7a-3c32-4102-93a4-adb1add78cbf', '5c0bbefb-0a61-47d8-8973-2e0e4817720c', 'ES04', 'Rijetko sam tužan (tužna).', 'emotional_stability', 'single_choice', 19, false, 1, true),
  ('3e9c9578-e8a5-42f9-9528-270b6ae62f19', '5c0bbefb-0a61-47d8-8973-2e0e4817720c', 'O04', 'Ne zanimaju me apstraktne ideje.', 'intellect', 'single_choice', 20, true, 1, true),
  ('061dbd9a-a8ce-4d27-8927-e2ece74ca667', '5c0bbefb-0a61-47d8-8973-2e0e4817720c', 'E05', 'Sam(a) započinjem razgovore.', 'extraversion', 'single_choice', 21, false, 1, true),
  ('78053410-d33e-41f4-83a4-25d1b8cf57b0', '5c0bbefb-0a61-47d8-8973-2e0e4817720c', 'A05', 'Ne zanimaju me tuđi problemi.', 'agreeableness', 'single_choice', 22, true, 1, true),
  ('8cc8d4f3-d1f5-483c-a0e2-ddaff27ca897', '5c0bbefb-0a61-47d8-8973-2e0e4817720c', 'C05', 'Odmah obavljam kućne poslove.', 'conscientiousness', 'single_choice', 23, false, 1, true),
  ('c9dad6f0-8841-46e8-a4e9-3232267ac14c', '5c0bbefb-0a61-47d8-8973-2e0e4817720c', 'ES05', 'Lako me zasmetati.', 'emotional_stability', 'single_choice', 24, true, 1, true),
  ('a8f6f124-f023-451f-a6a0-8be4d7270846', '5c0bbefb-0a61-47d8-8973-2e0e4817720c', 'O05', 'Imam izvrsne ideje.', 'intellect', 'single_choice', 25, false, 1, true),
  ('11a58719-2404-4a0f-bc7c-2335ea36dacd', '5c0bbefb-0a61-47d8-8973-2e0e4817720c', 'E06', 'Imam malo toga za reći.', 'extraversion', 'single_choice', 26, true, 1, true),
  ('9f15f9cb-ae33-4807-a352-c16bd7d3df9e', '5c0bbefb-0a61-47d8-8973-2e0e4817720c', 'A06', 'Imam meko srce.', 'agreeableness', 'single_choice', 27, false, 1, true),
  ('5a075e9d-9e45-40a7-802d-b8f6a5e2ed68', '5c0bbefb-0a61-47d8-8973-2e0e4817720c', 'C06', 'Često zaboravljam vratiti stvari na njihovo mjesto.', 'conscientiousness', 'single_choice', 28, true, 1, true),
  ('94238f4f-2722-4351-8a6a-7d9275752e90', '5c0bbefb-0a61-47d8-8973-2e0e4817720c', 'ES06', 'Lako se uzrujam.', 'emotional_stability', 'single_choice', 29, true, 1, true),
  ('61276b7c-dda1-41d2-b9e8-dfcbe151d4b9', '5c0bbefb-0a61-47d8-8973-2e0e4817720c', 'O06', 'Nemam mnogo mašte.', 'intellect', 'single_choice', 30, true, 1, true),
  ('53d953db-837f-415b-9af2-db296b366730', '5c0bbefb-0a61-47d8-8973-2e0e4817720c', 'E07', 'Na zabavama razgovaram s mnogo različitih osoba.', 'extraversion', 'single_choice', 31, false, 1, true),
  ('9ebb99b1-de6f-4887-8a8d-5771a2617d9c', '5c0bbefb-0a61-47d8-8973-2e0e4817720c', 'A07', 'Drugi ljudi me zapravo ne zanimaju.', 'agreeableness', 'single_choice', 32, true, 1, true),
  ('f9236273-1f0d-4bde-81e8-6e102a60b115', '5c0bbefb-0a61-47d8-8973-2e0e4817720c', 'C07', 'Volim red.', 'conscientiousness', 'single_choice', 33, false, 1, true),
  ('3adc8bc5-7f67-43e6-9187-9e532e60dac0', '5c0bbefb-0a61-47d8-8973-2e0e4817720c', 'ES07', 'Često mijenjam raspoloženja.', 'emotional_stability', 'single_choice', 34, true, 1, true),
  ('fc40c108-6f9b-4a31-ac72-51458561ea6e', '5c0bbefb-0a61-47d8-8973-2e0e4817720c', 'O07', 'Brzo shvaćam različite stvari.', 'intellect', 'single_choice', 35, false, 1, true),
  ('f39bf387-997e-4569-a6b3-c498fda578b0', '5c0bbefb-0a61-47d8-8973-2e0e4817720c', 'E08', 'Ne volim privlačiti pažnju.', 'extraversion', 'single_choice', 36, true, 1, true),
  ('6bc37857-caab-4f09-8cc5-c78268b268be', '5c0bbefb-0a61-47d8-8973-2e0e4817720c', 'A08', 'Posvećujem vrijeme drugim ljudima.', 'agreeableness', 'single_choice', 37, false, 1, true),
  ('2ac0c655-a0e1-4e7a-ac4d-cae364faf6cf', '5c0bbefb-0a61-47d8-8973-2e0e4817720c', 'C08', 'Izbjegavam obveze.', 'conscientiousness', 'single_choice', 38, true, 1, true),
  ('45ac538c-bf89-4608-a343-2ce1cf4f5403', '5c0bbefb-0a61-47d8-8973-2e0e4817720c', 'ES08', 'Podliježem čestim promjenama raspoloženja.', 'emotional_stability', 'single_choice', 39, true, 1, true),
  ('09a88cd7-d290-4c0b-97bb-f251162be09f', '5c0bbefb-0a61-47d8-8973-2e0e4817720c', 'O08', 'Koristim učene izraze.', 'intellect', 'single_choice', 40, false, 1, true),
  ('8556eb96-f813-4f81-a567-a4b60bfe8e40', '5c0bbefb-0a61-47d8-8973-2e0e4817720c', 'E09', 'Nije mi nelagodno biti u središtu pozornosti.', 'extraversion', 'single_choice', 41, false, 1, true),
  ('d2b02b93-99d1-433d-aad7-f3953d9e9586', '5c0bbefb-0a61-47d8-8973-2e0e4817720c', 'A09', 'Osjetljiv(a) sam na tuđe osjećaje.', 'agreeableness', 'single_choice', 42, false, 1, true),
  ('2c51a41b-c403-44fc-bb8d-9475113cab0b', '5c0bbefb-0a61-47d8-8973-2e0e4817720c', 'C09', 'Slijedim zacrtani plan.', 'conscientiousness', 'single_choice', 43, false, 1, true),
  ('2a3b070b-ef12-4609-907c-7e6cd4921f8d', '5c0bbefb-0a61-47d8-8973-2e0e4817720c', 'ES09', 'Lako me je razdražiti.', 'emotional_stability', 'single_choice', 44, true, 1, true),
  ('669b01b5-bcb7-4f3b-b4a0-d98d7a756a5d', '5c0bbefb-0a61-47d8-8973-2e0e4817720c', 'O09', 'Provodim vrijeme razmišljajući.', 'intellect', 'single_choice', 45, false, 1, true),
  ('d0ad5ede-8a73-4370-bc7f-8fadb4cf337e', '5c0bbefb-0a61-47d8-8973-2e0e4817720c', 'E10', 'Šutljiv(a) sam s nepoznatima.', 'extraversion', 'single_choice', 46, true, 1, true),
  ('cef2e03c-29ea-4b3c-bc80-3b48910f711e', '5c0bbefb-0a61-47d8-8973-2e0e4817720c', 'A10', 'Nastojim da se drugi osjećaju ugodno.', 'agreeableness', 'single_choice', 47, false, 1, true),
  ('b5711d8a-779c-4e66-9562-1f4fd98e3e6a', '5c0bbefb-0a61-47d8-8973-2e0e4817720c', 'C10', 'Posao obavljam točno i precizno.', 'conscientiousness', 'single_choice', 48, false, 1, true),
  ('f897b61b-cdf2-4022-951e-4e0aa1758f88', '5c0bbefb-0a61-47d8-8973-2e0e4817720c', 'ES10', 'Često sam tužan (tužna).', 'emotional_stability', 'single_choice', 49, true, 1, true),
  ('86264573-9f68-48eb-9580-8aee56005db1', '5c0bbefb-0a61-47d8-8973-2e0e4817720c', 'O10', 'Pun(a) sam ideja.', 'intellect', 'single_choice', 50, false, 1, true);

insert into public.answer_options (question_id, code, label, value, option_order)
values
  ('058ad608-5cd3-424b-baaa-61b708883504', '1', 'Vrlo netačno', 1, 1),
  ('058ad608-5cd3-424b-baaa-61b708883504', '2', 'Uglavnom netačno', 2, 2),
  ('058ad608-5cd3-424b-baaa-61b708883504', '3', 'Ni tačno ni netačno', 3, 3),
  ('058ad608-5cd3-424b-baaa-61b708883504', '4', 'Uglavnom tačno', 4, 4),
  ('058ad608-5cd3-424b-baaa-61b708883504', '5', 'Vrlo tačno', 5, 5),
  ('061dbd9a-a8ce-4d27-8927-e2ece74ca667', '1', 'Vrlo netačno', 1, 1),
  ('061dbd9a-a8ce-4d27-8927-e2ece74ca667', '2', 'Uglavnom netačno', 2, 2),
  ('061dbd9a-a8ce-4d27-8927-e2ece74ca667', '3', 'Ni tačno ni netačno', 3, 3),
  ('061dbd9a-a8ce-4d27-8927-e2ece74ca667', '4', 'Uglavnom tačno', 4, 4),
  ('061dbd9a-a8ce-4d27-8927-e2ece74ca667', '5', 'Vrlo tačno', 5, 5),
  ('09a88cd7-d290-4c0b-97bb-f251162be09f', '1', 'Vrlo netačno', 1, 1),
  ('09a88cd7-d290-4c0b-97bb-f251162be09f', '2', 'Uglavnom netačno', 2, 2),
  ('09a88cd7-d290-4c0b-97bb-f251162be09f', '3', 'Ni tačno ni netačno', 3, 3),
  ('09a88cd7-d290-4c0b-97bb-f251162be09f', '4', 'Uglavnom tačno', 4, 4),
  ('09a88cd7-d290-4c0b-97bb-f251162be09f', '5', 'Vrlo tačno', 5, 5),
  ('11a58719-2404-4a0f-bc7c-2335ea36dacd', '1', 'Vrlo netačno', 1, 1),
  ('11a58719-2404-4a0f-bc7c-2335ea36dacd', '2', 'Uglavnom netačno', 2, 2),
  ('11a58719-2404-4a0f-bc7c-2335ea36dacd', '3', 'Ni tačno ni netačno', 3, 3),
  ('11a58719-2404-4a0f-bc7c-2335ea36dacd', '4', 'Uglavnom tačno', 4, 4),
  ('11a58719-2404-4a0f-bc7c-2335ea36dacd', '5', 'Vrlo tačno', 5, 5),
  ('18e0e7c7-c6eb-4ffb-bd6d-0b42b4e18ba8', '1', 'Vrlo netačno', 1, 1),
  ('18e0e7c7-c6eb-4ffb-bd6d-0b42b4e18ba8', '2', 'Uglavnom netačno', 2, 2),
  ('18e0e7c7-c6eb-4ffb-bd6d-0b42b4e18ba8', '3', 'Ni tačno ni netačno', 3, 3),
  ('18e0e7c7-c6eb-4ffb-bd6d-0b42b4e18ba8', '4', 'Uglavnom tačno', 4, 4),
  ('18e0e7c7-c6eb-4ffb-bd6d-0b42b4e18ba8', '5', 'Vrlo tačno', 5, 5),
  ('1cf3ac40-1a1b-4970-a4c2-c27f11f273b7', '1', 'Vrlo netačno', 1, 1),
  ('1cf3ac40-1a1b-4970-a4c2-c27f11f273b7', '2', 'Uglavnom netačno', 2, 2),
  ('1cf3ac40-1a1b-4970-a4c2-c27f11f273b7', '3', 'Ni tačno ni netačno', 3, 3),
  ('1cf3ac40-1a1b-4970-a4c2-c27f11f273b7', '4', 'Uglavnom tačno', 4, 4),
  ('1cf3ac40-1a1b-4970-a4c2-c27f11f273b7', '5', 'Vrlo tačno', 5, 5),
  ('21ce6020-b834-4c01-9fd1-cee910c89270', '1', 'Vrlo netačno', 1, 1),
  ('21ce6020-b834-4c01-9fd1-cee910c89270', '2', 'Uglavnom netačno', 2, 2),
  ('21ce6020-b834-4c01-9fd1-cee910c89270', '3', 'Ni tačno ni netačno', 3, 3),
  ('21ce6020-b834-4c01-9fd1-cee910c89270', '4', 'Uglavnom tačno', 4, 4),
  ('21ce6020-b834-4c01-9fd1-cee910c89270', '5', 'Vrlo tačno', 5, 5),
  ('221860da-63e7-4c35-a9c0-868672a1d0fa', '1', 'Vrlo netačno', 1, 1),
  ('221860da-63e7-4c35-a9c0-868672a1d0fa', '2', 'Uglavnom netačno', 2, 2),
  ('221860da-63e7-4c35-a9c0-868672a1d0fa', '3', 'Ni tačno ni netačno', 3, 3),
  ('221860da-63e7-4c35-a9c0-868672a1d0fa', '4', 'Uglavnom tačno', 4, 4),
  ('221860da-63e7-4c35-a9c0-868672a1d0fa', '5', 'Vrlo tačno', 5, 5),
  ('22c3fc2f-6b05-4962-82b4-2eac067a1b40', '1', 'Vrlo netačno', 1, 1),
  ('22c3fc2f-6b05-4962-82b4-2eac067a1b40', '2', 'Uglavnom netačno', 2, 2),
  ('22c3fc2f-6b05-4962-82b4-2eac067a1b40', '3', 'Ni tačno ni netačno', 3, 3),
  ('22c3fc2f-6b05-4962-82b4-2eac067a1b40', '4', 'Uglavnom tačno', 4, 4),
  ('22c3fc2f-6b05-4962-82b4-2eac067a1b40', '5', 'Vrlo tačno', 5, 5),
  ('2a3b070b-ef12-4609-907c-7e6cd4921f8d', '1', 'Vrlo netačno', 1, 1),
  ('2a3b070b-ef12-4609-907c-7e6cd4921f8d', '2', 'Uglavnom netačno', 2, 2),
  ('2a3b070b-ef12-4609-907c-7e6cd4921f8d', '3', 'Ni tačno ni netačno', 3, 3),
  ('2a3b070b-ef12-4609-907c-7e6cd4921f8d', '4', 'Uglavnom tačno', 4, 4),
  ('2a3b070b-ef12-4609-907c-7e6cd4921f8d', '5', 'Vrlo tačno', 5, 5),
  ('2ac0c655-a0e1-4e7a-ac4d-cae364faf6cf', '1', 'Vrlo netačno', 1, 1),
  ('2ac0c655-a0e1-4e7a-ac4d-cae364faf6cf', '2', 'Uglavnom netačno', 2, 2),
  ('2ac0c655-a0e1-4e7a-ac4d-cae364faf6cf', '3', 'Ni tačno ni netačno', 3, 3),
  ('2ac0c655-a0e1-4e7a-ac4d-cae364faf6cf', '4', 'Uglavnom tačno', 4, 4),
  ('2ac0c655-a0e1-4e7a-ac4d-cae364faf6cf', '5', 'Vrlo tačno', 5, 5),
  ('2c51a41b-c403-44fc-bb8d-9475113cab0b', '1', 'Vrlo netačno', 1, 1),
  ('2c51a41b-c403-44fc-bb8d-9475113cab0b', '2', 'Uglavnom netačno', 2, 2),
  ('2c51a41b-c403-44fc-bb8d-9475113cab0b', '3', 'Ni tačno ni netačno', 3, 3),
  ('2c51a41b-c403-44fc-bb8d-9475113cab0b', '4', 'Uglavnom tačno', 4, 4),
  ('2c51a41b-c403-44fc-bb8d-9475113cab0b', '5', 'Vrlo tačno', 5, 5),
  ('388fba00-4a39-4753-83a5-3d0d94fbde82', '1', 'Vrlo netačno', 1, 1),
  ('388fba00-4a39-4753-83a5-3d0d94fbde82', '2', 'Uglavnom netačno', 2, 2),
  ('388fba00-4a39-4753-83a5-3d0d94fbde82', '3', 'Ni tačno ni netačno', 3, 3),
  ('388fba00-4a39-4753-83a5-3d0d94fbde82', '4', 'Uglavnom tačno', 4, 4),
  ('388fba00-4a39-4753-83a5-3d0d94fbde82', '5', 'Vrlo tačno', 5, 5),
  ('3adc8bc5-7f67-43e6-9187-9e532e60dac0', '1', 'Vrlo netačno', 1, 1),
  ('3adc8bc5-7f67-43e6-9187-9e532e60dac0', '2', 'Uglavnom netačno', 2, 2),
  ('3adc8bc5-7f67-43e6-9187-9e532e60dac0', '3', 'Ni tačno ni netačno', 3, 3),
  ('3adc8bc5-7f67-43e6-9187-9e532e60dac0', '4', 'Uglavnom tačno', 4, 4),
  ('3adc8bc5-7f67-43e6-9187-9e532e60dac0', '5', 'Vrlo tačno', 5, 5),
  ('3e9c9578-e8a5-42f9-9528-270b6ae62f19', '1', 'Vrlo netačno', 1, 1),
  ('3e9c9578-e8a5-42f9-9528-270b6ae62f19', '2', 'Uglavnom netačno', 2, 2),
  ('3e9c9578-e8a5-42f9-9528-270b6ae62f19', '3', 'Ni tačno ni netačno', 3, 3),
  ('3e9c9578-e8a5-42f9-9528-270b6ae62f19', '4', 'Uglavnom tačno', 4, 4),
  ('3e9c9578-e8a5-42f9-9528-270b6ae62f19', '5', 'Vrlo tačno', 5, 5),
  ('45ac538c-bf89-4608-a343-2ce1cf4f5403', '1', 'Vrlo netačno', 1, 1),
  ('45ac538c-bf89-4608-a343-2ce1cf4f5403', '2', 'Uglavnom netačno', 2, 2),
  ('45ac538c-bf89-4608-a343-2ce1cf4f5403', '3', 'Ni tačno ni netačno', 3, 3),
  ('45ac538c-bf89-4608-a343-2ce1cf4f5403', '4', 'Uglavnom tačno', 4, 4),
  ('45ac538c-bf89-4608-a343-2ce1cf4f5403', '5', 'Vrlo tačno', 5, 5),
  ('53d953db-837f-415b-9af2-db296b366730', '1', 'Vrlo netačno', 1, 1),
  ('53d953db-837f-415b-9af2-db296b366730', '2', 'Uglavnom netačno', 2, 2),
  ('53d953db-837f-415b-9af2-db296b366730', '3', 'Ni tačno ni netačno', 3, 3),
  ('53d953db-837f-415b-9af2-db296b366730', '4', 'Uglavnom tačno', 4, 4),
  ('53d953db-837f-415b-9af2-db296b366730', '5', 'Vrlo tačno', 5, 5),
  ('5a075e9d-9e45-40a7-802d-b8f6a5e2ed68', '1', 'Vrlo netačno', 1, 1),
  ('5a075e9d-9e45-40a7-802d-b8f6a5e2ed68', '2', 'Uglavnom netačno', 2, 2),
  ('5a075e9d-9e45-40a7-802d-b8f6a5e2ed68', '3', 'Ni tačno ni netačno', 3, 3),
  ('5a075e9d-9e45-40a7-802d-b8f6a5e2ed68', '4', 'Uglavnom tačno', 4, 4),
  ('5a075e9d-9e45-40a7-802d-b8f6a5e2ed68', '5', 'Vrlo tačno', 5, 5),
  ('60b66e4d-503a-479f-a68e-337f9eafc4c2', '1', 'Vrlo netačno', 1, 1),
  ('60b66e4d-503a-479f-a68e-337f9eafc4c2', '2', 'Uglavnom netačno', 2, 2),
  ('60b66e4d-503a-479f-a68e-337f9eafc4c2', '3', 'Ni tačno ni netačno', 3, 3),
  ('60b66e4d-503a-479f-a68e-337f9eafc4c2', '4', 'Uglavnom tačno', 4, 4),
  ('60b66e4d-503a-479f-a68e-337f9eafc4c2', '5', 'Vrlo tačno', 5, 5),
  ('61276b7c-dda1-41d2-b9e8-dfcbe151d4b9', '1', 'Vrlo netačno', 1, 1),
  ('61276b7c-dda1-41d2-b9e8-dfcbe151d4b9', '2', 'Uglavnom netačno', 2, 2),
  ('61276b7c-dda1-41d2-b9e8-dfcbe151d4b9', '3', 'Ni tačno ni netačno', 3, 3),
  ('61276b7c-dda1-41d2-b9e8-dfcbe151d4b9', '4', 'Uglavnom tačno', 4, 4),
  ('61276b7c-dda1-41d2-b9e8-dfcbe151d4b9', '5', 'Vrlo tačno', 5, 5),
  ('630d9f53-c823-4bb1-a141-e40d829a8e86', '1', 'Vrlo netačno', 1, 1),
  ('630d9f53-c823-4bb1-a141-e40d829a8e86', '2', 'Uglavnom netačno', 2, 2),
  ('630d9f53-c823-4bb1-a141-e40d829a8e86', '3', 'Ni tačno ni netačno', 3, 3),
  ('630d9f53-c823-4bb1-a141-e40d829a8e86', '4', 'Uglavnom tačno', 4, 4),
  ('630d9f53-c823-4bb1-a141-e40d829a8e86', '5', 'Vrlo tačno', 5, 5),
  ('669b01b5-bcb7-4f3b-b4a0-d98d7a756a5d', '1', 'Vrlo netačno', 1, 1),
  ('669b01b5-bcb7-4f3b-b4a0-d98d7a756a5d', '2', 'Uglavnom netačno', 2, 2),
  ('669b01b5-bcb7-4f3b-b4a0-d98d7a756a5d', '3', 'Ni tačno ni netačno', 3, 3),
  ('669b01b5-bcb7-4f3b-b4a0-d98d7a756a5d', '4', 'Uglavnom tačno', 4, 4),
  ('669b01b5-bcb7-4f3b-b4a0-d98d7a756a5d', '5', 'Vrlo tačno', 5, 5),
  ('6bc37857-caab-4f09-8cc5-c78268b268be', '1', 'Vrlo netačno', 1, 1),
  ('6bc37857-caab-4f09-8cc5-c78268b268be', '2', 'Uglavnom netačno', 2, 2),
  ('6bc37857-caab-4f09-8cc5-c78268b268be', '3', 'Ni tačno ni netačno', 3, 3),
  ('6bc37857-caab-4f09-8cc5-c78268b268be', '4', 'Uglavnom tačno', 4, 4),
  ('6bc37857-caab-4f09-8cc5-c78268b268be', '5', 'Vrlo tačno', 5, 5),
  ('78053410-d33e-41f4-83a4-25d1b8cf57b0', '1', 'Vrlo netačno', 1, 1),
  ('78053410-d33e-41f4-83a4-25d1b8cf57b0', '2', 'Uglavnom netačno', 2, 2),
  ('78053410-d33e-41f4-83a4-25d1b8cf57b0', '3', 'Ni tačno ni netačno', 3, 3),
  ('78053410-d33e-41f4-83a4-25d1b8cf57b0', '4', 'Uglavnom tačno', 4, 4),
  ('78053410-d33e-41f4-83a4-25d1b8cf57b0', '5', 'Vrlo tačno', 5, 5),
  ('7e586c56-ad74-40a2-85b0-768c481cdd31', '1', 'Vrlo netačno', 1, 1),
  ('7e586c56-ad74-40a2-85b0-768c481cdd31', '2', 'Uglavnom netačno', 2, 2),
  ('7e586c56-ad74-40a2-85b0-768c481cdd31', '3', 'Ni tačno ni netačno', 3, 3),
  ('7e586c56-ad74-40a2-85b0-768c481cdd31', '4', 'Uglavnom tačno', 4, 4),
  ('7e586c56-ad74-40a2-85b0-768c481cdd31', '5', 'Vrlo tačno', 5, 5),
  ('8556eb96-f813-4f81-a567-a4b60bfe8e40', '1', 'Vrlo netačno', 1, 1),
  ('8556eb96-f813-4f81-a567-a4b60bfe8e40', '2', 'Uglavnom netačno', 2, 2),
  ('8556eb96-f813-4f81-a567-a4b60bfe8e40', '3', 'Ni tačno ni netačno', 3, 3),
  ('8556eb96-f813-4f81-a567-a4b60bfe8e40', '4', 'Uglavnom tačno', 4, 4),
  ('8556eb96-f813-4f81-a567-a4b60bfe8e40', '5', 'Vrlo tačno', 5, 5),
  ('86264573-9f68-48eb-9580-8aee56005db1', '1', 'Vrlo netačno', 1, 1),
  ('86264573-9f68-48eb-9580-8aee56005db1', '2', 'Uglavnom netačno', 2, 2),
  ('86264573-9f68-48eb-9580-8aee56005db1', '3', 'Ni tačno ni netačno', 3, 3),
  ('86264573-9f68-48eb-9580-8aee56005db1', '4', 'Uglavnom tačno', 4, 4),
  ('86264573-9f68-48eb-9580-8aee56005db1', '5', 'Vrlo tačno', 5, 5),
  ('8cc8d4f3-d1f5-483c-a0e2-ddaff27ca897', '1', 'Vrlo netačno', 1, 1),
  ('8cc8d4f3-d1f5-483c-a0e2-ddaff27ca897', '2', 'Uglavnom netačno', 2, 2),
  ('8cc8d4f3-d1f5-483c-a0e2-ddaff27ca897', '3', 'Ni tačno ni netačno', 3, 3),
  ('8cc8d4f3-d1f5-483c-a0e2-ddaff27ca897', '4', 'Uglavnom tačno', 4, 4),
  ('8cc8d4f3-d1f5-483c-a0e2-ddaff27ca897', '5', 'Vrlo tačno', 5, 5),
  ('94238f4f-2722-4351-8a6a-7d9275752e90', '1', 'Vrlo netačno', 1, 1),
  ('94238f4f-2722-4351-8a6a-7d9275752e90', '2', 'Uglavnom netačno', 2, 2),
  ('94238f4f-2722-4351-8a6a-7d9275752e90', '3', 'Ni tačno ni netačno', 3, 3),
  ('94238f4f-2722-4351-8a6a-7d9275752e90', '4', 'Uglavnom tačno', 4, 4),
  ('94238f4f-2722-4351-8a6a-7d9275752e90', '5', 'Vrlo tačno', 5, 5),
  ('9ebb99b1-de6f-4887-8a8d-5771a2617d9c', '1', 'Vrlo netačno', 1, 1),
  ('9ebb99b1-de6f-4887-8a8d-5771a2617d9c', '2', 'Uglavnom netačno', 2, 2),
  ('9ebb99b1-de6f-4887-8a8d-5771a2617d9c', '3', 'Ni tačno ni netačno', 3, 3),
  ('9ebb99b1-de6f-4887-8a8d-5771a2617d9c', '4', 'Uglavnom tačno', 4, 4),
  ('9ebb99b1-de6f-4887-8a8d-5771a2617d9c', '5', 'Vrlo tačno', 5, 5),
  ('9f15f9cb-ae33-4807-a352-c16bd7d3df9e', '1', 'Vrlo netačno', 1, 1),
  ('9f15f9cb-ae33-4807-a352-c16bd7d3df9e', '2', 'Uglavnom netačno', 2, 2),
  ('9f15f9cb-ae33-4807-a352-c16bd7d3df9e', '3', 'Ni tačno ni netačno', 3, 3),
  ('9f15f9cb-ae33-4807-a352-c16bd7d3df9e', '4', 'Uglavnom tačno', 4, 4),
  ('9f15f9cb-ae33-4807-a352-c16bd7d3df9e', '5', 'Vrlo tačno', 5, 5),
  ('a8f6f124-f023-451f-a6a0-8be4d7270846', '1', 'Vrlo netačno', 1, 1),
  ('a8f6f124-f023-451f-a6a0-8be4d7270846', '2', 'Uglavnom netačno', 2, 2),
  ('a8f6f124-f023-451f-a6a0-8be4d7270846', '3', 'Ni tačno ni netačno', 3, 3),
  ('a8f6f124-f023-451f-a6a0-8be4d7270846', '4', 'Uglavnom tačno', 4, 4),
  ('a8f6f124-f023-451f-a6a0-8be4d7270846', '5', 'Vrlo tačno', 5, 5),
  ('b5711d8a-779c-4e66-9562-1f4fd98e3e6a', '1', 'Vrlo netačno', 1, 1),
  ('b5711d8a-779c-4e66-9562-1f4fd98e3e6a', '2', 'Uglavnom netačno', 2, 2),
  ('b5711d8a-779c-4e66-9562-1f4fd98e3e6a', '3', 'Ni tačno ni netačno', 3, 3),
  ('b5711d8a-779c-4e66-9562-1f4fd98e3e6a', '4', 'Uglavnom tačno', 4, 4),
  ('b5711d8a-779c-4e66-9562-1f4fd98e3e6a', '5', 'Vrlo tačno', 5, 5),
  ('b647058d-52de-48ff-9eb6-6f48399d7e92', '1', 'Vrlo netačno', 1, 1),
  ('b647058d-52de-48ff-9eb6-6f48399d7e92', '2', 'Uglavnom netačno', 2, 2),
  ('b647058d-52de-48ff-9eb6-6f48399d7e92', '3', 'Ni tačno ni netačno', 3, 3),
  ('b647058d-52de-48ff-9eb6-6f48399d7e92', '4', 'Uglavnom tačno', 4, 4),
  ('b647058d-52de-48ff-9eb6-6f48399d7e92', '5', 'Vrlo tačno', 5, 5),
  ('b7323a4c-d883-4ee5-8ece-9f10cd0236fd', '1', 'Vrlo netačno', 1, 1),
  ('b7323a4c-d883-4ee5-8ece-9f10cd0236fd', '2', 'Uglavnom netačno', 2, 2),
  ('b7323a4c-d883-4ee5-8ece-9f10cd0236fd', '3', 'Ni tačno ni netačno', 3, 3),
  ('b7323a4c-d883-4ee5-8ece-9f10cd0236fd', '4', 'Uglavnom tačno', 4, 4),
  ('b7323a4c-d883-4ee5-8ece-9f10cd0236fd', '5', 'Vrlo tačno', 5, 5),
  ('b8432a96-e967-466b-909c-b25a6532ab15', '1', 'Vrlo netačno', 1, 1),
  ('b8432a96-e967-466b-909c-b25a6532ab15', '2', 'Uglavnom netačno', 2, 2),
  ('b8432a96-e967-466b-909c-b25a6532ab15', '3', 'Ni tačno ni netačno', 3, 3),
  ('b8432a96-e967-466b-909c-b25a6532ab15', '4', 'Uglavnom tačno', 4, 4),
  ('b8432a96-e967-466b-909c-b25a6532ab15', '5', 'Vrlo tačno', 5, 5),
  ('bd6b54c1-ee7d-4326-bde6-c2d299a5aa15', '1', 'Vrlo netačno', 1, 1),
  ('bd6b54c1-ee7d-4326-bde6-c2d299a5aa15', '2', 'Uglavnom netačno', 2, 2),
  ('bd6b54c1-ee7d-4326-bde6-c2d299a5aa15', '3', 'Ni tačno ni netačno', 3, 3),
  ('bd6b54c1-ee7d-4326-bde6-c2d299a5aa15', '4', 'Uglavnom tačno', 4, 4),
  ('bd6b54c1-ee7d-4326-bde6-c2d299a5aa15', '5', 'Vrlo tačno', 5, 5),
  ('c80e9f1f-e9ab-4bb8-bc6a-487828637b95', '1', 'Vrlo netačno', 1, 1),
  ('c80e9f1f-e9ab-4bb8-bc6a-487828637b95', '2', 'Uglavnom netačno', 2, 2),
  ('c80e9f1f-e9ab-4bb8-bc6a-487828637b95', '3', 'Ni tačno ni netačno', 3, 3),
  ('c80e9f1f-e9ab-4bb8-bc6a-487828637b95', '4', 'Uglavnom tačno', 4, 4),
  ('c80e9f1f-e9ab-4bb8-bc6a-487828637b95', '5', 'Vrlo tačno', 5, 5),
  ('c9dad6f0-8841-46e8-a4e9-3232267ac14c', '1', 'Vrlo netačno', 1, 1),
  ('c9dad6f0-8841-46e8-a4e9-3232267ac14c', '2', 'Uglavnom netačno', 2, 2),
  ('c9dad6f0-8841-46e8-a4e9-3232267ac14c', '3', 'Ni tačno ni netačno', 3, 3),
  ('c9dad6f0-8841-46e8-a4e9-3232267ac14c', '4', 'Uglavnom tačno', 4, 4),
  ('c9dad6f0-8841-46e8-a4e9-3232267ac14c', '5', 'Vrlo tačno', 5, 5),
  ('cef2e03c-29ea-4b3c-bc80-3b48910f711e', '1', 'Vrlo netačno', 1, 1),
  ('cef2e03c-29ea-4b3c-bc80-3b48910f711e', '2', 'Uglavnom netačno', 2, 2),
  ('cef2e03c-29ea-4b3c-bc80-3b48910f711e', '3', 'Ni tačno ni netačno', 3, 3),
  ('cef2e03c-29ea-4b3c-bc80-3b48910f711e', '4', 'Uglavnom tačno', 4, 4),
  ('cef2e03c-29ea-4b3c-bc80-3b48910f711e', '5', 'Vrlo tačno', 5, 5),
  ('d0ad5ede-8a73-4370-bc7f-8fadb4cf337e', '1', 'Vrlo netačno', 1, 1),
  ('d0ad5ede-8a73-4370-bc7f-8fadb4cf337e', '2', 'Uglavnom netačno', 2, 2),
  ('d0ad5ede-8a73-4370-bc7f-8fadb4cf337e', '3', 'Ni tačno ni netačno', 3, 3),
  ('d0ad5ede-8a73-4370-bc7f-8fadb4cf337e', '4', 'Uglavnom tačno', 4, 4),
  ('d0ad5ede-8a73-4370-bc7f-8fadb4cf337e', '5', 'Vrlo tačno', 5, 5),
  ('d2b02b93-99d1-433d-aad7-f3953d9e9586', '1', 'Vrlo netačno', 1, 1),
  ('d2b02b93-99d1-433d-aad7-f3953d9e9586', '2', 'Uglavnom netačno', 2, 2),
  ('d2b02b93-99d1-433d-aad7-f3953d9e9586', '3', 'Ni tačno ni netačno', 3, 3),
  ('d2b02b93-99d1-433d-aad7-f3953d9e9586', '4', 'Uglavnom tačno', 4, 4),
  ('d2b02b93-99d1-433d-aad7-f3953d9e9586', '5', 'Vrlo tačno', 5, 5),
  ('dc77a29c-5067-4613-80de-2f8a4f9466c2', '1', 'Vrlo netačno', 1, 1),
  ('dc77a29c-5067-4613-80de-2f8a4f9466c2', '2', 'Uglavnom netačno', 2, 2),
  ('dc77a29c-5067-4613-80de-2f8a4f9466c2', '3', 'Ni tačno ni netačno', 3, 3),
  ('dc77a29c-5067-4613-80de-2f8a4f9466c2', '4', 'Uglavnom tačno', 4, 4),
  ('dc77a29c-5067-4613-80de-2f8a4f9466c2', '5', 'Vrlo tačno', 5, 5),
  ('eae42e44-8459-413a-87a1-285bf2cfd024', '1', 'Vrlo netačno', 1, 1),
  ('eae42e44-8459-413a-87a1-285bf2cfd024', '2', 'Uglavnom netačno', 2, 2),
  ('eae42e44-8459-413a-87a1-285bf2cfd024', '3', 'Ni tačno ni netačno', 3, 3),
  ('eae42e44-8459-413a-87a1-285bf2cfd024', '4', 'Uglavnom tačno', 4, 4),
  ('eae42e44-8459-413a-87a1-285bf2cfd024', '5', 'Vrlo tačno', 5, 5),
  ('ed1da360-f266-4f4c-8eff-6eeaed780405', '1', 'Vrlo netačno', 1, 1),
  ('ed1da360-f266-4f4c-8eff-6eeaed780405', '2', 'Uglavnom netačno', 2, 2),
  ('ed1da360-f266-4f4c-8eff-6eeaed780405', '3', 'Ni tačno ni netačno', 3, 3),
  ('ed1da360-f266-4f4c-8eff-6eeaed780405', '4', 'Uglavnom tačno', 4, 4),
  ('ed1da360-f266-4f4c-8eff-6eeaed780405', '5', 'Vrlo tačno', 5, 5),
  ('f39bf387-997e-4569-a6b3-c498fda578b0', '1', 'Vrlo netačno', 1, 1),
  ('f39bf387-997e-4569-a6b3-c498fda578b0', '2', 'Uglavnom netačno', 2, 2),
  ('f39bf387-997e-4569-a6b3-c498fda578b0', '3', 'Ni tačno ni netačno', 3, 3),
  ('f39bf387-997e-4569-a6b3-c498fda578b0', '4', 'Uglavnom tačno', 4, 4),
  ('f39bf387-997e-4569-a6b3-c498fda578b0', '5', 'Vrlo tačno', 5, 5),
  ('f3a01f7a-3c32-4102-93a4-adb1add78cbf', '1', 'Vrlo netačno', 1, 1),
  ('f3a01f7a-3c32-4102-93a4-adb1add78cbf', '2', 'Uglavnom netačno', 2, 2),
  ('f3a01f7a-3c32-4102-93a4-adb1add78cbf', '3', 'Ni tačno ni netačno', 3, 3),
  ('f3a01f7a-3c32-4102-93a4-adb1add78cbf', '4', 'Uglavnom tačno', 4, 4),
  ('f3a01f7a-3c32-4102-93a4-adb1add78cbf', '5', 'Vrlo tačno', 5, 5),
  ('f897b61b-cdf2-4022-951e-4e0aa1758f88', '1', 'Vrlo netačno', 1, 1),
  ('f897b61b-cdf2-4022-951e-4e0aa1758f88', '2', 'Uglavnom netačno', 2, 2),
  ('f897b61b-cdf2-4022-951e-4e0aa1758f88', '3', 'Ni tačno ni netačno', 3, 3),
  ('f897b61b-cdf2-4022-951e-4e0aa1758f88', '4', 'Uglavnom tačno', 4, 4),
  ('f897b61b-cdf2-4022-951e-4e0aa1758f88', '5', 'Vrlo tačno', 5, 5),
  ('f9236273-1f0d-4bde-81e8-6e102a60b115', '1', 'Vrlo netačno', 1, 1),
  ('f9236273-1f0d-4bde-81e8-6e102a60b115', '2', 'Uglavnom netačno', 2, 2),
  ('f9236273-1f0d-4bde-81e8-6e102a60b115', '3', 'Ni tačno ni netačno', 3, 3),
  ('f9236273-1f0d-4bde-81e8-6e102a60b115', '4', 'Uglavnom tačno', 4, 4),
  ('f9236273-1f0d-4bde-81e8-6e102a60b115', '5', 'Vrlo tačno', 5, 5),
  ('fc40c108-6f9b-4a31-ac72-51458561ea6e', '1', 'Vrlo netačno', 1, 1),
  ('fc40c108-6f9b-4a31-ac72-51458561ea6e', '2', 'Uglavnom netačno', 2, 2),
  ('fc40c108-6f9b-4a31-ac72-51458561ea6e', '3', 'Ni tačno ni netačno', 3, 3),
  ('fc40c108-6f9b-4a31-ac72-51458561ea6e', '4', 'Uglavnom tačno', 4, 4),
  ('fc40c108-6f9b-4a31-ac72-51458561ea6e', '5', 'Vrlo tačno', 5, 5);
</file>

<file path="components/assessment/completed-assessment-summary.tsx">
"use client";

import { useState } from "react";

import type { CompletedAssessmentReportState } from "@/lib/assessment/reports";
import {
  formatDimensionLabel,
  formatScoreLabel,
  getDimensionHelperLabel,
} from "@/lib/assessment/result-display";
import type { CompletedAssessmentResults } from "@/lib/assessment/scoring";

type CompletedAssessmentSummaryProps = {
  completedAt?: string | null;
  organizationName?: string | null;
  participantName?: string | null;
  testName?: string | null;
  results: CompletedAssessmentResults | null;
  reportState: CompletedAssessmentReportState | null;
};

type DimensionViewModel = {
  key: string;
  label: string;
  helperLabel: string | null;
  score: number;
  averageScore: number;
  scoredQuestionCount: number;
  shortInterpretation: string;
  scoreWidth: number;
  rank: number;
  totalDimensions: number;
};

type DimensionDetailBlock = {
  heading:
    | "Kako se to kod tebe često pokazuje"
    | "Šta ti to može donositi kao prednost"
    | "Na šta vrijedi obratiti pažnju";
  body: string;
};

function formatUnscoredReason(
  reason: CompletedAssessmentResults["unscoredResponses"][number]["reason"],
): string {
  if (reason === "question_type_not_scoreable") {
    return "Zabilježeno, ali nije bodovano u trenutnom MVP modelu.";
  }

  return "Zabilježeno bez numeričkih scoring vrijednosti u trenutnim seed podacima.";
}

function splitIntoSentences(text: string): string[] {
  return text
    .split(/(?<=[.!?])\s+/)
    .map((sentence) => sentence.trim())
    .filter(Boolean);
}

function getLeadSentence(text: string): string {
  return splitIntoSentences(text)[0] ?? text.trim();
}

function stripInsightLabel(text: string): string {
  return text.replace(/^[^:]{2,40}:\s*/, "").trim();
}

function ensureSentence(text: string): string {
  const normalized = text.trim().replace(/\s+/g, " ");

  if (!normalized) {
    return "";
  }

  return /[.!?]$/.test(normalized) ? normalized : `${normalized}.`;
}

function getAverageScore(rawScore: number, scoredQuestionCount: number): number {
  if (scoredQuestionCount === 0) {
    return 0;
  }

  return Math.round((rawScore / scoredQuestionCount) * 100) / 100;
}

function getScoreBand(score: number): "high" | "mid" | "low" {
  if (score >= 3.67) {
    return "high";
  }

  if (score >= 2.34) {
    return "mid";
  }

  return "low";
}

function toSecondPersonSingular(text: string): string {
  return text
    .replace(/\bProfil pokazuje\b/gi, "Pokazuješ")
    .replace(/\bRezultat pokazuje\b/gi, "Tvoji rezultati pokazuju")
    .replace(/\bVjerovatno glavno uporište radnog stila:\s*/gi, "U tvom radnom stilu najviše se ističe ")
    .replace(/\bSekundarni signal:\s*/gi, "Dodatno se primjećuje da ")
    .replace(/\bje najuočljiviji signal u ovom pokušaju\b/gi, "se kod tebe najviše ističe")
    .replace(/\bje komparativno niže izražena i treba je čitati kao razvojnu oblast, a ne kao nedostatak\b/gi, "je kod tebe suptilnija i najkorisnije ju je gledati kao prostor za razvoj, a ne kao nedostatak")
    .replace(/\bje niže izražena u ovom obrascu odgovora, pa osoba može\b/gi, "je kod tebe manje izražena, pa možeš")
    .replace(/\bosoba može\b/gi, "možeš")
    .replace(/\bukupni profil\b/gi, "ukupan obrazac")
    .replace(/\bukupnog profila\b/gi, "tvog ukupnog obrasca")
    .replace(/\bprofil može\b/gi, "možeš")
    .replace(/\bu ovom obrascu odgovora\b/gi, "u svojim odgovorima")
    .replace(/\bu ovom završenom pokušaju\b/gi, "u svojim odgovorima")
    .replace(/\bu ovom pokušaju\b/gi, "u svojim odgovorima")
    .replace(/\bovom pokušaju\b/gi, "tvojim odgovorima")
    .replace(/\bprofil\b/gi, "obrazac")
    .replace(/\s+/g, " ")
    .trim();
}

function formatTopInsightSentence(text: string): string {
  const cleaned = stripInsightLabel(getLeadSentence(toSecondPersonSingular(text)));
  const normalized = cleaned.replace(/\s+/g, " ").trim();
  const rewrites: Array<[RegExp, string]> = [
    [
      /^Često djeluje energizirano kroz socijalni kontakt i vidljivo uključivanje\.?$/i,
      "Kroz kontakt s drugima često se osjećaš energizirano i prirodno se uključuješ u dešavanja.",
    ],
    [
      /^Pokazuje uravnotežen spoj otvorenog angažmana i promišljenijeg tempa\.?$/i,
      "Pokazuješ dobar balans između otvorenog angažmana i promišljenijeg tempa.",
    ],
    [
      /^Može preferirati mirnije okruženje i odmjereniji interpersonalni ritam\.?$/i,
      "Često preferiraš mirnije okruženje i odmjereniji ritam u odnosima s drugima.",
    ],
    [
      /^Naglašava saradnju, taktičnost i kvalitet međuljudskih odnosa\.?$/i,
      "U odnosima s drugima često naglašavaš saradnju, taktičnost i dobar kvalitet odnosa.",
    ],
    [
      /^Može dobro balansirati iskrenost i saradnju, zavisno od konteksta\.?$/i,
      "Zavisno od situacije, dobro balansiraš iskrenost i saradnju.",
    ],
    [
      /^Može češće birati direktan izazov umjesto prilagođavanja ili konsenzusa\.?$/i,
      "Često biraš direktan pristup umjesto prilagođavanja ili traženja konsenzusa.",
    ],
    [
      /^Vjerovatno vrednuje strukturu, dosljednost i pouzdanu realizaciju\.?$/i,
      "Vjerovatno posebno vrednuješ strukturu, dosljednost i pouzdanu realizaciju.",
    ],
    [
      /^Može se prilagođavati između planiranja i fleksibilnosti kako se zahtjevi mijenjaju\.?$/i,
      "Kako se zahtjevi mijenjaju, znaš dobro prelaziti između planiranja i fleksibilnosti.",
    ],
    [
      /^Može raditi spontanije i imati korist od jasnije vanjske strukture\.?$/i,
      "Često radiš spontanije, a jasnija vanjska struktura ti može pomoći da lakše održiš ritam.",
    ],
    [
      /^Vjerovatno zadržava stabilnost i pod uobičajenim pritiskom\.?$/i,
      "U zahtjevnijim situacijama često ostaješ pribran i pod uobičajenim pritiskom.",
    ],
    [
      /^Pokazuje mješovit profil nošenja sa stresom koji može varirati po opterećenju ili kontekstu\.?$/i,
      "Na stres reaguješ različito, zavisno od opterećenja i konkretnog konteksta.",
    ],
    [
      /^Može intenzivnije doživljavati pritisak i imati korist od stabilnijih navika oporavka\.?$/i,
      "Pritisak možeš doživjeti intenzivnije, pa ti stabilnije navike oporavka mogu biti posebno korisne.",
    ],
    [
      /^Često je usmjeren prema idejama, istraživanju i konceptualnoj raznolikosti\.?$/i,
      "Često si usmjeren prema idejama, istraživanju i konceptualnoj raznolikosti.",
    ],
    [
      /^Može prihvatati nove ideje uz zadržavanje vrijednosti poznatih pristupa\.?$/i,
      "Otvoren si za nove ideje, ali i dalje vidiš vrijednost poznatih pristupa.",
    ],
    [
      /^Može preferirati praktičnu jasnoću umjesto apstraktnog istraživanja\.?$/i,
      "Češće preferiraš praktičnu jasnoću nego apstraktno istraživanje.",
    ],
  ];

  for (const [pattern, replacement] of rewrites) {
    if (pattern.test(normalized)) {
      return replacement;
    }
  }

  const directAddressPattern =
    /\b(ti|tvoj|tvom|tvoje|tvojim|pokazuješ|ostaješ|možeš|vrednuješ|preferiraš|naglašavaš|biraš|zadržavaš|reaguješ|pristupaš|prihvataš|radiš|osjećaš|znaš|si)\b/i;

  if (directAddressPattern.test(normalized)) {
    return ensureSentence(normalized);
  }

  if (/^u tvom\b/i.test(normalized) || /^tvoji\b/i.test(normalized)) {
    return ensureSentence(normalized);
  }

  return ensureSentence(`Kod tebe se posebno vidi da ${normalized.charAt(0).toLowerCase()}${normalized.slice(1)}`);
}

function joinSentences(...sentences: Array<string | null | undefined>): string {
  return sentences
    .filter((sentence): sentence is string => Boolean(sentence))
    .map((sentence) => ensureSentence(sentence))
    .join(" ");
}

function getRankContext(dimension: DimensionViewModel): string {
  if (dimension.rank === 0) {
    return "Ovo je jedna od izraženijih niti tvog trenutnog obrasca i često daje ton načinu na koji prilaziš ljudima i zadacima";
  }

  if (dimension.rank === dimension.totalDimensions - 1) {
    return "Ova dimenzija je kod tebe tiša, pa je najkorisnije pratiti kako se uključuje onda kada situacija to zaista traži";
  }

  return "Ova dimenzija kod tebe djeluje kao stabilna sredina, pa često služi kao način da zadržiš ravnotežu između impulsa i zahtjeva situacije";
}

function getFallbackDimensionDetailBlocks(dimension: DimensionViewModel): DimensionDetailBlock[] {
  return [
    {
      heading: "Kako se to kod tebe često pokazuje",
      body: joinSentences(
        getRankContext(dimension),
        "Najčešće se vidi kroz sitne izbore u tempu, komunikaciji i načinu na koji odgovaraš na očekivanja oko sebe",
      ),
    },
    {
      heading: "Šta ti to može donositi kao prednost",
      body: "Kada ovu tendenciju koristiš svjesno, može ti pomoći da prirodnije pronađeš stil rada i saradnje koji ti stvarno odgovara umjesto da radiš protiv sebe.",
    },
    {
      heading: "Na šta vrijedi obratiti pažnju",
      body: "Vrijedi paziti da ovu stranu sebe ne uzmeš kao fiksno pravilo, jer najbolji rezultat obično dolazi kada zadržiš malo fleksibilnosti prema kontekstu i ljudima oko sebe.",
    },
  ];
}

function getDimensionDetailBlocks(dimension: DimensionViewModel): DimensionDetailBlock[] {
  const band = getScoreBand(dimension.averageScore);

  const contentByDimension: Record<string, Record<"high" | "mid" | "low", DimensionDetailBlock[]>> = {
    extraversion: {
      high: [
        {
          heading: "Kako se to kod tebe često pokazuje",
          body: "Često ti prija kontakt s ljudima, brže se uključuješ u razgovor i lakše unosiš energiju u prostor. U grupi se obično ne povlačiš dugo, nego spontano zauzmeš svoje mjesto.",
        },
        {
          heading: "Šta ti to može donositi kao prednost",
          body: "To ti može pomoći u povezivanju, pokretanju saradnje i davanju zamaha timu kada treba probiti početnu rezervu. Ljudi te često lakše primijete i zapamte jer djeluješ pristupačno i angažovano.",
        },
        {
          heading: "Na šta vrijedi obratiti pažnju",
          body: "Vrijedi paziti da brzina uključivanja ne ostavi premalo prostora za tuđi ritam ili tiše signale u razgovoru. Nekad upravo kratka pauza i više slušanja pojačaju utisak koji već prirodno ostavljaš.",
        },
      ],
      mid: [
        {
          heading: "Kako se to kod tebe često pokazuje",
          body: "Kod tebe se često vidi dobar balans između društvene otvorenosti i potrebe za vlastitim tempom. Znaš biti prisutan i topao, ali bez potrebe da budeš u centru svake situacije.",
        },
        {
          heading: "Šta ti to može donositi kao prednost",
          body: "Ovakav raspon ti može pomoći da se prilagodiš različitim ljudima i okruženjima bez velikog napora. U saradnji možeš prirodno prebacivati između angažmana, slušanja i mirnijeg promišljanja.",
        },
        {
          heading: "Na šta vrijedi obratiti pažnju",
          body: "Pošto možeš djelovati i otvoreno i odmjereno, nekad drugi teže procijene kada ti treba više prostora, a kada više interakcije. Korisno je da to kažeš jasnije umjesto da pretpostaviš da će drugi sami prepoznati tvoj ritam.",
        },
      ],
      low: [
        {
          heading: "Kako se to kod tebe često pokazuje",
          body: "Često ti više odgovaraju mirniji razgovori, manji broj ljudi i situacije u kojima ne moraš stalno biti izložen. Obično se lakše otvaraš kada prvo osjetiš smisao, sigurnost ili dovoljno prostora za svoj tempo.",
        },
        {
          heading: "Šta ti to može donositi kao prednost",
          body: "To ti može davati smirenije prisustvo, bolju selektivnost u odnosima i više fokusa kada buka okoline raste. U radu često pomaže jer ne trošiš energiju na stalnu socijalnu aktivaciju nego je čuvaš za ono što ti je važno.",
        },
        {
          heading: "Na šta vrijedi obratiti pažnju",
          body: "Vrijedi paziti da potreba za mirom ne bude pogrešno pročitana kao nezainteresovanost ili distanca kada ti to zapravo nisi. Nekad je dovoljno da ranije pokažeš namjeru iako ne želiš odmah puno pričati.",
        },
      ],
    },
    agreeableness: {
      high: [
        {
          heading: "Kako se to kod tebe često pokazuje",
          body: "Često paziš kako tvoje riječi utiču na druge i prirodno tražiš ton koji čuva odnos. U saradnji ti je važno da komunikacija ostane korektna, a da ljudi osjete poštovanje i dobru namjeru.",
        },
        {
          heading: "Šta ti to može donositi kao prednost",
          body: "To ti može pomagati u građenju povjerenja, smirivanju tenzije i lakšem povezivanju različitih ljudi. Često si neko uz koga saradnja djeluje sigurnije i manje iscrpljujuće.",
        },
        {
          heading: "Na šta vrijedi obratiti pažnju",
          body: "Vrijedi paziti da želja za skladom ne potisne ono što ti stvarno misliš ili trebaš reći. Tvoja obzirnost ima najveću vrijednost kada uz nju ostane i dovoljno jasna granica.",
        },
      ],
      mid: [
        {
          heading: "Kako se to kod tebe često pokazuje",
          body: "Kod tebe se često vidi balans između saradnje i direktnosti. Znaš biti obziran, ali i reći šta misliš kada procijeniš da je to korisnije od pukog slaganja.",
        },
        {
          heading: "Šta ti to može donositi kao prednost",
          body: "To ti može donositi vjerodostojnost u odnosima jer ne djeluješ ni pretjerano mekan ni nepotrebno tvrd. U radu i dogovaranju često pomaže zato što možeš čuvati odnos, a ipak gurati stvari naprijed.",
        },
        {
          heading: "Na šta vrijedi obratiti pažnju",
          body: "Pošto se dobro krećeš između takta i iskrenosti, nekad možeš predugo vagati koji pristup je pravi. Korisno je da ranije odlučiš kada je važniji mir, a kada je važnija potpuna jasnoća.",
        },
      ],
      low: [
        {
          heading: "Kako se to kod tebe često pokazuje",
          body: "Često radije ideš direktno na suštinu nego da dugo ublažavaš poruku. Kada nešto ne vidiš kao dobro rješenje, vjerovatno ti je prirodnije da to jasno pokažeš nego da ostaneš u diplomatskoj neodređenosti.",
        },
        {
          heading: "Šta ti to može donositi kao prednost",
          body: "To ti može donositi brzinu, odlučnost i veću spremnost da otvoriš teške teme koje drugi zaobilaze. U situacijama gdje treba presjeći maglu, tvoja direktnost može biti veoma korisna.",
        },
        {
          heading: "Na šta vrijedi obratiti pažnju",
          body: "Vrijedi paziti da jasnoća ne zazvuči oštrije nego što namjeravaš, posebno kada druga strana traži više takta nego argumenta. Nekad mala doza topline u formi pojača prijem tvoje poruke bez gubitka iskrenosti.",
        },
      ],
    },
    conscientiousness: {
      high: [
        {
          heading: "Kako se to kod tebe često pokazuje",
          body: "Vjerovatno ti prijaju struktura, red i osjećaj da stvari imaju jasan tok. Često misliš unaprijed, pratiš detalje i osjetiš unutrašnje zadovoljstvo kada obaveze držiš pod kontrolom.",
        },
        {
          heading: "Šta ti to može donositi kao prednost",
          body: "To ti može pomagati u pouzdanosti, dosljednosti i pretvaranju planova u stvarne rezultate. Ljudi te često mogu doživjeti kao nekoga na koga se može računati kada treba iznijeti posao do kraja.",
        },
        {
          heading: "Na šta vrijedi obratiti pažnju",
          body: "Vrijedi paziti da visoki kriteriji ne prerastu u nepotreban pritisak ili osjećaj da sve mora biti potpuno sređeno prije kretanja. Ponekad je dovoljno da nešto bude dovoljno dobro da bi moglo ići dalje.",
        },
      ],
      mid: [
        {
          heading: "Kako se to kod tebe često pokazuje",
          body: "Kod tebe se često vidi praktičan balans između plana i fleksibilnosti. Možeš se organizovati kada treba, ali obično ne djeluješ ukočeno ako se okolnosti usput promijene.",
        },
        {
          heading: "Šta ti to može donositi kao prednost",
          body: "To ti može biti korisno jer znaš održati osnovni red bez gušenja spontanosti. U radu i saradnji često znači da možeš pratiti dogovor, a ipak ostati dovoljno prilagodljiv kada realnost krene drugim putem.",
        },
        {
          heading: "Na šta vrijedi obratiti pažnju",
          body: "Pošto se dobro snalaziš između strukture i improvizacije, nekad možeš prekasno primijetiti da zadatak ipak traži više sistema nego što si mu dao. Korisno je ranije procijeniti kada je dosta fleksibilnosti, a kada treba čvršći okvir.",
        },
      ],
      low: [
        {
          heading: "Kako se to kod tebe često pokazuje",
          body: "Često ti više odgovara spontaniji tok nego strogo praćenje unaprijed zadatog reda. Možeš brzo reagovati, krenuti iz momenta i tražiti vlastiti način umjesto da se oslanjaš na rigidnu rutinu.",
        },
        {
          heading: "Šta ti to može donositi kao prednost",
          body: "To ti može donositi svježinu, bržu prilagodbu i manje zakočenosti kada se okolnosti naglo mijenjaju. Posebno može pomoći tamo gdje treba uhvatiti momentum umjesto čekati savršen plan.",
        },
        {
          heading: "Na šta vrijedi obratiti pažnju",
          body: "Vrijedi paziti da spontanost ne pojede kontinuitet u stvarima koje traže praćenje, dovršavanje i dosljedan ritam. Često nije potreban veliki sistem, nego samo nekoliko vanjskih oslonaca koji te vraćaju na bitno.",
        },
      ],
    },
    emotional_stability: {
      high: [
        {
          heading: "Kako se to kod tebe često pokazuje",
          body: "U zahtjevnijim situacijama često zadržiš prisebnost i ne prepuštaš se lako početnom talasu pritiska. Drugima možeš djelovati kao neko ko i pod opterećenjem ostaje dovoljno miran da razmišlja jasno.",
        },
        {
          heading: "Šta ti to može donositi kao prednost",
          body: "To ti može pomoći u donošenju odluka, smirenijoj komunikaciji i održavanju stabilnog ritma kada okolina postane napeta. Ljudi uz tebe često lakše ostaju fokusirani jer ne pojačavaš dodatno stres u prostoru.",
        },
        {
          heading: "Na šta vrijedi obratiti pažnju",
          body: "Vrijedi paziti da unutrašnju stabilnost ne pretvoriš u preveliku distancu prema vlastitim signalima umora ili tuđoj emocionalnoj reakciji. Nekad baš ono što te drži mirnim treba dopuniti malo otvorenijim pokazivanjem kako ti je.",
        },
      ],
      mid: [
        {
          heading: "Kako se to kod tebe često pokazuje",
          body: "Na pritisak vjerovatno reaguješ zavisno od konteksta, intenziteta i toga koliko imaš kontrole nad situacijom. Nekad ostaješ vrlo sabran, a nekad ti treba više vremena da vratiš unutrašnji mir.",
        },
        {
          heading: "Šta ti to može donositi kao prednost",
          body: "Ovakav raspon ti može pomoći jer nisi ni previše tvrd prema sebi ni potpuno preplavljen kada stvari postanu zahtjevne. Kada prepoznaš šta te stabilizuje, obično možeš prilično dobro vratiti fokus i ritam.",
        },
        {
          heading: "Na šta vrijedi obratiti pažnju",
          body: "Pošto tvoja reakcija zavisi od situacije, korisno je da ranije prepoznaš svoje okidače umjesto da ih primijetiš tek kad se nakupi pritisak. Mala, redovna briga o oporavku ovdje često pravi veću razliku nego veliki jednokratni potezi.",
        },
      ],
      low: [
        {
          heading: "Kako se to kod tebe često pokazuje",
          body: "Pritisak možeš osjetiti brže i intenzivnije, posebno kada se nagomilaju neizvjesnost, očekivanja ili previše otvorenih frontova. To ne znači slabost, nego da tvoj sistem ranije registruje opterećenje i traži više regulacije.",
        },
        {
          heading: "Šta ti to može donositi kao prednost",
          body: "Kad to dobro upoznaš, može ti pomoći da ranije primijetiš šta nije održivo i da ozbiljnije shvatiš signale iscrpljenosti koje drugi ignorišu. Ta osjetljivost može te učiniti pažljivijim prema kvalitetu okruženja i načinu rada koji ti stvarno odgovara.",
        },
        {
          heading: "Na šta vrijedi obratiti pažnju",
          body: "Vrijedi paziti da intenzitet trenutnog osjećaja ne postane jedina slika cijele situacije. Korisno je unaprijed graditi male navike smirivanja i oslonce koji te vraćaju u osjećaj kontrole prije nego što stres preuzme tempo.",
        },
      ],
    },
    intellect: {
      high: [
        {
          heading: "Kako se to kod tebe često pokazuje",
          body: "Često te privuku nove ideje, drugačiji uglovi gledanja i prostor za istraživanje van očiglednog. Vjerovatno ti prija kada možeš povezivati teme, postavljati pitanja i širiti sliku prije nego što je zatvoriš.",
        },
        {
          heading: "Šta ti to može donositi kao prednost",
          body: "To ti može donositi kreativniji pristup, lakše učenje i veću spremnost da vidiš rješenja koja nisu odmah standardna. U razgovoru i radu često pomaže jer unosiš svježinu, radoznalost i mentalnu širinu.",
        },
        {
          heading: "Na šta vrijedi obratiti pažnju",
          body: "Vrijedi paziti da širina interesa ne odvuče fokus sa onoga što trenutno treba privesti kraju. Nekad najbolji efekat dolazi kada radoznalost namjerno spojiš s vrlo konkretnim narednim korakom.",
        },
      ],
      mid: [
        {
          heading: "Kako se to kod tebe često pokazuje",
          body: "Kod tebe se često vidi otvorenost za novo, ali bez potrebe da svaka stvar bude potpuno drugačija ili eksperimentalna. Možeš prihvatiti novu ideju kada vidiš smisao, a istovremeno zadržati poštovanje prema onome što već radi.",
        },
        {
          heading: "Šta ti to može donositi kao prednost",
          body: "To ti može pomagati da spajaš praktičnost i svježinu bez nepotrebnog idealizovanja novog ili starog. U saradnji je korisno jer često možeš biti most između onih koji guraju promjenu i onih kojima treba više sigurnosti.",
        },
        {
          heading: "Na šta vrijedi obratiti pažnju",
          body: "Pošto vidiš vrijednost na obje strane, nekad možeš predugo ostati između istraživanja i odluke. Korisno je ranije odrediti kad je vrijeme za još pitanja, a kad je dovoljno jasno da kreneš dalje.",
        },
      ],
      low: [
        {
          heading: "Kako se to kod tebe često pokazuje",
          body: "Često ti više odgovara jasno, konkretno i primjenjivo nego dugo zadržavanje u apstraktnim idejama. Kada nešto procjenjuješ, vjerovatno ti je važno da brzo vidiš čemu služi i kako se može stvarno upotrijebiti.",
        },
        {
          heading: "Šta ti to može donositi kao prednost",
          body: "To ti može donositi praktičnost, manje rasipanja energije i bolji osjećaj za ono što je zaista izvedivo. U radu često pomaže jer brže spuštaš stvari na zemlju i tražiš upotrebljivo rješenje umjesto teorijskog sjaja.",
        },
        {
          heading: "Na šta vrijedi obratiti pažnju",
          body: "Vrijedi paziti da potreba za jasnoćom ne zatvori vrata ideji koja u početku još nema savršen oblik. Nekad mala doza istraživanja prije zaključka otvori rješenje koje bi inače ostalo neprimijećeno.",
        },
      ],
    },
  };

  return contentByDimension[dimension.key]?.[band] ?? getFallbackDimensionDetailBlocks(dimension);
}

function formatCompletedAt(value?: string | null): string {
  if (!value) {
    return "Nije dostupno";
  }

  const date = new Date(value);

  if (Number.isNaN(date.getTime())) {
    return "Nije dostupno";
  }

  const months = ["jan", "feb", "mar", "apr", "maj", "jun", "jul", "aug", "sep", "okt", "nov", "dec"];
  const day = String(date.getDate()).padStart(2, "0");
  const month = months[date.getMonth()];
  const year = date.getFullYear();
  const hours = String(date.getHours()).padStart(2, "0");
  const minutes = String(date.getMinutes()).padStart(2, "0");

  return `${day}. ${month} ${year}, ${hours}:${minutes}`;
}

function getTopInsights(
  reportState: CompletedAssessmentReportState | null,
  dimensions: DimensionViewModel[],
): string[] {
  if (reportState?.status !== "ready") {
    return dimensions
      .slice(0, 3)
      .map((dimension) => formatTopInsightSentence(dimension.shortInterpretation));
  }

  const candidates = [
    ...reportState.report.strengths,
    ...reportState.report.work_style,
    ...reportState.report.blind_spots,
    ...reportState.report.development_recommendations,
    ...dimensions.map((dimension) => dimension.shortInterpretation),
  ];

  const uniqueItems = candidates.filter((item, index) => {
    const normalized = item.trim().toLowerCase();
    return normalized.length > 0 && candidates.findIndex((candidate) => candidate.trim().toLowerCase() === normalized) === index;
  });

  return uniqueItems
    .slice(0, 3)
    .map((item) => formatTopInsightSentence(item));
}

function getConclusion(
  reportState: CompletedAssessmentReportState | null,
  dimensions: DimensionViewModel[],
): string[] {
  if (reportState?.status !== "ready") {
    return ["Tvoji rezultati pokazuju prepoznatljiv obrazac po dimenzijama, ali narativni zaključak trenutno nije dostupan."];
  }

  const highest = dimensions[0];
  const lowest = dimensions[dimensions.length - 1];
  const summaryLead = splitIntoSentences(toSecondPersonSingular(reportState.report.summary))[0] ?? null;
  const firstParagraph = [
    summaryLead,
    highest ? `${highest.label} se kod tebe najviše ističe.` : null,
    lowest
      ? `${lowest.label} je suptilnija i daje mirniji ton tvom ukupnom obrascu.`
      : null,
  ]
    .filter((sentence): sentence is string => Boolean(sentence))
    .join(" ");
  const secondParagraph = toSecondPersonSingular(
    reportState.report.work_style[0] ??
      "U načinu na koji pristupaš radu vidi se jasan i prilično dosljedan obrazac.",
  );

  return [firstParagraph, secondParagraph].filter(Boolean);
}

function getRecommendations(reportState: CompletedAssessmentReportState | null): string[] {
  if (reportState?.status !== "ready") {
    return [];
  }

  const recommendations =
    reportState.report.development_recommendations.length > 0
      ? reportState.report.development_recommendations
      : reportState.report.blind_spots;

  return recommendations.slice(0, 4);
}

function formatRecommendation(item: string): { lead: string; body: string | null } {
  const normalized = item.trim().replace(/\s+/g, " ");
  const words = normalized.split(" ");

  if (normalized.includes(":")) {
    const [lead, ...rest] = normalized.split(":");
    return {
      lead: lead.trim(),
      body: rest.join(":").trim() || null,
    };
  }

  if (words.length <= 4) {
    return { lead: normalized.replace(/[.,;:!?]+$/, ""), body: null };
  }

  return {
    lead: words.slice(0, 4).join(" ").replace(/[.,;:!?]+$/, ""),
    body: words.slice(4).join(" ").trim() || null,
  };
}

export function CompletedAssessmentSummary({
  completedAt,
  organizationName,
  participantName,
  testName,
  results,
  reportState,
}: CompletedAssessmentSummaryProps) {
  const [expandedDimension, setExpandedDimension] = useState<string | null>(null);

  const maxRawScore =
    results && results.dimensions.length > 0
      ? Math.max(...results.dimensions.map((dimension) => dimension.rawScore), 0)
      : 0;

  const reportDimensionsByKey =
    reportState?.status === "ready"
      ? new Map(
          reportState.report.dimensions.map((dimension) => [dimension.dimension_key, dimension]),
        )
      : new Map();

  const dimensionCards: DimensionViewModel[] =
    results?.dimensions.map((dimension, index, dimensions) => {
      const reportDimension = reportDimensionsByKey.get(dimension.dimension);

      return {
        key: dimension.dimension,
        label: formatDimensionLabel(dimension.dimension),
        helperLabel: getDimensionHelperLabel(dimension.dimension),
        score: dimension.rawScore,
        averageScore: getAverageScore(dimension.rawScore, dimension.scoredQuestionCount),
        scoredQuestionCount: dimension.scoredQuestionCount,
        shortInterpretation:
          reportDimension?.short_interpretation ??
          "Detaljna interpretacija za ovu dimenziju trenutno nije dostupna.",
        scoreWidth: maxRawScore > 0 ? Math.max((dimension.rawScore / maxRawScore) * 100, 10) : 0,
        rank: index,
        totalDimensions: dimensions.length,
      };
    }) ?? [];

  const topInsights = getTopInsights(reportState, dimensionCards);
  const conclusionParagraphs = getConclusion(reportState, dimensionCards);
  const recommendations = getRecommendations(reportState);
  const scoreRangeLabel = maxRawScore > 0 ? `0–${maxRawScore} bodova` : null;
  const primaryMetaCount = [participantName, organizationName].filter(Boolean).length;

  return (
    <div className="results-report stack-md">
      <section className="results-report__hero">
        <div className="results-report__hero-copy">
          <p className="results-report__eyebrow">Izvještaj procjene</p>
          <h2>{testName ?? "Rezultati procjene"}</h2>

          <div className="results-report__hero-meta-wrap">
            <dl className="results-report__hero-meta">
              {participantName ? (
                <div className={primaryMetaCount === 1 ? "results-report__hero-meta-item results-report__hero-meta-item--wide" : "results-report__hero-meta-item"}>
                  <dt>Korisnik</dt>
                  <dd>{participantName}</dd>
                </div>
              ) : null}
              {organizationName ? (
                <div className={primaryMetaCount === 1 ? "results-report__hero-meta-item results-report__hero-meta-item--wide" : "results-report__hero-meta-item"}>
                  <dt>Organizacija</dt>
                  <dd>{organizationName}</dd>
                </div>
              ) : null}
              <div className="results-report__hero-meta-item results-report__hero-meta-item--wide">
                <dt>Završeno</dt>
                <dd>{formatCompletedAt(completedAt)}</dd>
              </div>
            </dl>
          </div>
        </div>

        {topInsights.length > 0 ? (
          <section
            className="results-report__hero-insights results-report__hero-insights--mobile"
            aria-label="Top insights"
          >
            <p className="results-report__hero-label">Top uvidi</p>
            <ul className="results-insight-list">
              {topInsights.map((insight) => (
                <li key={insight}>{insight}</li>
              ))}
            </ul>
          </section>
        ) : null}
      </section>

      {topInsights.length > 0 ? (
        <section
          className="results-report__section results-report__section--insights results-report__panel card stack-sm"
          aria-label="Top insights"
        >
          <div className="results-report__section-heading">
            <p className="results-report__section-kicker">Top uvidi</p>
            <h3>Sažetak ključnih obrazaca</h3>
          </div>
          <ul className="results-insight-list">
            {topInsights.map((insight) => (
              <li key={insight}>{insight}</li>
            ))}
          </ul>
        </section>
      ) : null}

      {results ? (
        <>
          <section className="results-report__section results-report__section--overview results-report__panel card stack-sm">
            <div className="results-report__section-heading">
              <h3>Pregled dimenzija</h3>
              {scoreRangeLabel ? <p className="results-report__section-note">{scoreRangeLabel}</p> : null}
            </div>

            {dimensionCards.length > 0 ? (
              <ol className="results-score-overview" aria-label="Pregled rezultata po dimenzijama">
                {dimensionCards.map((dimension) => (
                  <li key={dimension.key} className="results-score-overview__item">
                    <div className="results-score-overview__header">
                      <strong>{dimension.label}</strong>
                      <span>{formatScoreLabel(dimension.score)}</span>
                    </div>
                    <div
                      className="results-score-overview__bar"
                      role="img"
                      aria-label={`${dimension.label} skor ${dimension.score}`}
                    >
                      <span style={{ width: `${dimension.scoreWidth}%` }} />
                    </div>
                  </li>
                ))}
              </ol>
            ) : (
              <p>Za ovaj završeni pokušaj nisu dostupni odgovori koji se mogu bodovati.</p>
            )}
          </section>

          {dimensionCards.length > 0 ? (
            <section className="results-report__section results-report__section--dimensions stack-sm">
              <div className="results-report__section-heading">
                <h3>Dimenzije</h3>
              </div>

              <ol className="results-dimension-list">
                {dimensionCards.map((dimension) => {
                  const isExpanded = expandedDimension === dimension.key;
                  const detailId = `dimension-detail-${dimension.key}`;

                  return (
                    <li key={dimension.key} className="results-dimension-card">
                      <div className="results-dimension-card__header">
                        <div className="results-dimension-card__title">
                          <h4>{dimension.label}</h4>
                          {dimension.helperLabel ? (
                            <p className="results-dimension-card__helper">{dimension.helperLabel}</p>
                          ) : null}
                        </div>
                        <div className="results-dimension-card__score">
                          <span className="results-dimension-card__score-value">
                            {formatScoreLabel(dimension.score)}
                          </span>
                        </div>
                      </div>

                      <p className="results-dimension-card__summary">
                        {getLeadSentence(toSecondPersonSingular(dimension.shortInterpretation))}
                      </p>

                      <div className="results-dimension-card__footer">
                        <button
                          type="button"
                          className="results-dimension-card__toggle"
                          aria-expanded={isExpanded}
                          aria-controls={detailId}
                          onClick={() =>
                            setExpandedDimension((current) =>
                              current === dimension.key ? null : dimension.key,
                            )
                          }
                        >
                          <span className="results-dimension-card__toggle-label-mobile">
                            {isExpanded ? "Manje" : "Više"}
                          </span>
                          <span className="results-dimension-card__toggle-label-desktop" aria-hidden="true">
                            {isExpanded ? "Sakrij detalje" : "Prikaži detalje"}
                          </span>
                        </button>
                      </div>

                      {isExpanded ? (
                        <div id={detailId} className="results-dimension-card__details stack-xs">
                          {getDimensionDetailBlocks(dimension).map((detail) => (
                            <section key={detail.heading} className="results-dimension-card__detail-block">
                              <h5>{detail.heading}</h5>
                              <p>{detail.body}</p>
                            </section>
                          ))}
                        </div>
                      ) : null}
                    </li>
                  );
                })}
              </ol>
            </section>
          ) : null}

          {results.unscoredResponses.length > 0 ? (
            <section className="results-report__section results-report__panel card stack-sm">
              <div className="results-report__section-heading">
                <h3>Nebodovani odgovori</h3>
              </div>
              <ol className="results-inline-list">
                {results.unscoredResponses.map((response) => (
                  <li key={response.questionId}>
                    <strong>{response.questionCode}</strong>: {formatUnscoredReason(response.reason)}
                  </li>
                ))}
              </ol>
            </section>
          ) : null}
        </>
      ) : null}

      {reportState?.status === "ready" ? (
        <div className="results-report__closing stack-md">
          <section className="results-report__section results-report__section--conclusion results-report__panel card stack-sm">
            <div className="results-report__section-heading">
              <h3>Zaključak</h3>
            </div>
            <div className="results-report__section-body stack-xs">
              {conclusionParagraphs.map((paragraph) => (
                <p key={paragraph}>{paragraph}</p>
              ))}
            </div>
          </section>

          {recommendations.length > 0 ? (
            <section className="results-report__section results-report__section--recommendations results-report__panel card stack-sm">
              <div className="results-report__section-heading">
                <h3>Preporuke</h3>
              </div>
              <ul className="results-bullet-list">
                {recommendations.map((item) => {
                  const formatted = formatRecommendation(toSecondPersonSingular(item));

                  return (
                    <li key={item}>
                      <strong>{formatted.lead}:</strong>
                      {formatted.body ? ` ${formatted.body}` : null}
                    </li>
                  );
                })}
              </ul>
            </section>
          ) : null}
        </div>
      ) : null}

      {reportState?.status === "ready" ? (
        <p className="results-report__disclaimer">{reportState.report.disclaimer}</p>
      ) : null}

      {reportState?.status === "unavailable" ? (
        <section className="results-report__section results-report__panel card stack-sm">
          <div className="results-report__section-heading">
            <h3>Narativni izvještaj trenutno nije dostupan</h3>
          </div>
          <p className="results-report__section-body">
            AI izvještaj trenutno nije dostupan za ovaj završeni pokušaj. Numerički rezultati i
            dalje ostaju sačuvani za pregled.
          </p>
        </section>
      ) : null}
    </div>
  );
}
</file>

<file path="lib/assessment/reports.ts">
import "server-only";

import { loadAssessmentCompletionState } from "@/lib/assessment/completion-server";
import { getAiReportConfig } from "@/lib/assessment/report-config";
import { buildPreparedReportGenerationInput } from "@/lib/assessment/report-provider-helpers";
import { mockReportProvider } from "@/lib/assessment/report-provider-mock";
import { createSelectedReportProvider } from "@/lib/assessment/report-provider-registry";
import type {
  AttemptReportStatus,
  CompletedAssessmentReport,
  CompletedAssessmentReportRequest,
  ReportGeneratorType,
} from "@/lib/assessment/report-providers";
import {
  isAttemptReportStatus,
  isCompletedAssessmentReport,
} from "@/lib/assessment/report-providers";
import type { CompletedAssessmentResults } from "@/lib/assessment/scoring";
import { calculateCompletedAssessmentResults } from "@/lib/assessment/scoring";
import type { ScoringMethod } from "@/lib/assessment/types";
import { createSupabaseAdminClient } from "@/lib/supabase/admin";

type AttemptReportRow = {
  attempt_id: string;
  test_slug: string;
  generator_type: ReportGeneratorType;
  generated_at: string;
  report_status: AttemptReportStatus;
  failure_code: string | null;
  failure_reason: string | null;
  report_snapshot: unknown;
};

type AttemptRecord = {
  id: string;
  status: "in_progress" | "completed" | "abandoned";
};

type TestRecord = {
  id: string;
  slug: string;
  scoring_method: ScoringMethod;
};

type LoadedReportContext = {
  supabase: ReturnType<typeof createSupabaseAdminClient>;
  test: TestRecord;
  results: CompletedAssessmentResults;
};

export type CompletedAssessmentReportState =
  | {
      status: "ready";
      report: CompletedAssessmentReport;
    }
  | {
      status: "unavailable";
      generatorType: ReportGeneratorType;
      generatedAt: string;
      failureCode: string | null;
      failureReason: string | null;
    };

type ReportGenerationResult =
  | {
      status: "ready";
      report: CompletedAssessmentReport;
    }
  | {
      status: "unavailable";
      generatorType: ReportGeneratorType;
      failureCode: string;
      failureReason: string;
    };

async function generateReportWithFallback(
  input: CompletedAssessmentReportRequest,
): Promise<ReportGenerationResult> {
  const config = getAiReportConfig();
  const selectedProvider = createSelectedReportProvider();
  const preparedInput = buildPreparedReportGenerationInput(input);
  const primaryResult = await selectedProvider.generateReport(preparedInput);

  if (primaryResult.ok) {
    return {
      status: "ready",
      report: primaryResult.report,
    };
  }

  console.error("Report generation failed for primary provider", {
    provider: selectedProvider.type,
    attemptId: input.attemptId,
    testSlug: input.testSlug,
    reason: primaryResult.reason,
    fallbackToMockEnabled: config.fallbackToMock,
  });

  if (selectedProvider.type !== "mock" && config.fallbackToMock) {
    const fallbackResult = await mockReportProvider.generateReport(preparedInput);

    if (fallbackResult.ok) {
      return {
        status: "ready",
        report: fallbackResult.report,
      };
    }

    console.error("Report generation fallback failed", {
      provider: mockReportProvider.type,
      attemptId: input.attemptId,
      testSlug: input.testSlug,
      reason: fallbackResult.reason,
    });

    return {
      status: "unavailable",
      generatorType: mockReportProvider.type,
      failureCode: "report_generation_failed",
      failureReason: fallbackResult.reason,
    };
  }

  return {
    status: "unavailable",
    generatorType: selectedProvider.type,
    failureCode: "report_generation_failed",
    failureReason: primaryResult.reason,
  };
}

async function loadReportContext(testId: string, attemptId: string): Promise<LoadedReportContext | null> {
  const supabase = createSupabaseAdminClient();

  const { data: attemptData, error: attemptError } = await supabase
    .from("attempts")
    .select("id, status")
    .eq("id", attemptId)
    .eq("test_id", testId)
    .maybeSingle();

  if (attemptError) {
    throw new Error(`Failed to load attempt for report generation: ${attemptError.message}`);
  }

  const attempt = attemptData as AttemptRecord | null;

  if (!attempt || attempt.status !== "completed") {
    return null;
  }

  const completionState = await loadAssessmentCompletionState(testId, attemptId);

  if (!completionState.isComplete) {
    return null;
  }

  const { data: testData, error: testError } = await supabase
    .from("tests")
    .select("id, slug, scoring_method")
    .eq("id", testId)
    .maybeSingle();

  if (testError || !testData) {
    throw new Error(
      `Failed to load test for report generation: ${testError?.message ?? "Unknown error"}`,
    );
  }

  const results = await calculateCompletedAssessmentResults(testId, attemptId);

  if (!results) {
    return null;
  }

  return {
    supabase,
    test: testData as TestRecord,
    results,
  };
}

async function loadPersistedReportSnapshot(
  context: LoadedReportContext,
  attemptId: string,
): Promise<CompletedAssessmentReportState | null> {
  const { data, error } = await context.supabase
    .from("attempt_reports")
    .select(
      "attempt_id, test_slug, generator_type, generated_at, report_status, failure_code, failure_reason, report_snapshot",
    )
    .eq("attempt_id", attemptId)
    .maybeSingle();

  if (error) {
    throw new Error(`Failed to load attempt report: ${error.message}`);
  }

  const row = data as AttemptReportRow | null;

  if (!row) {
    return null;
  }

  if (!isAttemptReportStatus(row.report_status)) {
    throw new Error(`Invalid attempt report status for attempt ${attemptId}.`);
  }

  if (row.report_status === "ready") {
    if (!isCompletedAssessmentReport(row.report_snapshot)) {
      throw new Error(`Attempt report ${attemptId} is marked ready without a valid snapshot.`);
    }

    return {
      status: "ready",
      report: row.report_snapshot,
    };
  }

  return {
    status: "unavailable",
    generatorType: row.generator_type,
    generatedAt: row.generated_at,
    failureCode: row.failure_code,
    failureReason: row.failure_reason,
  };
}

export async function getCompletedAssessmentReport(
  testId: string,
  attemptId: string | null,
): Promise<CompletedAssessmentReportState | null> {
  if (!attemptId) {
    return null;
  }

  const context = await loadReportContext(testId, attemptId);

  if (!context) {
    return null;
  }

  const persistedReport = await loadPersistedReportSnapshot(context, attemptId);

  if (persistedReport) {
    return persistedReport;
  }

  return persistCompletedAssessmentReport(testId, attemptId, context);
}

export async function persistCompletedAssessmentReport(
  testId: string,
  attemptId: string,
  existingContext?: LoadedReportContext,
): Promise<CompletedAssessmentReportState | null> {
  const context = existingContext ?? (await loadReportContext(testId, attemptId));

  if (!context) {
    return null;
  }

  const existingReport = await loadPersistedReportSnapshot(context, attemptId);

  if (existingReport) {
    return existingReport;
  }

  const generationResult = await generateReportWithFallback({
    attemptId,
    testSlug: context.test.slug,
    scoringMethod: context.test.scoring_method,
    promptVersion: getAiReportConfig().promptVersion,
    results: context.results,
  });

  const persistedGeneratedAt =
    generationResult.status === "ready"
      ? generationResult.report.generated_at
      : new Date().toISOString();

  const { error } = await context.supabase.from("attempt_reports").upsert(
    generationResult.status === "ready"
      ? {
          attempt_id: attemptId,
          test_slug: generationResult.report.test_slug,
          generator_type: generationResult.report.generator_type,
          generated_at: persistedGeneratedAt,
          report_status: "ready",
          failure_code: null,
          failure_reason: null,
          report_snapshot: generationResult.report,
        }
      : {
          attempt_id: attemptId,
          test_slug: context.test.slug,
          generator_type: generationResult.generatorType,
          generated_at: persistedGeneratedAt,
          report_status: "unavailable",
          failure_code: generationResult.failureCode,
          failure_reason: generationResult.failureReason,
          report_snapshot: null,
        },
    {
      onConflict: "attempt_id",
    },
  );

  if (error) {
    throw new Error(`Failed to persist attempt report: ${error.message}`);
  }

  if (generationResult.status === "ready") {
    return generationResult;
  }

  return {
    status: "unavailable",
    generatorType: generationResult.generatorType,
    generatedAt: persistedGeneratedAt,
    failureCode: generationResult.failureCode,
    failureReason: generationResult.failureReason,
  };
}

export type { CompletedAssessmentReport } from "@/lib/assessment/report-providers";
</file>

<file path="scripts/verify-report-flow.mjs">
import process from "node:process";
import { setTimeout as delay } from "node:timers/promises";
import { createClient } from "@supabase/supabase-js";

const APP_URL = process.env.APP_URL ?? "http://localhost:3100";
const ATTEMPT_COOKIE_NAME = "assessment_attempt_id";
const HEALTH_URL = `${APP_URL}/api/health`;
const EXPECTED_ACTIVE_TEST_SLUG = "ipip50-hr-v1";
const EXPECTED_REPORT_BEHAVIOR = process.env.VERIFY_REPORT_EXPECTED_BEHAVIOR ?? "snapshot";
const EXPECTED_REPORT_GENERATOR = process.env.VERIFY_REPORT_EXPECTED_GENERATOR ?? "mock";
const REPORT_CASES = [
  { code: "E01", optionIndex: 4 },
  { code: "A01", optionIndex: 3 },
  { code: "C01", optionIndex: 2 },
  { code: "ES01", optionIndex: 1 },
  { code: "O01", optionIndex: 0 },
];

function fail(message) {
  throw new Error(message);
}

function assertIncludes(haystack, needle, message) {
  if (!haystack.includes(needle)) {
    fail(message);
  }
}

function assertNotIncludes(haystack, needle, message) {
  if (haystack.includes(needle)) {
    fail(message);
  }
}

async function fetchAssessmentPage(attemptId) {
  const response = await fetch(APP_URL, {
    headers: {
      cookie: `${ATTEMPT_COOKIE_NAME}=${attemptId}`,
    },
  });

  if (!response.ok) {
    fail(`Page request failed with status ${response.status}.`);
  }

  return response.text();
}

async function waitForServer() {
  let lastDetail = `No response from ${HEALTH_URL}.`;

  for (let attempt = 0; attempt < 120; attempt += 1) {
    try {
      const response = await fetch(HEALTH_URL);

      if (response.ok) {
        return;
      }

      lastDetail = `HTTP ${response.status} from ${HEALTH_URL}.`;
    } catch (error) {
      lastDetail = error instanceof Error ? error.message : String(error);
    }

    await delay(1000);
  }

  fail(`Next.js server did not become ready at ${HEALTH_URL}. Last check: ${lastDetail}`);
}

async function loadRequiredQuestionsWithOptions(supabase, testId) {
  const { data: questions, error: questionsError } = await supabase
    .from("questions")
    .select("id, code, question_type, is_required")
    .eq("test_id", testId)
    .eq("is_active", true)
    .order("question_order", { ascending: true });

  if (questionsError || !questions) {
    fail(`Unable to load report verification questions: ${questionsError?.message ?? "Unknown error"}`);
  }

  const requiredQuestions = questions.filter((question) => question.is_required);
  const nonTextQuestionIds = requiredQuestions
    .filter((question) => question.question_type !== "text")
    .map((question) => question.id);

  const { data: answerOptions, error: answerOptionsError } = await supabase
    .from("answer_options")
    .select("id, question_id, option_order")
    .in("question_id", nonTextQuestionIds)
    .order("question_id", { ascending: true })
    .order("option_order", { ascending: true });

  if (answerOptionsError || !answerOptions) {
    fail(
      `Unable to load report verification options: ${answerOptionsError?.message ?? "Unknown error"}`,
    );
  }

  const answerOptionsByQuestionId = answerOptions.reduce((groupedOptions, option) => {
    const questionOptions = groupedOptions.get(option.question_id) ?? [];
    questionOptions.push(option);
    groupedOptions.set(option.question_id, questionOptions);
    return groupedOptions;
  }, new Map());

  return { requiredQuestions, answerOptionsByQuestionId };
}

async function insertAnswerForQuestion(supabase, attemptId, question, answerOptionsByQuestionId, optionIndex = 0) {
  if (question.question_type === "text") {
    const { error } = await supabase.from("responses").insert({
      attempt_id: attemptId,
      question_id: question.id,
      response_kind: "text",
      text_value: `Verification response for ${question.code}`,
    });

    if (error) {
      fail(`Unable to create text response for ${question.code}: ${error.message}`);
    }

    return;
  }

  const options = answerOptionsByQuestionId.get(question.id) ?? [];
  const selectedOption = options[optionIndex] ?? options[0];

  if (!selectedOption) {
    fail(`Expected option index ${optionIndex} for ${question.code}.`);
  }

  if (question.question_type === "single_choice") {
    const { error } = await supabase.from("responses").insert({
      attempt_id: attemptId,
      question_id: question.id,
      response_kind: "single_choice",
      answer_option_id: selectedOption.id,
    });

    if (error) {
      fail(`Unable to create single choice response for ${question.code}: ${error.message}`);
    }

    return;
  }

  const { data: responseRow, error: responseError } = await supabase
    .from("responses")
    .insert({
      attempt_id: attemptId,
      question_id: question.id,
      response_kind: "multiple_choice",
    })
    .select("id")
    .single();

  if (responseError || !responseRow) {
    fail(`Unable to create multiple choice response for ${question.code}: ${responseError?.message ?? "Unknown error"}`);
  }

  const { error: selectionError } = await supabase.from("response_selections").insert({
    response_id: responseRow.id,
    question_id: question.id,
    answer_option_id: selectedOption.id,
  });

  if (selectionError) {
    fail(`Unable to create multiple choice selection for ${question.code}: ${selectionError.message}`);
  }
}

async function createAttempt(supabase, testId, status) {
  const insert = status === "completed"
    ? { test_id: testId, status: "completed", completed_at: new Date().toISOString() }
    : { test_id: testId };

  const { data, error } = await supabase.from("attempts").insert(insert).select("id").single();

  if (error || !data) {
    fail(`Unable to create ${status} attempt: ${error?.message ?? "Unknown error"}`);
  }

  return data.id;
}

async function assertSnapshotBehavior(supabase, validAttemptId) {
  const firstHtml = await fetchAssessmentPage(validAttemptId);
  assertIncludes(firstHtml, "AI izvještaj procjene", "Expected assessment report section to render.");
  assertIncludes(firstHtml, "Generator:", "Expected generator label to render.");
  assertIncludes(firstHtml, "Sačuvani izvještaj generisan:", "Expected report timestamp label to render.");
  assertIncludes(firstHtml, "Snage", "Expected strengths section to render.");
  assertIncludes(firstHtml, "Slijepe tačke", "Expected blind spots section to render.");
  assertIncludes(
    firstHtml,
    "Preporuke za razvoj",
    "Expected recommendations section to render.",
  );

  const { data: firstReportRow, error: firstReportError } = await supabase
    .from("attempt_reports")
    .select("generated_at, generator_type, report_snapshot")
    .eq("attempt_id", validAttemptId)
    .single();

  if (firstReportError || !firstReportRow) {
    fail(
      `Unable to load persisted report snapshot: ${firstReportError?.message ?? "Unknown error"}`,
    );
  }

  if (firstReportRow.generator_type !== EXPECTED_REPORT_GENERATOR) {
    fail(`Expected persisted generator_type ${EXPECTED_REPORT_GENERATOR}, received ${firstReportRow.generator_type}.`);
  }

  if (firstReportRow.report_snapshot?.generator_type !== EXPECTED_REPORT_GENERATOR) {
    fail(`Expected persisted report snapshot to include generator_type ${EXPECTED_REPORT_GENERATOR}.`);
  }

  const firstGeneratedAt = firstReportRow.generated_at;
  const secondHtml = await fetchAssessmentPage(validAttemptId);
  assertIncludes(secondHtml, "AI izvještaj procjene", "Expected assessment report section to remain on reload.");
  assertIncludes(secondHtml, "Generator:", "Expected generator label to remain on reload.");

  const { data: secondReportRow, error: secondReportError } = await supabase
    .from("attempt_reports")
    .select("generated_at")
    .eq("attempt_id", validAttemptId)
    .single();

  if (secondReportError || !secondReportRow) {
    fail(
      `Unable to reload persisted report snapshot: ${secondReportError?.message ?? "Unknown error"}`,
    );
  }

  if (secondReportRow.generated_at !== firstGeneratedAt) {
    fail("Expected report snapshot to stay stable on reload without regeneration.");
  }
}

async function assertUnavailableBehavior(supabase, validAttemptId) {
  const firstHtml = await fetchAssessmentPage(validAttemptId);
  assertIncludes(firstHtml, "AI izvještaj procjene", "Unavailable report should still render a stable report section.");
  assertIncludes(
    firstHtml,
    "AI izvještaj trenutno nije dostupan za ovaj završeni pokušaj.",
    "Unavailable report message should render.",
  );

  const { data: firstReportRow, error: firstReportError } = await supabase
    .from("attempt_reports")
    .select("report_status, generator_type, generated_at, report_snapshot, failure_code")
    .eq("attempt_id", validAttemptId)
    .single();

  if (firstReportError) {
    fail(`Unable to inspect unavailable report state: ${firstReportError.message}`);
  }

  if (firstReportRow.report_status !== "unavailable") {
    fail(`Expected unavailable report status, received ${firstReportRow.report_status}.`);
  }

  if (firstReportRow.generator_type !== EXPECTED_REPORT_GENERATOR) {
    fail(`Expected unavailable generator_type ${EXPECTED_REPORT_GENERATOR}, received ${firstReportRow.generator_type}.`);
  }

  if (firstReportRow.report_snapshot !== null) {
    fail("Unavailable report scenario should persist a null report snapshot.");
  }

  if (firstReportRow.failure_code !== "report_generation_failed") {
    fail(`Expected failure_code report_generation_failed, received ${firstReportRow.failure_code}.`);
  }

  const firstGeneratedAt = firstReportRow.generated_at;
  const secondHtml = await fetchAssessmentPage(validAttemptId);
  assertIncludes(
    secondHtml,
    "AI izvještaj trenutno nije dostupan za ovaj završeni pokušaj.",
    "Unavailable report message should remain stable on reload.",
  );

  const { data: secondReportRow, error: secondReportError } = await supabase
    .from("attempt_reports")
    .select("generated_at")
    .eq("attempt_id", validAttemptId)
    .single();

  if (secondReportError || !secondReportRow) {
    fail(
      `Unable to reload unavailable report snapshot: ${secondReportError?.message ?? "Unknown error"}`,
    );
  }

  if (secondReportRow.generated_at !== firstGeneratedAt) {
    fail("Expected unavailable report marker to stay stable on reload without regeneration.");
  }
}

async function main() {
  const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL;
  const serviceRoleKey = process.env.SUPABASE_SERVICE_ROLE_KEY;

  if (!supabaseUrl || !serviceRoleKey) {
    fail("Missing required Supabase env vars.");
  }

  const supabase = createClient(supabaseUrl, serviceRoleKey, {
    auth: {
      autoRefreshToken: false,
      persistSession: false,
    },
  });

  await waitForServer();

  const { data: activeTest, error: activeTestError } = await supabase
    .from("tests")
    .select("id, slug")
    .eq("is_active", true)
    .maybeSingle();

  if (activeTestError || !activeTest) {
    fail(`Unable to load active test: ${activeTestError?.message ?? "Unknown error"}`);
  }

  if (activeTest.slug !== EXPECTED_ACTIVE_TEST_SLUG) {
    fail(`Expected active test ${EXPECTED_ACTIVE_TEST_SLUG}, received ${activeTest.slug}.`);
  }

  const { requiredQuestions, answerOptionsByQuestionId } = await loadRequiredQuestionsWithOptions(
    supabase,
    activeTest.id,
  );
  const reportOverrides = new Map(REPORT_CASES.map((reportCase) => [reportCase.code, reportCase.optionIndex]));

  const incompleteAttemptId = await createAttempt(supabase, activeTest.id, "completed");
  const partialQuestion = requiredQuestions.find((question) => question.code === REPORT_CASES[0].code);

  if (!partialQuestion) {
    fail(`Expected seeded question ${REPORT_CASES[0].code} was not found.`);
  }

  await insertAnswerForQuestion(
    supabase,
    incompleteAttemptId,
    partialQuestion,
    answerOptionsByQuestionId,
    REPORT_CASES[0].optionIndex,
  );

  const incompleteHtml = await fetchAssessmentPage(incompleteAttemptId);
  assertNotIncludes(
    incompleteHtml,
    "AI izvještaj procjene",
    "Incomplete completed attempt should not render a report.",
  );

  const { data: incompleteReportRow, error: incompleteReportError } = await supabase
    .from("attempt_reports")
    .select("attempt_id")
    .eq("attempt_id", incompleteAttemptId)
    .maybeSingle();

  if (incompleteReportError) {
    fail(`Unable to inspect incomplete attempt report state: ${incompleteReportError.message}`);
  }

  if (incompleteReportRow) {
    fail("Incomplete completed attempt should not persist a report snapshot.");
  }

  const validAttemptId = await createAttempt(supabase, activeTest.id, "completed");

  for (const question of requiredQuestions) {
    await insertAnswerForQuestion(
      supabase,
      validAttemptId,
      question,
      answerOptionsByQuestionId,
      reportOverrides.get(question.code) ?? 0,
    );
  }

  if (EXPECTED_REPORT_BEHAVIOR === "unavailable") {
    await assertUnavailableBehavior(supabase, validAttemptId);
  } else {
    await assertSnapshotBehavior(supabase, validAttemptId);
  }

  console.log("Report flow verification passed.");
  console.log(`Verified incomplete attempt id: ${incompleteAttemptId}`);
  console.log(`Verified valid completed attempt id: ${validAttemptId}`);
  console.log("Verified behaviors:");
  console.log("- incomplete completed attempt does not generate or render a report");

  if (EXPECTED_REPORT_BEHAVIOR === "unavailable") {
    console.log("- fully answered completed attempt stays stable when report generation is unavailable");
  } else {
    console.log(`- fully answered completed attempt generates a persisted ${EXPECTED_REPORT_GENERATOR} report snapshot on first render`);
    console.log("- reload reuses the same generated_at timestamp instead of regenerating a new report");
  }
}

main().catch((error) => {
  console.error(error instanceof Error ? error.message : String(error));
  process.exitCode = 1;
});
</file>

<file path="app/(protected)/dashboard/attempts/[attemptId]/page.tsx">
import { notFound, redirect } from "next/navigation";
import { CompletedAssessmentSummary } from "@/components/assessment/completed-assessment-summary";
import {
  getCompletedAssessmentReportSnapshot,
  getCompletedAssessmentResults,
} from "@/lib/assessment/tests";
import {
  getActiveOrganizationForUser,
  getAttemptForOrganization,
} from "@/lib/b2b/organizations";
import { requireAuthenticatedUser } from "@/lib/auth/session";

type AttemptDetailPageProps = {
  params: {
    attemptId: string;
  };
};

export const dynamic = "force-dynamic";

export default async function AttemptDetailPage({ params }: AttemptDetailPageProps) {
  const user = await requireAuthenticatedUser();
  const organization = await getActiveOrganizationForUser(user.id);

  if (!organization) {
    notFound();
  }

  const attempt = await getAttemptForOrganization(organization.id, params.attemptId);

  if (!attempt) {
    notFound();
  }

  if (attempt.status !== "completed") {
    redirect(`/dashboard/attempts/${attempt.id}/run`);
  }

  const [results, report] = await Promise.all([
    getCompletedAssessmentResults(attempt.test_id, attempt.id),
    getCompletedAssessmentReportSnapshot(attempt.test_id, attempt.id),
  ]);

  return (
    <main className="attempt-results-page stack-md">
      <section className="attempt-results-page__content">
        <CompletedAssessmentSummary
          completedAt={attempt.completed_at}
          organizationName={attempt.organizations?.name ?? organization.name}
          participantName={attempt.participants?.full_name ?? null}
          testName={attempt.tests?.name ?? attempt.tests?.slug ?? null}
          results={results}
          reportState={report}
        />
      </section>
    </main>
  );
}
</file>

<file path="scripts/verify-completion-flow.mjs">
import process from "node:process";
import { setTimeout as delay } from "node:timers/promises";
import { createClient } from "@supabase/supabase-js";

const APP_URL = process.env.APP_URL ?? "http://localhost:3100";
const ATTEMPT_COOKIE_NAME = "assessment_attempt_id";
const HEALTH_URL = `${APP_URL}/api/health`;
const EXPECTED_ACTIVE_TEST_SLUG = "ipip50-hr-v1";
const COMPLETION_CODE = "E01";

function fail(message) {
  throw new Error(message);
}

function assertIncludes(haystack, needle, message) {
  if (!haystack.includes(needle)) {
    fail(message);
  }
}

function assertNotIncludes(haystack, needle, message) {
  if (haystack.includes(needle)) {
    fail(message);
  }
}

async function fetchAssessmentPage(attemptId) {
  const response = await fetch(APP_URL, {
    headers: {
      cookie: `${ATTEMPT_COOKIE_NAME}=${attemptId}`,
    },
  });

  if (!response.ok) {
    fail(`Page request failed with status ${response.status}.`);
  }

  return response.text();
}

async function waitForServer() {
  let lastDetail = `No response from ${HEALTH_URL}.`;

  for (let attempt = 0; attempt < 120; attempt += 1) {
    try {
      const response = await fetch(HEALTH_URL);

      if (response.ok) {
        return;
      }

      lastDetail = `HTTP ${response.status} from ${HEALTH_URL}.`;
    } catch (error) {
      lastDetail = error instanceof Error ? error.message : String(error);
    }

    await delay(1000);
  }

  fail(`Next.js server did not become ready at ${HEALTH_URL}. Last check: ${lastDetail}`);
}

async function loadRequiredQuestionsWithOptions(supabase, testId) {
  const { data: questions, error: questionsError } = await supabase
    .from("questions")
    .select("id, code, question_type, is_required")
    .eq("test_id", testId)
    .eq("is_active", true)
    .order("question_order", { ascending: true });

  if (questionsError || !questions) {
    fail(`Unable to load active questions: ${questionsError?.message ?? "Unknown error"}`);
  }

  const requiredQuestions = questions.filter((question) => question.is_required);
  const nonTextQuestionIds = requiredQuestions
    .filter((question) => question.question_type !== "text")
    .map((question) => question.id);

  const { data: answerOptions, error: answerOptionsError } = await supabase
    .from("answer_options")
    .select("id, question_id, option_order")
    .in("question_id", nonTextQuestionIds)
    .order("question_id", { ascending: true })
    .order("option_order", { ascending: true });

  if (answerOptionsError) {
    fail(`Unable to load answer options: ${answerOptionsError.message}`);
  }

  const answerOptionsByQuestionId = (answerOptions ?? []).reduce((grouped, option) => {
    const questionOptions = grouped.get(option.question_id) ?? [];
    questionOptions.push(option);
    grouped.set(option.question_id, questionOptions);
    return grouped;
  }, new Map());

  return { requiredQuestions, answerOptionsByQuestionId };
}

async function insertAnswerForQuestion(supabase, attemptId, question, answerOptionsByQuestionId, optionIndex = 0) {
  if (question.question_type === "text") {
    const { error } = await supabase.from("responses").insert({
      attempt_id: attemptId,
      question_id: question.id,
      response_kind: "text",
      text_value: `Verification response for ${question.code}`,
    });

    if (error) {
      fail(`Unable to create text response for ${question.code}: ${error.message}`);
    }

    return;
  }

  const options = answerOptionsByQuestionId.get(question.id) ?? [];
  const selectedOption = options[optionIndex] ?? options[0];

  if (!selectedOption) {
    fail(`Unable to find answer option for ${question.code}.`);
  }

  if (question.question_type === "single_choice") {
    const { error } = await supabase.from("responses").insert({
      attempt_id: attemptId,
      question_id: question.id,
      response_kind: "single_choice",
      answer_option_id: selectedOption.id,
    });

    if (error) {
      fail(`Unable to create single choice response for ${question.code}: ${error.message}`);
    }

    return;
  }

  const { data: responseRow, error: responseError } = await supabase
    .from("responses")
    .insert({
      attempt_id: attemptId,
      question_id: question.id,
      response_kind: "multiple_choice",
    })
    .select("id")
    .single();

  if (responseError || !responseRow) {
    fail(`Unable to create multiple choice response for ${question.code}: ${responseError?.message ?? "Unknown error"}`);
  }

  const { error: selectionError } = await supabase.from("response_selections").insert({
    response_id: responseRow.id,
    question_id: question.id,
    answer_option_id: selectedOption.id,
  });

  if (selectionError) {
    fail(`Unable to create multiple choice selection for ${question.code}: ${selectionError.message}`);
  }
}

async function createAttempt(supabase, testId, status = "in_progress") {
  const insert = status === "completed"
    ? { test_id: testId, status: "completed", completed_at: new Date().toISOString() }
    : { test_id: testId };

  const { data, error } = await supabase.from("attempts").insert(insert).select("id").single();

  if (error || !data) {
    fail(`Unable to create attempt: ${error?.message ?? "Unknown error"}`);
  }

  return data.id;
}

async function main() {
  const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL;
  const serviceRoleKey = process.env.SUPABASE_SERVICE_ROLE_KEY;

  if (!supabaseUrl || !serviceRoleKey) {
    fail("Missing required Supabase env vars.");
  }

  const supabase = createClient(supabaseUrl, serviceRoleKey, {
    auth: {
      autoRefreshToken: false,
      persistSession: false,
    },
  });

  await waitForServer();

  const { data: activeTest, error: activeTestError } = await supabase
    .from("tests")
    .select("id, slug")
    .eq("is_active", true)
    .maybeSingle();

  if (activeTestError || !activeTest) {
    fail(`Unable to load active test: ${activeTestError?.message ?? "Unknown error"}`);
  }

  if (activeTest.slug !== EXPECTED_ACTIVE_TEST_SLUG) {
    fail(`Expected active test ${EXPECTED_ACTIVE_TEST_SLUG}, received ${activeTest.slug}.`);
  }

  const { requiredQuestions, answerOptionsByQuestionId } = await loadRequiredQuestionsWithOptions(
    supabase,
    activeTest.id,
  );

  const completionQuestion = requiredQuestions.find((question) => question.code === COMPLETION_CODE);

  if (!completionQuestion) {
    fail(`Expected required question ${COMPLETION_CODE} was not found.`);
  }

  const inProgressAttemptId = await createAttempt(supabase, activeTest.id);
  await insertAnswerForQuestion(
    supabase,
    inProgressAttemptId,
    completionQuestion,
    answerOptionsByQuestionId,
    3,
  );

  const inProgressHtml = await fetchAssessmentPage(inProgressAttemptId);
  assertIncludes(
    inProgressHtml,
    "Answer the remaining",
    "Expected incomplete completion guidance to render for an in-progress attempt.",
  );
  assertIncludes(
    inProgressHtml,
    "Complete assessment",
    "Expected completion button to remain visible before completion.",
  );
  assertNotIncludes(
    inProgressHtml,
    "Procjena je završena.",
    "Incomplete in-progress attempt should not render completed messaging.",
  );

  const completedAttemptId = await createAttempt(supabase, activeTest.id, "completed");

  for (const question of requiredQuestions) {
    const optionIndex = question.code === COMPLETION_CODE ? 3 : 0;
    await insertAnswerForQuestion(
      supabase,
      completedAttemptId,
      question,
      answerOptionsByQuestionId,
      optionIndex,
    );
  }

  const html = await fetchAssessmentPage(completedAttemptId);
  assertIncludes(html, "Procjena je završena.", "Expected completed state message to render.");
  assertIncludes(
    html,
    "Vaši odgovori su sada dostupni samo za pregled.",
    "Expected read-only completed messaging to render.",
  );
  assertIncludes(html, "fieldset disabled", "Expected completed page fieldsets to be disabled.");
  assertIncludes(html, "Rezultati", "Expected results section to render for a completed attempt.");
  assertIncludes(html, "Ekstraverzija", "Expected the completed result dimension to render.");
  assertNotIncludes(
    html,
    "Answer the remaining",
    "Completed attempt should not render incomplete completion messaging.",
  );
  assertNotIncludes(html, "Save progress", "Completed attempt should not render the save button.");

  console.log("Completion flow verification passed.");
  console.log(`Verified in-progress attempt id: ${inProgressAttemptId}`);
  console.log(`Verified completed attempt id: ${completedAttemptId}`);
  console.log("Verified behaviors:");
  console.log("- incomplete in-progress attempt keeps save/resume state and shows completion guidance");
  console.log("- fully answered required-question attempt reloads as completed and read-only");
  console.log("- completed attempt renders results and hides save actions");
}

main().catch((error) => {
  console.error(error instanceof Error ? error.message : String(error));
  process.exitCode = 1;
});
</file>

<file path="app/globals.css">
:root {
  font-family: Arial, Helvetica, sans-serif;
  color-scheme: light;
  --page-bg: #f3f5f8;
  --surface: #ffffff;
  --surface-muted: #f8fafc;
  --border: #d8e0ea;
  --border-strong: #bcc8d6;
  --text: #16202b;
  --text-muted: #5b6776;
  --accent: #1f3a5f;
  --accent-soft: #eaf0f7;
  --success-bg: #edf7f1;
  --success-text: #1e6a46;
  --danger-bg: #fbefee;
  --danger-text: #9c3d36;
}

* {
  box-sizing: border-box;
}

body {
  margin: 0;
  padding: 2rem;
  background:
    radial-gradient(circle at top, #ffffff 0%, #f6f8fb 38%, var(--page-bg) 100%);
  color: var(--text);
}

main {
  max-width: 48rem;
}

.card {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: 12px;
  padding: 1rem;
  box-shadow: 0 20px 50px -40px rgba(15, 23, 42, 0.35);
}

.stack-xs > * + * {
  margin-top: 0.375rem;
}

.stack-sm > * + * {
  margin-top: 0.75rem;
}

.stack-md > * + * {
  margin-top: 1rem;
}

input,
textarea,
button {
  font: inherit;
}

input:not([type="radio"]):not([type="checkbox"]),
textarea {
  width: 100%;
  padding: 0.75rem;
  border: 1px solid var(--border-strong);
  border-radius: 8px;
  background: #fff;
}

button {
  padding: 0.7rem 1rem;
  border: 1px solid var(--accent);
  border-radius: 8px;
  background: var(--accent);
  color: #fff;
  cursor: pointer;
}

button:disabled {
  cursor: not-allowed;
  opacity: 0.7;
}

.button-secondary {
  background: var(--surface);
  color: var(--text);
  border-color: var(--border-strong);
}

.sr-only {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  white-space: nowrap;
  border: 0;
}

.assessment-eyebrow {
  margin: 0;
  font-size: 0.75rem;
  font-weight: 700;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: var(--accent);
}

.assessment-run-page {
  max-width: 82rem;
  margin: 0 auto;
  padding: clamp(1rem, 2vw, 1.5rem);
  border-radius: 36px;
  background:
    radial-gradient(circle at top left, rgba(157, 171, 150, 0.14), transparent 30%),
    radial-gradient(circle at top right, rgba(168, 118, 92, 0.08), transparent 24%),
    linear-gradient(180deg, #f2eade 0%, #e9decf 100%);
}

.assessment-run-hero {
  position: relative;
  overflow: hidden;
  border: 1px solid #d5c8b7;
  border-radius: 30px;
  padding: clamp(1.6rem, 2.5vw, 2.45rem);
  background:
    linear-gradient(135deg, #fdf9f3 0%, #f2e7d9 100%);
  box-shadow:
    0 28px 60px -46px rgba(49, 43, 35, 0.28),
    inset 0 -1px 0 rgba(194, 177, 154, 0.18),
    inset 0 1px 0 rgba(255, 255, 255, 0.72);
}

.assessment-run-hero::before {
  content: "";
  position: absolute;
  inset: auto -4.5rem -4rem auto;
  width: 13rem;
  height: 13rem;
  border-radius: 999px;
  background: radial-gradient(circle, rgba(141, 157, 136, 0.18), rgba(141, 157, 136, 0));
  pointer-events: none;
}

.assessment-run-hero__content {
  position: relative;
  z-index: 1;
}

.assessment-run-hero__intro {
  display: grid;
  gap: 1.5rem;
}

.assessment-run-hero__identity {
  max-width: 42rem;
}

.assessment-run-hero__title-group {
  display: grid;
  gap: 0.95rem;
}

.assessment-run-hero__intro h1 {
  margin: 0;
  max-width: 24ch;
  font-family: Georgia, "Times New Roman", Times, serif;
  font-size: clamp(1.65rem, 2.3vw, 2.1rem);
  font-weight: 520;
  line-height: 1.14;
  letter-spacing: 0.01em;
  color: #25211d;
}

.assessment-run-hero__participant {
  margin: 0;
  font-size: 1.02rem;
  font-weight: 500;
  line-height: 1.45;
  letter-spacing: 0.01em;
  color: #606759;
}

.assessment-run-shell {
  border: 1px solid #d2c4b3;
  border-radius: 34px;
  padding: clamp(1.1rem, 2.2vw, 2.15rem);
  background:
    linear-gradient(180deg, #f8f0e4 0%, #f0e6d8 100%);
  box-shadow:
    0 34px 78px -58px rgba(49, 43, 35, 0.32),
    inset 0 -1px 0 rgba(196, 180, 158, 0.14),
    inset 0 1px 0 rgba(255, 255, 255, 0.8);
}

.assessment-run {
  max-width: 70rem;
  margin: 0 auto;
}

.assessment-progress {
  display: grid;
  gap: 0.95rem;
  align-self: stretch;
  min-width: 0;
  padding: 1.3rem 1.35rem 1.15rem;
  border: 1px solid rgba(188, 170, 147, 0.72);
  border-radius: 26px;
  background:
    linear-gradient(180deg, rgba(255, 250, 243, 0.82) 0%, rgba(246, 236, 223, 0.96) 100%);
  box-shadow:
    inset 0 1px 0 rgba(255, 255, 255, 0.72),
    inset 0 -1px 0 rgba(188, 170, 147, 0.12),
    0 24px 44px -38px rgba(63, 54, 43, 0.28);
}

.assessment-progress__metric-value,
.assessment-progress__caption,
.assessment-progress-note,
.assessment-step-card__kicker {
  margin: 0;
  color: var(--text-muted);
}

.assessment-progress__summary {
  display: grid;
  grid-template-columns: minmax(0, 1fr);
  gap: 0.75rem;
}

.assessment-progress__metric {
  display: grid;
  min-width: 0;
  gap: 0.35rem;
}

.assessment-progress__metric-label {
  display: block;
  font-size: 0.68rem;
  font-weight: 650;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  color: #7f7c73;
}

.assessment-progress__metric-value {
  margin: 0;
  min-width: 0;
  font-size: 1.08rem;
  font-weight: 600;
  line-height: 1.25;
  letter-spacing: 0.005em;
  color: #31362f;
}

.assessment-progress__bar-region {
  display: grid;
  gap: 0.35rem;
  padding: 0;
}

.assessment-progress__track {
  width: 100%;
  height: 0.8rem;
  overflow: hidden;
  border-radius: 999px;
  background: linear-gradient(180deg, #d7cfc1 0%, #e0d8cb 100%);
  box-shadow:
    inset 0 1px 2px rgba(96, 79, 60, 0.1),
    inset 0 0 0 1px rgba(255, 255, 255, 0.24);
}

.assessment-progress__fill {
  height: 100%;
  border-radius: inherit;
  background: linear-gradient(90deg, #66775f 0%, #8b6958 100%);
  box-shadow:
    inset 0 1px 0 rgba(255, 255, 255, 0.22),
    0 8px 18px -14px rgba(85, 73, 58, 0.42);
}

.assessment-progress__caption {
  font-size: 0.75rem;
  letter-spacing: 0.01em;
  color: #757569;
}

.assessment-step-card {
  border: 1px solid #d4c6b4;
  border-radius: 30px;
  padding: clamp(1.8rem, 3vw, 2.6rem);
  background:
    linear-gradient(180deg, #fffdf9 0%, #f7efe2 100%);
  box-shadow:
    0 36px 76px -60px rgba(49, 43, 35, 0.3),
    inset 0 -1px 0 rgba(196, 180, 158, 0.14),
    inset 0 1px 0 rgba(255, 255, 255, 0.82);
}

.assessment-step-card__header {
  display: flex;
  flex-direction: column;
  margin-bottom: 2.4rem;
}

.assessment-step-card__question-region {
  display: flex;
  flex-direction: column;
  justify-content: flex-start;
  gap: 1.1rem;
  max-width: 54rem;
  padding: 0.15rem 0 0.35rem;
}

.assessment-step-card__question-region--stable {
  min-height: clamp(7.75rem, 12vw, 9.5rem);
}

.assessment-step-card__header h3 {
  margin: 0;
  font-size: clamp(1.85rem, 3.3vw, 2.6rem);
  font-weight: 650;
  line-height: 1.2;
  letter-spacing: -0.035em;
  text-wrap: balance;
  color: #1f211f;
}

.assessment-step-card__kicker {
  font-size: 0.74rem;
  font-weight: 700;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  color: #7a8274;
}

.assessment-step-card__fieldset {
  margin: 0;
  padding: 0;
  border: 0;
}

.assessment-options {
  display: grid;
  gap: 0.75rem;
  padding: 0;
  margin: 0;
  list-style: none;
}

.assessment-likert {
  max-width: 54rem;
}

.assessment-likert__scale {
  display: grid;
  gap: 1rem;
  padding: 1.25rem 1.3rem 1.3rem;
  border: 1px solid #d6c8b8;
  border-radius: 26px;
  background:
    linear-gradient(180deg, #f8f0e4 0%, #eee1d1 100%);
  box-shadow:
    inset 0 1px 0 rgba(255, 255, 255, 0.76),
    inset 0 0 0 1px rgba(255, 255, 255, 0.2);
}

.assessment-likert__labels {
  display: flex;
  justify-content: space-between;
  gap: 1.75rem;
  padding: 0 0.2rem;
  font-size: 0.8rem;
  font-weight: 650;
  letter-spacing: 0.01em;
  line-height: 1.45;
  color: #5d6559;
}

.assessment-likert__labels span:last-child {
  text-align: right;
}

.assessment-likert__options {
  display: grid;
  grid-template-columns: repeat(5, minmax(0, 1fr));
  gap: 0.8rem;
  padding: 0;
  margin: 0;
  list-style: none;
}

.assessment-likert-option {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-height: 5rem;
  padding: 0.95rem 0.7rem 0.85rem;
  border: 1px solid #cbbda9;
  border-radius: 20px;
  background: linear-gradient(180deg, #fffaf2 0%, #f1e5d5 100%);
  box-shadow:
    inset 0 1px 0 rgba(255, 255, 255, 0.82),
    0 18px 28px -30px rgba(60, 52, 42, 0.28);
  color: #2f342d;
  cursor: pointer;
  transition:
    transform 120ms ease,
    border-color 120ms ease,
    background-color 120ms ease,
    box-shadow 120ms ease,
    color 120ms ease;
}

.assessment-likert-option:hover {
  transform: translateY(-1px);
  border-color: #a4aa95;
  box-shadow:
    inset 0 1px 0 rgba(255, 255, 255, 0.84),
    0 22px 34px -28px rgba(49, 43, 35, 0.26);
}

.assessment-likert-option:focus-within {
  outline: 2px solid rgba(104, 120, 99, 0.2);
  outline-offset: 2px;
}

.assessment-likert-option input {
  position: absolute;
  inset: 0;
  margin: 0;
  opacity: 0;
  cursor: pointer;
}

.assessment-likert-option--selected {
  transform: translateY(-1px);
  border-color: #6c7967;
  background: linear-gradient(180deg, #6d7b67 0%, #596757 100%);
  box-shadow:
    inset 0 1px 0 rgba(255, 255, 255, 0.16),
    0 24px 36px -24px rgba(73, 84, 68, 0.46);
  color: #fff;
}

.assessment-likert-option__value {
  font-size: 1.02rem;
  font-weight: 700;
  line-height: 1;
}

.assessment-option {
  position: relative;
  display: grid;
  grid-template-columns: auto auto 1fr;
  align-items: center;
  gap: 0.875rem;
  padding: 1rem;
  border: 1px solid var(--border);
  border-radius: 16px;
  background: var(--surface-muted);
  cursor: pointer;
  transition: border-color 120ms ease, background-color 120ms ease, box-shadow 120ms ease;
}

.assessment-option:hover {
  border-color: rgba(31, 58, 95, 0.28);
}

.assessment-option input {
  width: 1.1rem;
  height: 1.1rem;
  margin: 0;
}

.assessment-option input:checked + .assessment-option__marker {
  background: var(--accent);
  color: #fff;
  border-color: var(--accent);
}

.assessment-option__marker {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-width: 2rem;
  min-height: 2rem;
  border: 1px solid var(--border-strong);
  border-radius: 999px;
  background: #fff;
  color: var(--accent);
  font-size: 0.875rem;
  font-weight: 700;
}

.assessment-option__label {
  font-weight: 600;
  color: var(--text);
}

.assessment-textarea {
  min-height: 8rem;
  resize: vertical;
}

.assessment-inline-message {
  margin: 1.15rem 0 0;
  padding: 0.9rem 1rem;
  border-radius: 16px;
  font-size: 0.92rem;
}

.assessment-inline-message--success {
  background: var(--success-bg);
  color: var(--success-text);
}

.assessment-inline-message--error {
  background: var(--danger-bg);
  color: var(--danger-text);
}

.assessment-step-layout {
  display: grid;
  gap: 1rem;
}

.assessment-step-layout__footer {
  display: grid;
  gap: 1rem;
}

.assessment-step-actions {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 0.9rem;
}

.assessment-step-actions--compact {
  grid-template-columns: repeat(2, minmax(0, 1fr));
}

.assessment-step-actions button {
  min-height: 3.55rem;
  font-weight: 650;
}

.assessment-step-actions__button {
  padding: 0.9rem 1.2rem;
  border-radius: 18px;
  border-width: 1px;
  line-height: 1.2;
  letter-spacing: -0.01em;
  transition:
    transform 120ms ease,
    box-shadow 120ms ease,
    border-color 120ms ease,
    background-color 120ms ease,
    color 120ms ease;
}

.assessment-step-actions__button:hover:not(:disabled) {
  transform: translateY(-1px);
}

.assessment-step-actions__button--ghost {
  border-color: #d3c6b4;
  background: linear-gradient(180deg, rgba(249, 242, 232, 0.82) 0%, rgba(239, 228, 211, 0.92) 100%);
  box-shadow:
    inset 0 1px 0 rgba(255, 255, 255, 0.76),
    0 14px 24px -26px rgba(71, 61, 49, 0.18);
  color: #4b4f48;
}

.assessment-step-actions__button--save {
  border-color: #baa992;
  background: linear-gradient(180deg, #f6ebdd 0%, #eadbc8 100%);
  box-shadow:
    inset 0 1px 0 rgba(255, 255, 255, 0.62),
    0 18px 30px -24px rgba(90, 72, 55, 0.28);
  color: #2f312d;
}

.assessment-step-actions__button--primary {
  border-color: #667460;
  background: linear-gradient(180deg, #6d7c67 0%, #586856 100%);
  box-shadow:
    inset 0 1px 0 rgba(255, 255, 255, 0.14),
    0 18px 30px -22px rgba(54, 64, 51, 0.46);
}

.assessment-step-actions__button--primary:hover:not(:disabled) {
  box-shadow: 0 18px 32px -24px rgba(54, 64, 51, 0.62);
}

.assessment-step-actions__button--ghost:hover:not(:disabled) {
  border-color: #c7b7a2;
  background: linear-gradient(180deg, rgba(250, 244, 235, 0.92) 0%, rgba(241, 231, 216, 0.98) 100%);
  box-shadow: 0 18px 30px -26px rgba(62, 53, 43, 0.2);
}

.assessment-step-actions__button--save:hover:not(:disabled) {
  border-color: #a8937a;
  background: linear-gradient(180deg, #f4e6d6 0%, #e5d3bf 100%);
  box-shadow: 0 20px 32px -24px rgba(78, 63, 49, 0.3);
}

.assessment-step-actions__button--ghost:focus-visible,
.assessment-step-actions__button--save:focus-visible,
.assessment-step-actions__button--primary:focus-visible {
  outline: 2px solid rgba(104, 120, 99, 0.22);
  outline-offset: 2px;
}

.assessment-step-actions__button--ghost:disabled,
.assessment-step-actions__button--save:disabled,
.assessment-step-actions__button--primary:disabled {
  box-shadow: none;
}

.assessment-completion-state {
  max-width: 40rem;
  margin: 0 auto;
  padding: clamp(1.75rem, 4vw, 2.75rem);
  border: 1px solid rgba(31, 58, 95, 0.1);
  border-radius: 28px;
  background:
    radial-gradient(circle at top, rgba(80, 115, 160, 0.1), transparent 48%),
    linear-gradient(180deg, rgba(255, 255, 255, 0.98), rgba(244, 247, 251, 0.98));
  box-shadow:
    0 32px 80px -56px rgba(15, 23, 42, 0.45),
    inset 0 1px 0 rgba(255, 255, 255, 0.8);
  text-align: center;
}

.assessment-completion-state__hero h2 {
  margin: 0;
  font-size: clamp(2rem, 4vw, 2.9rem);
  line-height: 1.08;
  letter-spacing: -0.03em;
}

.assessment-completion-state__description,
.assessment-completion-state__status {
  margin: 0;
  color: var(--text-muted);
}

.assessment-completion-state__indicator {
  display: grid;
  justify-items: center;
  gap: 1rem;
  margin: 2rem 0 1.25rem;
}

.assessment-completion-state__indicator-orbit {
  position: relative;
  display: grid;
  place-items: center;
  width: 4.5rem;
  aspect-ratio: 1;
  border-radius: 999px;
  background: radial-gradient(circle, rgba(255, 255, 255, 0.96), rgba(234, 240, 247, 0.72));
  box-shadow:
    inset 0 0 0 1px rgba(41, 71, 111, 0.08),
    0 18px 40px -30px rgba(41, 71, 111, 0.45);
}

.assessment-completion-state__indicator-orbit::before {
  content: "";
  position: absolute;
  inset: 0;
  border-radius: inherit;
  border: 1px solid rgba(41, 71, 111, 0.12);
}

.assessment-completion-state__indicator-orbit::after {
  content: "";
  position: absolute;
  inset: 0.3rem;
  border-radius: inherit;
  border: 2px solid rgba(41, 71, 111, 0.12);
  border-top-color: #29476f;
  border-right-color: rgba(80, 115, 160, 0.65);
  animation: assessment-completion-rotate 1100ms linear infinite;
}

.assessment-completion-state__indicator-core {
  width: 1rem;
  height: 1rem;
  border-radius: 999px;
  background: linear-gradient(180deg, #29476f 0%, #6b8bb3 100%);
  box-shadow: 0 0 0 0 rgba(41, 71, 111, 0.18);
  animation: assessment-completion-pulse 1800ms ease-out infinite;
}

.assessment-completion-state__indicator-bar {
  position: relative;
  display: block;
  width: min(100%, 18rem);
  height: 0.4rem;
  overflow: hidden;
  border-radius: 999px;
  background: linear-gradient(90deg, rgba(31, 58, 95, 0.08), rgba(31, 58, 95, 0.16));
}

.assessment-completion-state__indicator-bar::after {
  content: "";
  position: absolute;
  inset: 0;
  width: 34%;
  border-radius: inherit;
  background: linear-gradient(90deg, rgba(41, 71, 111, 0) 0%, #29476f 48%, #6b8bb3 100%);
  animation: assessment-completion-slide 1600ms ease-in-out infinite;
}

.assessment-completion-state__status {
  font-size: 0.95rem;
}

.attempt-results-page {
  max-width: 46rem;
  width: 100%;
  margin: 0 auto;
}

.results-report__hero-meta {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 0.5rem;
  margin: 0;
}

.results-report__hero-meta-item {
  padding: 0.65rem 0.8rem 0.7rem;
  border: 1px solid rgba(181, 162, 136, 0.45);
  border-radius: 16px;
  background: rgba(255, 249, 242, 0.78);
}

.results-report__hero-meta-item--wide {
  grid-column: 1 / -1;
}

.results-report__hero-meta dt {
  font-size: 0.62rem;
  font-weight: 700;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  color: #7c6f61;
}

.results-report__hero-meta dd {
  margin: 0.28rem 0 0;
  font-size: 0.9rem;
  font-weight: 600;
  line-height: 1.35;
  color: #292c28;
}

.attempt-results-page__content {
  display: block;
}

.results-report {
  display: grid;
  gap: 0.78rem;
}

.results-report__hero {
  display: grid;
  gap: 0.9rem;
  padding: clamp(1.05rem, 2.4vw, 1.45rem);
  border: 1px solid rgba(206, 189, 167, 0.86);
  border-radius: 28px;
  background:
    radial-gradient(circle at top right, rgba(142, 105, 83, 0.12), transparent 26%),
    linear-gradient(180deg, rgba(255, 250, 243, 0.98) 0%, rgba(243, 233, 218, 0.98) 100%);
  box-shadow:
    0 22px 42px -34px rgba(70, 56, 40, 0.22),
    inset 0 1px 0 rgba(255, 255, 255, 0.72);
}

.results-report__hero-copy {
  display: grid;
  gap: 0.58rem;
}

.results-report__hero-meta-wrap {
  display: block;
}

.results-report__eyebrow,
.results-report__section-kicker {
  margin: 0;
  font-size: 0.65rem;
  font-weight: 700;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  color: #7d6957;
}

.results-report__hero h2,
.results-report__section-heading h3,
.results-panel__header h4,
.results-dimension-card__header h4 {
  margin: 0;
  letter-spacing: -0.03em;
}

.results-report__hero h2 {
  max-width: 18ch;
  font-size: clamp(1.52rem, 5.8vw, 2.2rem);
  line-height: 1.08;
  text-wrap: balance;
  overflow-wrap: normal;
}

.results-report__intro,
.results-report__section-intro,
.results-report__section-note,
.results-report__section-body,
.results-report__disclaimer,
.results-dimension-card__header p,
.results-dimension-card__summary,
.results-dimension-card__details p {
  margin: 0;
  color: var(--text-muted);
  line-height: 1.58;
}

.results-report__intro {
  max-width: 28rem;
  font-size: 0.9rem;
  color: #4d4f49;
}

.results-report__section-intro {
  max-width: 36rem;
  font-size: 1rem;
  font-weight: 560;
  line-height: 1.56;
  letter-spacing: 0.005em;
  color: #2f342d;
}

.results-report__hero-insights {
  display: grid;
  gap: 0.55rem;
  padding-top: 0;
}

.results-report__section--insights {
  display: none;
}

.results-report__hero-label {
  margin: 0;
  font-size: 0.68rem;
  font-weight: 700;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  color: #5f7059;
}

.results-report__status-pill {
  display: inline-flex;
  align-items: center;
  padding: 0.3rem 0.65rem;
  border-radius: 999px;
  background: var(--success-bg);
  color: var(--success-text);
  font-size: 0.82rem;
  font-weight: 700;
}

.results-report__section {
  display: grid;
  gap: 0.58rem;
}

.results-report__closing {
  display: grid;
  gap: 0.78rem;
}

.results-report__panel {
  padding: 0.88rem 0.92rem;
  border-color: rgba(206, 189, 167, 0.84);
  border-radius: 22px;
  background: linear-gradient(180deg, rgba(255, 253, 249, 0.98) 0%, rgba(247, 239, 226, 0.98) 100%);
  box-shadow:
    0 20px 34px -30px rgba(69, 57, 43, 0.18),
    inset 0 1px 0 rgba(255, 255, 255, 0.8);
}

.results-report__section-heading {
  display: grid;
  gap: 0.42rem;
  padding: 0;
}

.results-report__section-heading > .results-report__section-intro {
  margin-bottom: 0.7rem;
}

.results-report__section-heading h3 {
  font-size: 1rem;
  line-height: 1.2;
}

.results-dimension-list,
.results-score-overview,
.results-insight-list,
.results-inline-list,
.results-bullet-list {
  padding: 0;
  margin: 0;
  list-style: none;
}

.results-insight-list {
  display: grid;
  gap: 0.45rem;
}

.results-insight-list li {
  position: relative;
  padding-left: 0.9rem;
  color: #2f342d;
  font-size: 0.88rem;
  font-weight: 600;
  line-height: 1.38;
}

.results-insight-list li::before {
  content: "";
  position: absolute;
  top: 0.4rem;
  left: 0;
  width: 0.38rem;
  height: 0.38rem;
  border-radius: 999px;
  background: #5f7059;
}

.results-score-overview {
  display: grid;
  gap: 0.72rem;
}

.results-score-overview__item {
  display: grid;
  gap: 0.38rem;
}

.results-score-overview__header {
  display: flex;
  align-items: baseline;
  justify-content: space-between;
  gap: 0.75rem;
}

.results-score-overview__header strong {
  font-size: 0.93rem;
}

.results-score-overview__header span {
  flex-shrink: 0;
  font-size: 0.8rem;
  font-weight: 700;
  color: #6e5f4e;
}

.results-score-overview__bar {
  height: 0.72rem;
  overflow: hidden;
  border-radius: 999px;
  background: linear-gradient(180deg, #ddd2c4 0%, #e8dfd3 100%);
}

.results-score-overview__bar span {
  display: block;
  height: 100%;
  border-radius: inherit;
  background: linear-gradient(90deg, #5f7059 0%, #866454 100%);
}

.results-dimension-list {
  display: grid;
  gap: 0.72rem;
}

.results-dimension-card {
  display: grid;
  gap: 0.52rem;
  padding: 0.8rem 0.9rem;
  border: 1px solid rgba(205, 189, 168, 0.8);
  border-radius: 20px;
  background:
    linear-gradient(180deg, rgba(255, 254, 252, 0.98), rgba(247, 239, 226, 0.96)),
    linear-gradient(135deg, rgba(95, 112, 89, 0.05), transparent 55%);
  box-shadow:
    0 18px 30px -28px rgba(69, 57, 43, 0.16),
    inset 0 1px 0 rgba(255, 255, 255, 0.82);
}

.results-dimension-card__header {
  display: flex;
  align-items: baseline;
  justify-content: space-between;
  gap: 0.65rem;
}

.results-dimension-card__title {
  display: grid;
  gap: 0.18rem;
}

.results-dimension-card__header h4 {
  font-size: 0.96rem;
  line-height: 1.22;
}

.results-dimension-card__helper {
  font-size: 0.8rem;
  line-height: 1.35;
  color: #6f6255;
}

.results-dimension-card__score {
  text-align: right;
}

.results-dimension-card__score span {
  display: block;
  font-size: 0.88rem;
  font-weight: 700;
  line-height: 1.2;
  color: #5f7059;
  white-space: nowrap;
}

.results-dimension-card__score-value {
  display: block;
}

.results-dimension-card__summary {
  font-size: 0.88rem;
  color: #4c4d46;
  line-height: 1.42;
}

.results-dimension-card__footer {
  display: flex;
  justify-content: flex-start;
}

.results-dimension-card__toggle {
  min-width: 5.5rem;
  min-height: 2.45rem;
  padding: 0.58rem 0.92rem;
  border: 1px solid rgba(95, 112, 89, 0.2);
  border-radius: 999px;
  background: linear-gradient(180deg, #5f7059 0%, #556350 100%);
  color: #fffaf2;
  font: inherit;
  font-size: 0.88rem;
  font-weight: 700;
  letter-spacing: 0.01em;
  cursor: pointer;
  box-shadow: 0 12px 24px -18px rgba(79, 62, 45, 0.48);
}

.results-dimension-card__toggle:hover {
  background: linear-gradient(180deg, #667760 0%, #596953 100%);
}

.results-dimension-card__toggle[aria-expanded="true"] {
  min-width: 4.8rem;
  min-height: 2.15rem;
  padding: 0.46rem 0.82rem;
  border-color: rgba(95, 112, 89, 0.26);
  background: rgba(255, 250, 243, 0.92);
  color: #44513f;
  font-size: 0.84rem;
  font-weight: 600;
  box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.82);
}

.results-dimension-card__toggle:focus-visible {
  outline: 3px solid rgba(95, 112, 89, 0.24);
  outline-offset: 2px;
}

.results-dimension-card__toggle-label-desktop {
  display: none;
}

.results-dimension-card__details {
  display: grid;
  gap: 0.95rem;
  padding-top: 0.9rem;
  border-top: 1px solid rgba(181, 162, 136, 0.32);
}

.results-dimension-card__detail-block {
  display: grid;
  gap: 0.34rem;
}

.results-dimension-card__detail-block h5 {
  margin: 0;
  font-size: 0.8rem;
  line-height: 1.32;
  font-weight: 800;
  letter-spacing: 0.01em;
  color: #4f6048;
}

.results-dimension-card__detail-block p {
  margin: 0;
  font-size: 0.88rem;
  line-height: 1.52;
  color: #4c4d46;
}

.results-report-grid {
  display: grid;
  gap: 1rem;
  grid-template-columns: repeat(1, minmax(0, 1fr));
}

.results-panel {
  padding: clamp(1rem, 2vw, 1.4rem);
  border-radius: 22px;
}

.results-panel--wide {
  grid-column: 1 / -1;
}

.results-panel__header {
  display: grid;
  gap: 0.35rem;
}

.results-panel__lead {
  margin: 0;
  font-size: 1.02rem;
  line-height: 1.8;
}

.results-bullet-list {
  display: grid;
  gap: 0.72rem;
}

.results-bullet-list li,
.results-inline-list li {
  position: relative;
  padding-left: 1rem;
  color: var(--text);
  line-height: 1.55;
}

.results-bullet-list li strong {
  color: #2f342d;
}

.results-bullet-list li::before,
.results-inline-list li::before {
  content: "";
  position: absolute;
  top: 0.72rem;
  left: 0;
  width: 0.38rem;
  height: 0.38rem;
  border-radius: 999px;
  background: #6a86a8;
}

.results-report__disclaimer {
  font-size: 0.72rem;
  line-height: 1.5;
  color: rgba(77, 79, 73, 0.72);
}

@keyframes assessment-completion-slide {
  0% {
    transform: translateX(-120%);
  }

  50% {
    transform: translateX(95%);
  }

  100% {
    transform: translateX(240%);
  }
}

@keyframes assessment-completion-rotate {
  to {
    transform: rotate(360deg);
  }
}

@keyframes assessment-completion-pulse {
  0%,
  100% {
    transform: scale(1);
    box-shadow: 0 0 0 0 rgba(41, 71, 111, 0.16);
  }

  55% {
    transform: scale(1.08);
    box-shadow: 0 0 0 0.75rem rgba(41, 71, 111, 0);
  }
}

@media (min-width: 768px) {
  .attempt-results-page {
    max-width: 66rem;
  }

  .attempt-results-page__content {
    width: 100%;
  }

  .results-report {
    gap: 1.75rem;
  }

  .results-report__hero {
    grid-template-columns: minmax(0, 1fr);
    gap: 1.55rem;
    padding: 2.15rem 2rem;
    border-radius: 32px;
    border-color: rgba(201, 184, 162, 0.92);
    background:
      radial-gradient(circle at top right, rgba(142, 105, 83, 0.14), transparent 28%),
      radial-gradient(circle at left center, rgba(95, 112, 89, 0.07), transparent 30%),
      linear-gradient(180deg, rgba(255, 250, 244, 0.99) 0%, rgba(243, 233, 219, 0.99) 100%);
    box-shadow:
      0 30px 62px -44px rgba(70, 56, 40, 0.24),
      inset 0 1px 0 rgba(255, 255, 255, 0.78);
  }

  .results-report__hero h2 {
    max-width: 15ch;
    font-size: clamp(2.45rem, 4.4vw, 3.55rem);
    line-height: 0.95;
    letter-spacing: -0.05em;
    color: #211d19;
  }

  .results-report__hero-copy {
    gap: 1.2rem;
    max-width: 50rem;
  }

  .results-report__eyebrow {
    font-size: 0.74rem;
    font-weight: 800;
    letter-spacing: 0.2em;
    color: #79604b;
  }

  .results-report__hero-meta-wrap {
    max-width: 44rem;
  }

  .results-report__hero-meta {
    grid-template-columns: repeat(3, minmax(0, 13rem));
    justify-content: start;
    gap: 0.9rem;
  }

  .results-report__hero-meta-item {
    min-height: 5.75rem;
    padding: 1rem 1.05rem 1.02rem;
    border-radius: 22px;
    border-color: rgba(186, 168, 143, 0.4);
    background:
      linear-gradient(180deg, rgba(255, 251, 246, 0.9) 0%, rgba(251, 245, 237, 0.78) 100%);
    backdrop-filter: blur(6px);
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.82),
      0 18px 32px -30px rgba(76, 59, 41, 0.12);
  }

  .results-report__hero-meta-item--wide {
    grid-column: auto;
  }

  .results-report__hero-meta dt {
    font-size: 0.69rem;
    letter-spacing: 0.14em;
    color: #816d5b;
  }

  .results-report__hero-meta dd {
    margin-top: 0.44rem;
    font-size: 1.03rem;
    line-height: 1.42;
    color: #242722;
  }

  .results-report__hero-insights {
    gap: 0;
    padding: 0;
    border-left: 0;
    background: none;
    border-radius: 0;
    box-shadow: none;
  }

  .results-report__hero-label {
    font-size: 0.72rem;
    letter-spacing: 0.18em;
  }

  .results-report__hero-insights--mobile {
    display: none;
  }

  .results-report__section--insights {
    display: grid;
    gap: 1.15rem;
    max-width: 48rem;
    width: 100%;
    margin: 0 auto;
    background:
      linear-gradient(180deg, rgba(255, 252, 248, 0.99) 0%, rgba(246, 238, 227, 0.97) 100%),
      linear-gradient(90deg, rgba(95, 112, 89, 0.08), transparent 46%);
    border-color: rgba(193, 177, 152, 0.88);
    box-shadow:
      0 28px 48px -40px rgba(74, 58, 41, 0.16),
      inset 0 1px 0 rgba(255, 255, 255, 0.84);
  }

  .results-report__section--insights .results-report__section-heading {
    gap: 0.42rem;
    padding-bottom: 0.4rem;
    border-bottom: 1px solid rgba(185, 168, 144, 0.24);
  }

  .results-report__section--insights .results-report__section-kicker {
    color: #5b6a54;
  }

  .results-report__section--insights .results-report__section-heading h3 {
    max-width: 24ch;
    font-size: 1.4rem;
    line-height: 1.12;
  }

  .results-insight-list {
    gap: 1.05rem;
  }

  .results-insight-list li {
    padding-left: 1.35rem;
    font-size: 1.02rem;
    line-height: 1.72;
    font-weight: 600;
    color: #2c312b;
  }

  .results-insight-list li::before {
    top: 0.7rem;
    width: 0.46rem;
    height: 0.46rem;
    background: linear-gradient(180deg, #687a62 0%, #55664f 100%);
  }

  .results-report__panel {
    padding: 1.55rem 1.6rem;
    border-radius: 28px;
  }

  .results-report__section {
    gap: 1.1rem;
  }

  .results-report__section-heading {
    gap: 0.6rem;
    align-items: start;
  }

  .results-report__section-kicker {
    font-size: 0.72rem;
    font-weight: 800;
    letter-spacing: 0.18em;
  }

  .results-report__section-heading h3 {
    font-size: 1.36rem;
    line-height: 1.06;
    letter-spacing: -0.035em;
    color: #221e19;
  }

  .results-report__section-note {
    font-size: 0.95rem;
    font-weight: 560;
    line-height: 1.55;
    color: #5f635c;
  }

  .results-report__section--overview {
    max-width: 52rem;
    width: 100%;
    margin: 0 auto;
    background:
      linear-gradient(180deg, rgba(255, 253, 249, 0.99) 0%, rgba(248, 241, 231, 0.97) 100%),
      linear-gradient(90deg, rgba(95, 112, 89, 0.07), transparent 42%);
    border-color: rgba(196, 178, 153, 0.84);
  }

  .results-report__section--dimensions {
    gap: 1.2rem;
    max-width: 52rem;
    width: 100%;
    margin: 0 auto;
  }

  .results-report__closing {
    gap: 1.3rem;
  }

  .assessment-run-hero {
    padding: clamp(2rem, 2.6vw, 2.7rem);
  }

  .assessment-run-hero__intro {
    grid-template-columns: minmax(0, 1.34fr) minmax(24rem, 1fr);
    align-items: stretch;
    gap: 1.75rem;
  }

  .assessment-progress__metric-value {
    white-space: nowrap;
  }

  .attempt-results-page__summary {
    grid-template-columns: minmax(0, 1.4fr) minmax(16rem, 1fr);
    align-items: end;
  }

  .results-score-overview {
    grid-template-columns: minmax(0, 1fr);
    row-gap: 1rem;
  }

  .results-score-overview__item {
    gap: 0.78rem;
    padding: 1.05rem 0.95rem 1.12rem;
    border-top: 1px solid rgba(206, 189, 167, 0.5);
  }

  .results-score-overview__item:first-child {
    padding-top: 0.1rem;
    border-top: 0;
  }

  .results-score-overview__header strong {
    font-size: 1.05rem;
    line-height: 1.32;
    letter-spacing: -0.015em;
    color: #26231f;
  }

  .results-score-overview__header span {
    font-size: 0.95rem;
    letter-spacing: -0.01em;
    color: #6a5848;
  }

  .results-score-overview__bar {
    height: 0.9rem;
    box-shadow:
      inset 0 1px 2px rgba(96, 79, 60, 0.08),
      inset 0 0 0 1px rgba(255, 255, 255, 0.22);
  }

  .results-dimension-list {
    gap: 1.2rem;
    grid-template-columns: minmax(0, 1fr);
  }

  .results-dimension-card {
    gap: 1.2rem;
    padding: 1.65rem 1.55rem;
    border-radius: 30px;
    border-color: rgba(199, 182, 158, 0.84);
    background:
      linear-gradient(180deg, rgba(255, 254, 252, 0.99), rgba(249, 243, 235, 0.97)),
      linear-gradient(135deg, rgba(95, 112, 89, 0.06), transparent 62%);
    box-shadow:
      0 26px 48px -38px rgba(69, 57, 43, 0.18),
      inset 0 1px 0 rgba(255, 255, 255, 0.84),
      inset 0 -1px 0 rgba(186, 169, 145, 0.12);
  }

  .results-dimension-card__header {
    gap: 1rem;
    align-items: flex-start;
  }

  .results-dimension-card__title {
    gap: 0.32rem;
  }

  .results-dimension-card__header h4 {
    font-size: 1.24rem;
    line-height: 1.06;
    letter-spacing: -0.03em;
    color: #22201c;
  }

  .results-dimension-card__helper {
    font-size: 0.92rem;
    font-weight: 560;
    line-height: 1.48;
    color: #655747;
  }

  .results-dimension-card__score {
    min-width: 8.4rem;
    padding: 0.15rem 0 0 1rem;
    position: relative;
  }

  .results-dimension-card__score span {
    font-size: 1.22rem;
    line-height: 1.02;
    font-weight: 800;
    letter-spacing: -0.02em;
    color: #53644c;
  }

  .results-dimension-card__summary {
    max-width: 58ch;
    font-size: 1.04rem;
    font-weight: 500;
    line-height: 1.8;
    color: #373b35;
  }

  .results-dimension-card__footer {
    padding-top: 0.1rem;
  }

  .results-dimension-card__toggle {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-width: 10.75rem;
    min-height: 2.9rem;
    padding: 0.72rem 1.15rem;
    border-color: rgba(87, 102, 80, 0.2);
    background: linear-gradient(180deg, #5b6b55 0%, #4f5e4a 100%);
    font-size: 0.9rem;
    font-weight: 750;
    letter-spacing: 0.01em;
    box-shadow:
      0 16px 26px -20px rgba(79, 62, 45, 0.42),
      inset 0 1px 0 rgba(255, 255, 255, 0.14);
    transition:
      transform 140ms ease,
      box-shadow 140ms ease,
      background-color 140ms ease,
      border-color 140ms ease,
      color 140ms ease;
  }

  .results-dimension-card__toggle:hover {
    background: linear-gradient(180deg, #64745e 0%, #566652 100%);
    box-shadow:
      0 20px 32px -24px rgba(79, 62, 45, 0.48),
      inset 0 1px 0 rgba(255, 255, 255, 0.16);
    transform: translateY(-1px);
  }

  .results-dimension-card__toggle[aria-expanded="true"] {
    min-width: 10rem;
    min-height: 2.9rem;
    padding: 0.72rem 1.15rem;
    border-color: rgba(95, 112, 89, 0.24);
    background: rgba(255, 251, 245, 0.96);
    color: #42503d;
    font-size: 0.9rem;
    font-weight: 700;
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.9),
      0 12px 24px -24px rgba(74, 58, 41, 0.2);
  }

  .results-dimension-card__toggle-label-mobile {
    display: none;
  }

  .results-dimension-card__toggle-label-desktop {
    display: inline;
  }

  .results-dimension-card__details {
    gap: 1.1rem;
    max-width: 44rem;
    padding-top: 1.2rem;
    border-top-color: rgba(184, 166, 142, 0.36);
  }

  .results-dimension-card__detail-block h5 {
    font-size: 0.92rem;
    font-weight: 800;
    letter-spacing: 0.015em;
    color: #4d5e46;
  }

  .results-dimension-card__detail-block p {
    font-size: 0.98rem;
    line-height: 1.78;
    color: #424640;
  }

  .results-report-grid {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }

  .assessment-step-card {
    padding: 2.75rem;
  }

  .assessment-step-card__fieldset {
    display: grid;
    gap: 1rem;
  }

  .assessment-step-actions {
    grid-template-columns: auto auto minmax(12rem, 1fr);
    justify-content: end;
    align-items: center;
  }

  .assessment-step-actions--compact {
    grid-template-columns: auto auto;
    justify-content: end;
  }

  .assessment-step-actions button {
    min-width: 9.75rem;
  }
}

@media (min-width: 1024px) {
  .attempt-results-page {
    max-width: 69rem;
  }

  .results-report {
    gap: 1.75rem;
  }

  .results-report__hero {
    grid-template-columns: minmax(0, 1fr);
    gap: 1.7rem;
    padding: 2.5rem 2.4rem;
    border-radius: 36px;
  }

  .results-report__hero-copy {
    gap: 1.3rem;
    max-width: 52rem;
  }

  .results-report__hero h2 {
    max-width: 16ch;
    font-size: clamp(3rem, 4vw, 4.1rem);
    line-height: 0.92;
  }

  .results-report__hero-meta {
    grid-template-columns: repeat(3, minmax(12.25rem, 13.5rem));
    justify-content: start;
    gap: 1rem;
  }

  .results-report__hero-meta-item {
    min-height: 6.15rem;
    padding: 1.12rem 1.1rem 1.02rem;
    border-radius: 22px;
  }

  .results-report__hero-label {
    font-size: 0.74rem;
  }

  .results-insight-list li {
    font-size: 1.08rem;
    line-height: 1.74;
  }

  .results-report__panel {
    padding: 1.8rem 1.8rem;
    border-radius: 30px;
  }

  .results-report__section-heading h3 {
    font-size: 1.56rem;
  }

  .results-report__section-note {
    font-size: 0.98rem;
  }

  .results-report__section-note,
  .results-report__section-body,
  .results-report__disclaimer,
  .results-dimension-card__summary,
  .results-dimension-card__details p {
    line-height: 1.68;
  }

  .results-score-overview {
    grid-template-columns: minmax(0, 1fr);
    row-gap: 1.1rem;
  }

  .results-score-overview__item {
    gap: 0.82rem;
    padding: 1.05rem 0.9rem 1.1rem;
    border: 0;
    border-top: 1px solid rgba(205, 189, 168, 0.58);
    border-radius: 0;
    background: transparent;
  }

  .results-score-overview__header strong {
    font-size: 1.04rem;
    line-height: 1.34;
  }

  .results-score-overview__header span {
    font-size: 0.92rem;
  }

  .results-score-overview__bar {
    height: 0.86rem;
  }

  .results-dimension-card {
    gap: 1.25rem;
    padding: 1.8rem 1.7rem;
    border-radius: 32px;
  }

  .results-dimension-card__header h4 {
    font-size: 1.34rem;
  }

  .results-dimension-card__helper {
    font-size: 0.94rem;
  }

  .results-dimension-card__score span {
    font-size: 1.3rem;
  }

  .results-dimension-card__footer {
    justify-content: flex-start;
  }

  .results-dimension-card__toggle {
    width: auto;
    min-width: 11.25rem;
  }

  .results-dimension-card__details {
    grid-template-columns: minmax(0, 1fr);
    gap: 1.15rem;
  }

  .results-dimension-card__detail-block {
    align-content: start;
  }

  .results-report__closing {
    grid-template-columns: minmax(0, 1fr);
    align-items: stretch;
    gap: 1.4rem;
  }

  .results-report__section--conclusion {
    min-height: 100%;
    max-width: 48rem;
    width: 100%;
    margin: 0 auto;
    background:
      linear-gradient(180deg, rgba(255, 252, 247, 0.99) 0%, rgba(246, 238, 226, 0.97) 100%),
      linear-gradient(135deg, rgba(95, 112, 89, 0.06), transparent 56%);
  }

  .results-report__section--recommendations {
    min-height: 100%;
    max-width: 48rem;
    width: 100%;
    margin: 0 auto;
    background:
      linear-gradient(180deg, rgba(255, 252, 247, 0.99) 0%, rgba(248, 242, 233, 0.99) 100%),
      linear-gradient(135deg, rgba(134, 100, 84, 0.06), transparent 56%);
  }

  .results-report__section-body {
    font-size: 1.04rem;
    max-width: 60ch;
    color: #3d413b;
  }

  .results-bullet-list {
    gap: 1rem;
    max-width: 60ch;
  }

  .results-bullet-list li,
  .results-inline-list li {
    padding-left: 1.15rem;
    font-size: 0.98rem;
  }

  .assessment-run-shell {
    padding: 2.35rem;
  }

  .assessment-run {
    gap: 1.35rem;
  }

  .assessment-run-hero__identity {
    max-width: 100%;
  }

  .assessment-run-hero__title-group {
    gap: 1.15rem;
  }

  .assessment-run-hero__intro h1 {
    max-width: none;
    white-space: nowrap;
    font-size: clamp(1.72rem, 1.8vw, 1.96rem);
  }

  .assessment-progress__summary {
    gap: 0.75rem;
  }

  .assessment-progress {
    padding: 1.35rem 1.45rem 1.15rem;
  }

  .assessment-step-card {
    min-height: 31rem;
  }

  .assessment-step-card__header {
    margin-bottom: 2.5rem;
  }

  .assessment-likert__scale {
    padding: 1.5rem;
  }
}

@media (min-width: 1280px) {
  .attempt-results-page {
    max-width: 72rem;
  }

  .results-report__hero {
    grid-template-columns: minmax(0, 1fr);
    padding: 2.5rem 2.45rem;
  }

  .results-report__panel {
    padding: 1.8rem 1.8rem;
  }

  .results-dimension-card {
    padding: 1.7rem 1.6rem;
  }
}

@media (max-width: 767px) {
  :root {
    --assessment-mobile-radius-lg: 22px;
    --assessment-mobile-radius-md: 18px;
    --assessment-mobile-shadow: 0 22px 48px -36px rgba(60, 49, 37, 0.22);
    --assessment-mobile-shadow-strong: 0 22px 38px -28px rgba(69, 57, 43, 0.28);
  }

  body {
    padding: 0.75rem;
  }

  .assessment-run-page {
    min-height: calc(100svh - 1.5rem);
    min-height: calc(100dvh - 1.5rem);
    padding: 0.35rem 0.35rem 0.45rem;
    border-radius: 26px;
  }

  .assessment-run-shell,
  .assessment-run-hero {
    border-radius: var(--assessment-mobile-radius-lg);
  }

  .assessment-run-shell {
    display: flex;
    min-height: 100%;
    padding: 0.8rem 0.8rem 0.8rem;
    border-color: rgba(202, 186, 165, 0.78);
    background: linear-gradient(180deg, rgba(248, 239, 226, 0.96) 0%, rgba(241, 230, 213, 0.98) 100%);
    box-shadow:
      var(--assessment-mobile-shadow),
      inset 0 1px 0 rgba(255, 255, 255, 0.68);
  }

  .assessment-run-shell > * {
    flex: 1 1 auto;
    min-width: 0;
  }

  .assessment-run {
    display: grid;
    grid-template-rows: auto minmax(0, 1fr);
    gap: 0.85rem;
    min-height: 100%;
  }

  .assessment-run > * + * {
    margin-top: 0;
  }

  .assessment-run-hero {
    padding: 1rem 1rem 0.95rem;
    border-color: rgba(207, 191, 170, 0.9);
    box-shadow:
      0 20px 42px -34px rgba(63, 50, 37, 0.2),
      inset 0 1px 0 rgba(255, 255, 255, 0.76);
  }

  .assessment-run-hero::before {
    width: 9rem;
    height: 9rem;
    inset: auto -2.5rem -3rem auto;
  }

  .assessment-run-hero__intro {
    gap: 0.85rem;
  }

  .assessment-run-hero__title-group {
    gap: 0.35rem;
  }

  .assessment-run-hero__intro h1 {
    font-size: clamp(1.32rem, 5.2vw, 1.58rem);
    line-height: 1.12;
    max-width: 18ch;
  }

  .assessment-run-hero__participant {
    font-size: 0.92rem;
    line-height: 1.35;
    color: #55584f;
  }

  .assessment-progress {
    gap: 0.7rem;
    padding: 0.9rem 1rem 0.95rem;
    border-color: rgba(183, 161, 136, 0.84);
    border-radius: var(--assessment-mobile-radius-md);
    background: linear-gradient(180deg, rgba(255, 250, 243, 0.78) 0%, rgba(242, 231, 215, 0.94) 100%);
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.78),
      inset 0 0 0 1px rgba(255, 255, 255, 0.12),
      0 16px 28px -24px rgba(70, 56, 40, 0.18);
  }

  .assessment-progress__summary {
    gap: 0.25rem;
  }

  .assessment-progress__metric {
    gap: 0.2rem;
  }

  .assessment-progress__metric-label {
    font-size: 0.6rem;
    letter-spacing: 0.16em;
    color: #7a7469;
  }

  .assessment-progress__metric-value {
    font-size: 0.96rem;
    line-height: 1.2;
    color: #292c28;
  }

  .assessment-progress__track {
    height: 0.95rem;
    background: linear-gradient(180deg, #d8cebe 0%, #e7ddcf 100%);
    box-shadow:
      inset 0 1px 2px rgba(88, 69, 50, 0.16),
      inset 0 0 0 1px rgba(255, 255, 255, 0.28);
  }

  .assessment-progress__fill {
    background: linear-gradient(90deg, #5f7059 0%, #866454 100%);
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.22),
      0 10px 18px -14px rgba(79, 62, 45, 0.42);
  }

  .assessment-step-card {
    display: flex;
    flex-direction: column;
    min-height: 100%;
    padding: 1.15rem 1rem 1.05rem;
    border-color: rgba(210, 194, 173, 0.92);
    border-radius: var(--assessment-mobile-radius-lg);
    background: linear-gradient(180deg, rgba(255, 253, 249, 0.98) 0%, rgba(247, 239, 226, 0.98) 100%);
    box-shadow:
      var(--assessment-mobile-shadow),
      inset 0 1px 0 rgba(255, 255, 255, 0.82);
  }

  .assessment-step-card__header {
    margin-bottom: 1.2rem;
  }

  .assessment-step-card__question-region {
    gap: 0.55rem;
    min-height: clamp(4.75rem, 13dvh, 6.1rem);
    padding: 0;
  }

  .assessment-step-card__question-region--stable {
    min-height: clamp(4.75rem, 13dvh, 6.1rem);
  }

  .assessment-step-card__kicker {
    font-size: 0.62rem;
    letter-spacing: 0.14em;
    color: #858173;
  }

  .assessment-step-card__header h3 {
    font-size: clamp(1.5rem, 6vw, 1.9rem);
    line-height: 1.16;
    letter-spacing: -0.03em;
  }

  .assessment-options {
    gap: 0.6rem;
  }

  .assessment-likert {
    max-width: none;
  }

  .assessment-step-layout {
    display: grid;
    grid-template-rows: minmax(0, 1fr) auto;
    gap: 0.85rem;
    min-height: 0;
  }

  .assessment-step-layout__footer {
    display: grid;
    gap: 0.7rem;
    margin-top: auto;
  }

  .assessment-step-actions {
    grid-template-columns: 1fr;
    gap: 0.7rem;
    padding-bottom: calc(0.2rem + env(safe-area-inset-bottom, 0px));
  }

  .assessment-completion-state {
    padding: 1.25rem;
    border-radius: 20px;
  }

  .assessment-completion-state__hero h2 {
    font-size: clamp(1.8rem, 8vw, 2.35rem);
  }

  .attempt-results-page__shell,
  .results-report__hero,
  .results-panel,
  .results-dimension-card {
    border-radius: 20px;
  }

  .results-dimension-card__header {
    align-items: flex-start;
    flex-direction: column;
  }

  .results-dimension-card__score {
    text-align: left;
  }

  .results-dimension-card__toggle {
    width: 100%;
  }

  .results-dimension-card__toggle[aria-expanded="true"] {
    width: auto;
  }

  .assessment-likert__labels {
    font-size: 0.72rem;
    gap: 0.65rem;
    padding: 0;
    color: #54584f;
  }

  .assessment-likert__options {
    gap: 0.5rem;
  }

  .assessment-likert__scale {
    gap: 0.75rem;
    padding: 0;
    border: 0;
    border-radius: 0;
    background: transparent;
    box-shadow: none;
  }

  .assessment-likert-option {
    min-height: 4.35rem;
    padding: 0.95rem 0.4rem 0.9rem;
    border-width: 1px;
    border-color: rgba(194, 175, 151, 0.92);
    border-radius: 17px;
    background: linear-gradient(180deg, #fffaf2 0%, #f4e7d7 100%);
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.78),
      0 14px 24px -24px rgba(64, 53, 41, 0.2);
  }

  .assessment-likert-option:active {
    transform: scale(0.985);
    border-color: #9b8c78;
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.72),
      0 10px 16px -18px rgba(64, 53, 41, 0.18);
  }

  .assessment-likert-option--selected {
    transform: translateY(-1px);
    border-color: #64715e;
    background: linear-gradient(180deg, #6b7965 0%, #556353 100%);
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.14),
      0 18px 28px -20px rgba(73, 82, 67, 0.46),
      0 0 0 1px rgba(99, 114, 93, 0.18);
  }

  .assessment-likert-option__value {
    font-size: 1.06rem;
  }

  .assessment-option {
    gap: 0.8rem;
    min-height: 4.15rem;
    padding: 0.95rem 0.95rem 0.95rem 0.9rem;
    border-color: rgba(205, 191, 170, 0.94);
    border-radius: 17px;
    background: linear-gradient(180deg, rgba(252, 248, 241, 0.96) 0%, rgba(245, 236, 223, 0.96) 100%);
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.76),
      0 12px 24px -24px rgba(65, 53, 41, 0.14);
  }

  .assessment-option:active {
    transform: scale(0.992);
    border-color: #b49d80;
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.74),
      0 8px 16px -18px rgba(65, 53, 41, 0.18);
  }

  .assessment-option--selected {
    border-color: #7a6a57;
    background: linear-gradient(180deg, rgba(244, 231, 213, 0.98) 0%, rgba(237, 221, 202, 0.98) 100%);
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.72),
      0 18px 30px -24px rgba(92, 71, 53, 0.22),
      0 0 0 1px rgba(145, 118, 94, 0.22);
  }

  .assessment-option input {
    width: 1.2rem;
    height: 1.2rem;
  }

  .assessment-option__marker {
    min-width: 2.25rem;
    min-height: 2.25rem;
    border-color: rgba(170, 149, 126, 0.92);
    background: rgba(255, 252, 247, 0.96);
    color: #635748;
  }

  .assessment-option--selected .assessment-option__marker {
    border-color: #736350;
    background: linear-gradient(180deg, #7a6a57 0%, #655545 100%);
    color: #fff;
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.14),
      0 8px 16px -12px rgba(88, 67, 49, 0.4);
  }

  .assessment-option__label {
    font-size: 0.97rem;
    line-height: 1.35;
    color: #292721;
  }

  .assessment-option--selected .assessment-option__label {
    color: #1f1d18;
  }

  .assessment-inline-message {
    margin-top: 0.9rem;
    padding: 0.8rem 0.9rem;
    border-radius: 15px;
  }

  .assessment-step-actions button {
    min-height: 3.7rem;
  }

  .assessment-step-actions__button {
    border-radius: var(--assessment-mobile-radius-md);
  }

  .assessment-step-actions__button--ghost {
    border-color: rgba(198, 181, 159, 0.9);
    background: linear-gradient(180deg, rgba(248, 241, 231, 0.72) 0%, rgba(239, 228, 211, 0.84) 100%);
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.72),
      0 10px 18px -20px rgba(71, 61, 49, 0.12);
    color: #5a5d56;
    font-weight: 600;
  }

  .assessment-step-actions__button--save {
    border-color: rgba(164, 143, 119, 0.96);
    background: linear-gradient(180deg, rgba(245, 232, 214, 0.98) 0%, rgba(233, 217, 197, 0.98) 100%);
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.66),
      0 18px 28px -22px rgba(88, 69, 51, 0.24),
      0 0 0 1px rgba(176, 152, 124, 0.12);
    color: #27251f;
    font-weight: 675;
  }

  .assessment-step-actions__button:active:not(:disabled) {
    transform: scale(0.992);
  }
}
</file>

<file path="app/page.tsx">
import { cookies } from "next/headers";
import { AssessmentForm } from "@/components/assessment/assessment-form";
import {
  ASSESSMENT_ATTEMPT_COOKIE_NAME,
  getActiveTest,
  getAnswerOptionsForQuestions,
  getAssessmentResumeState,
  getCompletedAssessmentReportSnapshot,
  getCompletedAssessmentResults,
  getQuestionsForTest,
} from "@/lib/assessment/tests";

export const dynamic = "force-dynamic";

export default async function HomePage() {
  try {
    const test = await getActiveTest();

    if (!test) {
      return (
        <main>
          <section className="card">
            <h1>No active test available</h1>
            <p>Please check back later.</p>
          </section>
        </main>
      );
    }

    const questions = await getQuestionsForTest(test.id);

    if (questions.length === 0) {
      return (
        <main>
          <section className="card">
            <h1>{test.name}</h1>
            <p>{test.description ?? "Opis testa trenutno nije dostupan."}</p>
          </section>

          <section className="card">
            <h2>No questions available</h2>
            <p>This test does not have any questions yet.</p>
          </section>
        </main>
      );
    }

    const attemptId = cookies().get(ASSESSMENT_ATTEMPT_COOKIE_NAME)?.value;
    const answerOptionsByQuestionId = await getAnswerOptionsForQuestions(
      questions.map((question) => question.id),
    );
    const resumeState = await getAssessmentResumeState(test.id, attemptId);
    const results = await getCompletedAssessmentResults(test.id, resumeState.attemptId);
    const report = await getCompletedAssessmentReportSnapshot(test.id, resumeState.attemptId);

    return (
      <main>
        <section className="card">
          <h1>{test.name}</h1>
          <p>{test.description ?? "Opis testa trenutno nije dostupan."}</p>
        </section>

        <section className="card">
          <AssessmentForm
            testId={test.id}
            questions={questions}
            answerOptionsByQuestionId={answerOptionsByQuestionId}
            initialAttemptId={resumeState.attemptId}
            initialAttemptStatus={resumeState.attemptStatus}
            initialCompletedAt={resumeState.completedAt}
            initialSelections={resumeState.selections}
            initialResults={results}
            initialReport={report}
          />
        </section>
      </main>
    );
  } catch {
    return (
      <main>
        <section className="card">
          <h1>Unable to load assessment</h1>
          <p>Please try again later.</p>
        </section>
      </main>
    );
  }
}
</file>

<file path="lib/assessment/tests.ts">
import type {
  AnswerOption,
  AssessmentSelectionsInput,
  AttemptStatus,
  Question,
  QuestionType,
  Test,
} from "@/lib/assessment/types";
import {
  buildSelectionsFromResponses,
  type AssessmentCompletionState,
} from "@/lib/assessment/completion";
import {
  calculateCompletedAssessmentResults,
  persistCompletedAssessmentResults,
  type CompletedAssessmentResults,
} from "@/lib/assessment/scoring";
import {
  getCompletedAssessmentReport,
  type CompletedAssessmentReportState,
} from "@/lib/assessment/reports";
import { createSupabaseAdminClient } from "@/lib/supabase/admin";
import { createSupabaseServerClient } from "@/lib/supabase/server";

export type ActiveTest = Pick<Test, "id" | "slug" | "name" | "description">;
export type TestQuestion = Pick<
  Question,
  "id" | "code" | "text" | "question_order" | "question_type" | "is_required"
>;
export type TestAnswerOption = Pick<
  AnswerOption,
  "id" | "question_id" | "label" | "option_order"
>;
export const ASSESSMENT_ATTEMPT_COOKIE_NAME = "assessment_attempt_id";

type ResumeResponseRecord = {
  question_id: string;
  response_kind: QuestionType;
  answer_option_id: string | null;
  text_value: string | null;
  response_selections: Array<{
    answer_option_id: string;
  }> | null;
};

type ResumeAttemptRecord = {
  id: string;
  status: AttemptStatus;
  completed_at: string | null;
};

export type AssessmentResumeState = {
  attemptId: string | null;
  attemptStatus: AttemptStatus | null;
  completedAt: string | null;
  selections: AssessmentSelectionsInput;
};

export async function getActiveTest(): Promise<ActiveTest | null> {
  const supabase = createSupabaseServerClient();
  const { data, error } = await supabase
    .from("tests")
    .select("id, slug, name, description")
    .eq("is_active", true)
    .maybeSingle();

  if (error) {
    throw new Error(`Failed to load active test: ${error.message}`);
  }

  return data as ActiveTest | null;
}

export async function getQuestionsForTest(testId: string): Promise<TestQuestion[]> {
  const supabase = createSupabaseServerClient();
  const { data, error } = await supabase
    .from("questions")
    .select("id, code, text, question_order, question_type, is_required")
    .eq("test_id", testId)
    .eq("is_active", true)
    .order("question_order", { ascending: true });

  if (error) {
    throw new Error(`Failed to load questions: ${error.message}`);
  }

  return (data ?? []) as TestQuestion[];
}

export async function getAnswerOptionsForQuestions(
  questionIds: string[],
): Promise<Record<string, TestAnswerOption[]>> {
  if (questionIds.length === 0) {
    return {};
  }

  const supabase = createSupabaseServerClient();
  const { data, error } = await supabase
    .from("answer_options")
    .select("id, question_id, label, option_order")
    .in("question_id", questionIds)
    .order("option_order", { ascending: true });

  if (error) {
    throw new Error(`Failed to load answer options: ${error.message}`);
  }

  return ((data ?? []) as TestAnswerOption[]).reduce<Record<string, TestAnswerOption[]>>(
    (groupedOptions, option) => {
      const questionOptions = groupedOptions[option.question_id] ?? [];
      questionOptions.push(option);
      groupedOptions[option.question_id] = questionOptions;
      return groupedOptions;
    },
    {},
  );
}

export async function getAssessmentResumeState(
  testId: string,
  attemptId: string | undefined,
): Promise<AssessmentResumeState> {
  if (!attemptId) {
    return {
      attemptId: null,
      attemptStatus: null,
      completedAt: null,
      selections: {},
    };
  }

  const supabase = createSupabaseAdminClient();
  const { data: attempt, error: attemptError } = await supabase
    .from("attempts")
    .select("id, status, completed_at")
    .eq("id", attemptId)
    .eq("test_id", testId)
    .maybeSingle();

  if (attemptError) {
    throw new Error(`Failed to load attempt: ${attemptError.message}`);
  }

  if (!attempt) {
    return {
      attemptId: null,
      attemptStatus: null,
      completedAt: null,
      selections: {},
    };
  }

  const { data: responsesData, error: responsesError } = await supabase
    .from("responses")
    .select(
      "question_id, response_kind, answer_option_id, text_value, response_selections(answer_option_id)",
    )
    .eq("attempt_id", attemptId);

  if (responsesError) {
    throw new Error(`Failed to load responses: ${responsesError.message}`);
  }

  const resumeAttempt = attempt as ResumeAttemptRecord;

  return {
    attemptId: resumeAttempt.id,
    attemptStatus: resumeAttempt.status,
    completedAt: resumeAttempt.completed_at,
    selections: buildSelectionsFromResponses((responsesData ?? []) as ResumeResponseRecord[]),
  };
}

export async function getCompletedAssessmentResults(
  testId: string,
  attemptId: string | null,
): Promise<CompletedAssessmentResults | null> {
  if (!attemptId) {
    return null;
  }

  const results = await calculateCompletedAssessmentResults(testId, attemptId);

  if (!results) {
    return null;
  }

  if (results.dimensions.length === 0) {
    return results;
  }

  const supabase = createSupabaseAdminClient();
  const { count, error } = await supabase
    .from("dimension_scores")
    .select("id", { count: "exact", head: true })
    .eq("attempt_id", attemptId);

  if (error) {
    throw new Error(`Failed to load persisted results: ${error.message}`);
  }

  if ((count ?? 0) === 0) {
    return persistCompletedAssessmentResults(testId, attemptId);
  }

  return results;
}

export async function getCompletedAssessmentReportSnapshot(
  testId: string,
  attemptId: string | null,
): Promise<CompletedAssessmentReportState | null> {
  return getCompletedAssessmentReport(testId, attemptId);
}
</file>

<file path="app/actions/assessment.ts">
"use server";

import { cookies } from "next/headers";
import { redirect } from "next/navigation";
import {
  loadAssessmentCompletionState,
} from "@/lib/assessment/completion-server";
import {
  getActiveOrganizationForUser,
  getAttemptForOrganization,
  getParticipantForOrganization,
} from "@/lib/b2b/organizations";
import {
  persistCompletedAssessmentReport,
  type CompletedAssessmentReportState,
} from "@/lib/assessment/reports";
import {
  persistCompletedAssessmentResults,
  type CompletedAssessmentResults,
} from "@/lib/assessment/scoring";
import { ASSESSMENT_ATTEMPT_COOKIE_NAME } from "@/lib/assessment/tests";
import type {
  AssessmentSelectionsInput,
  AssessmentSelectionValue,
  AttemptOwnershipContext,
  AttemptStatus,
  QuestionType,
} from "@/lib/assessment/types";
import {
  AuthenticationRequiredError,
  requireAuthenticatedUser,
  requireAuthenticatedUserForAction,
} from "@/lib/auth/session";
import { createSupabaseAdminClient } from "@/lib/supabase/admin";

export type SaveAssessmentSelectionsInput = {
  attemptId: string | null;
  testId: string;
  selections: AssessmentSelectionsInput;
  ownershipContext?: AttemptOwnershipContext;
};

type SaveAssessmentSelectionsResult =
  | {
      ok: true;
      attemptId: string;
      message: string;
    }
  | {
      ok: false;
      message: string;
    };

type CompleteAssessmentAttemptResult =
  | {
      ok: true;
      attemptId: string;
      completedAt: string;
      message: string;
      results: CompletedAssessmentResults | null;
      report: CompletedAssessmentReportState | null;
    }
  | {
      ok: false;
      message: string;
    };

type QuestionRecord = {
  id: string;
  question_type: QuestionType;
};

type AnswerOptionRecord = {
  id: string;
  question_id: string;
};

type AttemptRecord = {
  id: string;
  status: AttemptStatus;
  completed_at: string | null;
};

type ResponseInsert = {
  attempt_id: string;
  question_id: string;
  response_kind: QuestionType;
  answer_option_id?: string | null;
  text_value?: string | null;
};

type InsertedResponseRecord = {
  id: string;
  question_id: string;
  response_kind: QuestionType;
};

type ResponseSelectionInsert = {
  response_id: string;
  question_id: string;
  answer_option_id: string;
};

type PersistSelectionsResult =
  | {
      ok: true;
      attemptId: string;
    }
  | {
      ok: false;
      message: string;
    };

type PersistAssessmentSelectionsOptions = {
  persistAttemptCookie?: boolean;
  requireProtectedOwnership?: boolean;
};

const DEFAULT_B2B_TEST_SLUG = "ipip50-hr-v1";

function isStringArray(value: AssessmentSelectionValue): value is string[] {
  return Array.isArray(value);
}

function isSelectionValue(value: unknown): value is AssessmentSelectionValue {
  if (typeof value === "string") {
    return true;
  }

  return Array.isArray(value) && value.every((entry) => typeof entry === "string");
}

function isSelectionMap(value: SaveAssessmentSelectionsInput["selections"]): boolean {
  return Object.entries(value).every(
    ([questionId, selection]) => !!questionId && isSelectionValue(selection),
  );
}

function getDistinctOptionIds(optionIds: string[]): string[] {
  return [...new Set(optionIds.filter((optionId) => optionId.length > 0))];
}

function isSupportedQuestionType(questionType: QuestionType): boolean {
  return (
    questionType === "single_choice" ||
    questionType === "multiple_choice" ||
    questionType === "text"
  );
}

function getSaveFailureMessage(error: unknown): string {
  if (error instanceof AuthenticationRequiredError) {
    return "Your session has expired. Sign in again to continue.";
  }

  if (
    error instanceof Error &&
    error.message === "Missing required env var: SUPABASE_SERVICE_ROLE_KEY"
  ) {
    return "Saving is not configured on the server.";
  }

  return "Unable to save progress right now. Please try again.";
}

function getCompletionFailureMessage(error: unknown): string {
  if (error instanceof AuthenticationRequiredError) {
    return "Your session has expired. Sign in again to continue.";
  }

  if (
    error instanceof Error &&
    error.message === "Missing required env var: SUPABASE_SERVICE_ROLE_KEY"
  ) {
    return "Completion is not configured on the server.";
  }

  return "Unable to complete the assessment right now. Please try again.";
}

function getIncompleteRequiredAnswersMessage(missingRequiredQuestionCount: number): string {
  return missingRequiredQuestionCount === 1
    ? "Answer the remaining required question before completing the assessment."
    : `Answer all required questions before completing the assessment. ${missingRequiredQuestionCount} required questions are still unanswered.`;
}

function isNextRedirectError(error: unknown): boolean {
  if (!error || typeof error !== "object") {
    return false;
  }

  const digest = Reflect.get(error, "digest");
  return typeof digest === "string" && digest.startsWith("NEXT_REDIRECT");
}

async function persistAssessmentSelections(
  input: SaveAssessmentSelectionsInput,
  options: PersistAssessmentSelectionsOptions = {},
): Promise<PersistSelectionsResult> {
  if (!input.testId) {
    return { ok: false, message: "Missing test id." };
  }

  if (!isSelectionMap(input.selections)) {
    return { ok: false, message: "Invalid save payload." };
  }

  const supabase = createSupabaseAdminClient();

  const { data: existingTest, error: testError } = await supabase
    .from("tests")
    .select("id")
    .eq("id", input.testId)
    .maybeSingle();

  if (testError) {
    return { ok: false, message: "Unable to validate test." };
  }

  if (!existingTest) {
    return { ok: false, message: "Test not found." };
  }

  const selectionEntries = Object.entries(input.selections);
  const questionIds = selectionEntries.map(([questionId]) => questionId);
  let questionsById = new Map<string, QuestionRecord>();

  if (questionIds.length > 0) {
    const { data: questionsData, error: questionsError } = await supabase
      .from("questions")
      .select("id, question_type")
      .eq("test_id", input.testId)
      .in("id", questionIds);

    if (questionsError) {
      return { ok: false, message: "Unable to validate questions." };
    }

    const questions = (questionsData ?? []) as QuestionRecord[];
    questionsById = new Map(questions.map((question) => [question.id, question]));

    if (questionIds.some((questionId) => !questionsById.has(questionId))) {
      return {
        ok: false,
        message: "One or more questions are invalid for this test.",
      };
    }
  }

  const unsupportedQuestion = questionIds.find((questionId) => {
    const question = questionsById.get(questionId);
    return question ? !isSupportedQuestionType(question.question_type) : false;
  });

  if (unsupportedQuestion) {
    return {
      ok: false,
      message:
        "This assessment contains question types that are not supported for saving yet.",
    };
  }

  const optionIdsToValidate = getDistinctOptionIds(
    selectionEntries.flatMap(([questionId, value]) => {
      const question = questionsById.get(questionId);

      if (!question || question.question_type === "text") {
        return [];
      }

      if (question.question_type === "single_choice") {
        return typeof value === "string" ? [value] : [];
      }

      return isStringArray(value) ? value : [];
    }),
  );

  let answerOptionsById = new Map<string, AnswerOptionRecord>();

  if (optionIdsToValidate.length > 0) {
    const { data: answerOptionsData, error: answerOptionsError } = await supabase
      .from("answer_options")
      .select("id, question_id")
      .in("id", optionIdsToValidate);

    if (answerOptionsError) {
      return { ok: false, message: "Unable to validate answer options." };
    }

    const answerOptions = (answerOptionsData ?? []) as AnswerOptionRecord[];
    answerOptionsById = new Map(answerOptions.map((option) => [option.id, option]));
  }

  for (const [questionId, value] of selectionEntries) {
    const question = questionsById.get(questionId);

    if (!question) {
      return { ok: false, message: "Question validation failed." };
    }

    if (question.question_type === "text") {
      if (typeof value !== "string") {
        return { ok: false, message: "Text responses must be saved as text." };
      }

      continue;
    }

    if (question.question_type === "single_choice") {
      if (typeof value !== "string") {
        return {
          ok: false,
          message: "Single choice responses must contain exactly one option.",
        };
      }

      if (!value) {
        continue;
      }

      const option = answerOptionsById.get(value);

      if (!option || option.question_id !== questionId) {
        return { ok: false, message: "One or more answer options are invalid." };
      }

      continue;
    }

    if (!isStringArray(value)) {
      return {
        ok: false,
        message: "Multiple choice responses must be saved as an option list.",
      };
    }

    for (const optionId of getDistinctOptionIds(value)) {
      const option = answerOptionsById.get(optionId);

      if (!option || option.question_id !== questionId) {
        return { ok: false, message: "One or more answer options are invalid." };
      }
    }
  }

  let nextAttemptId = input.attemptId;

  if (options.requireProtectedOwnership && !nextAttemptId) {
    return {
      ok: false,
      message: "Protected assessment execution requires an existing attempt.",
    };
  }

  if (nextAttemptId) {
    if (options.requireProtectedOwnership) {
      const user = await requireAuthenticatedUserForAction();
      const organization = await getActiveOrganizationForUser(user.id);

      if (!organization) {
        return { ok: false, message: "No active organization is available for this user." };
      }

      const ownedAttempt = await getAttemptForOrganization(organization.id, nextAttemptId);

      if (!ownedAttempt || ownedAttempt.test_id !== input.testId) {
        return { ok: false, message: "This assessment attempt is not available in the active organization." };
      }
    }

    const { data: existingAttemptData, error: existingAttemptError } = await supabase
      .from("attempts")
      .select("id, status, completed_at")
      .eq("id", nextAttemptId)
      .eq("test_id", input.testId)
      .maybeSingle();

    if (existingAttemptError) {
      return { ok: false, message: "Unable to validate attempt." };
    }

    if (!existingAttemptData) {
      nextAttemptId = null;
    } else {
      const attempt = existingAttemptData as AttemptRecord;

      if (attempt.status === "completed") {
        return {
          ok: false,
          message: "This assessment attempt is already completed and cannot be edited.",
        };
      }

      if (attempt.status !== "in_progress") {
        return {
          ok: false,
          message: "This assessment attempt is no longer available for editing.",
        };
      }
    }
  }

  if (!nextAttemptId) {
    if (options.requireProtectedOwnership) {
      return {
        ok: false,
        message: "Protected assessment execution requires an existing attempt.",
      };
    }

    const { data: createdAttemptData, error: createAttemptError } = await supabase
      .from("attempts")
      .insert({
        test_id: input.testId,
        user_id: input.ownershipContext?.userId ?? null,
        organization_id: input.ownershipContext?.organizationId ?? null,
        participant_id: input.ownershipContext?.participantId ?? null,
      })
      .select("id")
      .single();

    if (createAttemptError || !createdAttemptData) {
      return { ok: false, message: "Unable to create attempt." };
    }

    nextAttemptId = (createdAttemptData as Pick<AttemptRecord, "id">).id;
  }

  if (!nextAttemptId) {
    return { ok: false, message: "Unable to determine attempt id." };
  }

  if (questionIds.length > 0) {
    const { error: deleteResponsesError } = await supabase
      .from("responses")
      .delete()
      .eq("attempt_id", nextAttemptId)
      .in("question_id", questionIds);

    if (deleteResponsesError) {
      return { ok: false, message: "Unable to replace saved responses." };
    }
  }

  const responseRows: ResponseInsert[] = [];
  const multipleChoiceSelectionsByQuestionId = new Map<string, string[]>();

  for (const [questionId, value] of selectionEntries) {
    const question = questionsById.get(questionId);

    if (!question) {
      continue;
    }

    if (question.question_type === "text") {
      if (typeof value !== "string" || value.length === 0) {
        continue;
      }

      responseRows.push({
        attempt_id: nextAttemptId,
        question_id: questionId,
        response_kind: "text",
        text_value: value,
      });
      continue;
    }

    if (question.question_type === "single_choice") {
      if (typeof value !== "string" || value.length === 0) {
        continue;
      }

      responseRows.push({
        attempt_id: nextAttemptId,
        question_id: questionId,
        response_kind: "single_choice",
        answer_option_id: value,
      });
      continue;
    }

    if (!isStringArray(value)) {
      continue;
    }

    const selectedOptionIds = getDistinctOptionIds(value);

    if (selectedOptionIds.length === 0) {
      continue;
    }

    responseRows.push({
      attempt_id: nextAttemptId,
      question_id: questionId,
      response_kind: "multiple_choice",
    });
    multipleChoiceSelectionsByQuestionId.set(questionId, selectedOptionIds);
  }

  if (responseRows.length > 0) {
    const { data: insertedResponsesData, error: insertResponsesError } = await supabase
      .from("responses")
      .insert(responseRows)
      .select("id, question_id, response_kind");

    if (insertResponsesError) {
      return { ok: false, message: "Unable to save responses." };
    }

    const insertedResponses = (insertedResponsesData ?? []) as InsertedResponseRecord[];
    const responseSelections: ResponseSelectionInsert[] = insertedResponses.flatMap((response) => {
      if (response.response_kind !== "multiple_choice") {
        return [];
      }

      const selectedOptionIds = multipleChoiceSelectionsByQuestionId.get(response.question_id) ?? [];

      return selectedOptionIds.map((answerOptionId) => ({
        response_id: response.id,
        question_id: response.question_id,
        answer_option_id: answerOptionId,
      }));
    });

    if (responseSelections.length > 0) {
      const { error: insertSelectionsError } = await supabase
        .from("response_selections")
        .insert(responseSelections);

      if (insertSelectionsError) {
        return { ok: false, message: "Unable to save multiple choice selections." };
      }
    }
  }

  if (options.persistAttemptCookie !== false) {
    cookies().set(ASSESSMENT_ATTEMPT_COOKIE_NAME, nextAttemptId, {
      httpOnly: true,
      sameSite: "lax",
      path: "/",
    });
  }

  return {
    ok: true,
    attemptId: nextAttemptId,
  };
}

async function getTestIdBySlug(testSlug: string): Promise<string | null> {
  const supabase = createSupabaseAdminClient();
  const { data, error } = await supabase
    .from("tests")
    .select("id")
    .eq("slug", testSlug)
    .maybeSingle();

  if (error) {
    throw new Error(`Failed to resolve test: ${error.message}`);
  }

  return data?.id ?? null;
}

export async function saveAssessmentProgress(
  input: SaveAssessmentSelectionsInput,
): Promise<SaveAssessmentSelectionsResult> {
  try {
    const result = await persistAssessmentSelections(input);

    if (!result.ok) {
      return result;
    }

    return {
      ok: true,
      attemptId: result.attemptId,
      message: "Progress saved.",
    };
  } catch (error) {
    if (isNextRedirectError(error)) {
      throw error;
    }

    console.error("saveAssessmentProgress failed", error);

    return {
      ok: false,
      message: getSaveFailureMessage(error),
    };
  }
}

export async function saveProtectedAssessmentProgress(
  input: SaveAssessmentSelectionsInput,
): Promise<SaveAssessmentSelectionsResult> {
  try {
    const result = await persistAssessmentSelections(input, {
      persistAttemptCookie: false,
      requireProtectedOwnership: true,
    });

    if (!result.ok) {
      return result;
    }

    return {
      ok: true,
      attemptId: result.attemptId,
      message: "Progress saved.",
    };
  } catch (error) {
    if (isNextRedirectError(error)) {
      throw error;
    }

    console.error("saveProtectedAssessmentProgress failed", error);

    return {
      ok: false,
      message: getSaveFailureMessage(error),
    };
  }
}

export async function createB2BAttempt(formData: FormData) {
  const participantId = String(formData.get("participantId") ?? "").trim();
  const testSlug = String(formData.get("testSlug") ?? DEFAULT_B2B_TEST_SLUG).trim();
  const user = await requireAuthenticatedUser();

  if (!participantId) {
    redirect("/dashboard?error=missing-participant");
  }

  const organization = await getActiveOrganizationForUser(user.id);

  if (!organization) {
    redirect("/dashboard?error=no-active-organization");
  }

  const participant = await getParticipantForOrganization(organization.id, participantId);

  if (!participant) {
    redirect("/dashboard?error=participant-not-found");
  }

  const testId = await getTestIdBySlug(testSlug);

  if (!testId) {
    redirect("/dashboard?error=test-not-found");
  }

  const supabase = createSupabaseAdminClient();
  const { data, error } = await supabase
    .from("attempts")
    .insert({
      test_id: testId,
      user_id: user.id,
      organization_id: organization.id,
      participant_id: participant.id,
    })
    .select("id")
    .single();

  if (error || !data) {
    console.error("createB2BAttempt failed", error);
    redirect("/dashboard?error=create-attempt-failed");
  }

  redirect(`/dashboard/attempts/${data.id}`);
}

export async function completeAssessmentAttempt(
  input: SaveAssessmentSelectionsInput,
): Promise<CompleteAssessmentAttemptResult> {
  try {
    const persistResult = await persistAssessmentSelections(input);

    if (!persistResult.ok) {
      return persistResult;
    }

    const completionState = await loadAssessmentCompletionState(input.testId, persistResult.attemptId);

    if (!completionState.isComplete) {
      return {
        ok: false,
        message: getIncompleteRequiredAnswersMessage(
          completionState.missingRequiredQuestionIds.length,
        ),
      };
    }

    const supabase = createSupabaseAdminClient();
    const completedAt = new Date().toISOString();
    const { data: completedAttemptData, error: completeAttemptError } = await supabase
      .from("attempts")
      .update({
        status: "completed",
        completed_at: completedAt,
      })
      .eq("id", persistResult.attemptId)
      .eq("test_id", input.testId)
      .eq("status", "in_progress")
      .select("id, status, completed_at")
      .maybeSingle();

    if (completeAttemptError) {
      return { ok: false, message: "Unable to complete attempt." };
    }

    let completedAttempt: AttemptRecord | null = completedAttemptData as AttemptRecord | null;

    if (!completedAttempt) {
      const { data: existingAttemptData, error: existingAttemptError } = await supabase
        .from("attempts")
        .select("id, status, completed_at")
        .eq("id", persistResult.attemptId)
        .eq("test_id", input.testId)
        .maybeSingle();

      if (existingAttemptError) {
        return { ok: false, message: "Unable to confirm attempt completion." };
      }

      if ((existingAttemptData as AttemptRecord | null)?.status !== "completed") {
        return { ok: false, message: "Unable to complete attempt." };
      }

      completedAttempt = existingAttemptData as AttemptRecord;
    }

    const results = await persistCompletedAssessmentResults(input.testId, persistResult.attemptId);
    const report = await persistCompletedAssessmentReport(input.testId, persistResult.attemptId);

    cookies().set(ASSESSMENT_ATTEMPT_COOKIE_NAME, persistResult.attemptId, {
      httpOnly: true,
      sameSite: "lax",
      path: "/",
    });

    return {
      ok: true,
      attemptId: persistResult.attemptId,
      completedAt: completedAttempt.completed_at ?? completedAt,
      message: "Procjena je završena. Vaši odgovori su zaključani.",
      results,
      report,
    };
  } catch (error) {
    if (isNextRedirectError(error)) {
      throw error;
    }

    console.error("completeAssessmentAttempt failed", error);

    return {
      ok: false,
      message: getCompletionFailureMessage(error),
    };
  }
}

export async function completeProtectedAssessmentAttempt(
  input: SaveAssessmentSelectionsInput,
): Promise<CompleteAssessmentAttemptResult> {
  try {
    const persistResult = await persistAssessmentSelections(input, {
      persistAttemptCookie: false,
      requireProtectedOwnership: true,
    });

    if (!persistResult.ok) {
      return persistResult;
    }

    const completionState = await loadAssessmentCompletionState(input.testId, persistResult.attemptId);

    if (!completionState.isComplete) {
      return {
        ok: false,
        message: getIncompleteRequiredAnswersMessage(
          completionState.missingRequiredQuestionIds.length,
        ),
      };
    }

    const supabase = createSupabaseAdminClient();
    const completedAt = new Date().toISOString();
    const { data: completedAttemptData, error: completeAttemptError } = await supabase
      .from("attempts")
      .update({
        status: "completed",
        completed_at: completedAt,
      })
      .eq("id", persistResult.attemptId)
      .eq("test_id", input.testId)
      .eq("status", "in_progress")
      .select("id, status, completed_at")
      .maybeSingle();

    if (completeAttemptError) {
      return { ok: false, message: "Unable to complete attempt." };
    }

    let completedAttempt: AttemptRecord | null = completedAttemptData as AttemptRecord | null;

    if (!completedAttempt) {
      const { data: existingAttemptData, error: existingAttemptError } = await supabase
        .from("attempts")
        .select("id, status, completed_at")
        .eq("id", persistResult.attemptId)
        .eq("test_id", input.testId)
        .maybeSingle();

      if (existingAttemptError) {
        return { ok: false, message: "Unable to confirm attempt completion." };
      }

      if ((existingAttemptData as AttemptRecord | null)?.status !== "completed") {
        return { ok: false, message: "Unable to complete attempt." };
      }

      completedAttempt = existingAttemptData as AttemptRecord;
    }

    const results = await persistCompletedAssessmentResults(input.testId, persistResult.attemptId);
    const report = await persistCompletedAssessmentReport(input.testId, persistResult.attemptId);

    return {
      ok: true,
      attemptId: persistResult.attemptId,
      completedAt: completedAttempt.completed_at ?? completedAt,
      message: "Procjena je završena. Vaši odgovori su zaključani.",
      results,
      report,
    };
  } catch (error) {
    if (isNextRedirectError(error)) {
      throw error;
    }

    console.error("completeProtectedAssessmentAttempt failed", error);

    return {
      ok: false,
      message: getCompletionFailureMessage(error),
    };
  }
}
</file>

<file path="components/assessment/assessment-form.tsx">
"use client";

import { useRouter } from "next/navigation";
import { useRef, useState } from "react";
import {
  completeProtectedAssessmentAttempt,
  completeAssessmentAttempt,
  saveProtectedAssessmentProgress,
  saveAssessmentProgress,
} from "@/app/actions/assessment";
import { CompletedAssessmentSummary } from "@/components/assessment/completed-assessment-summary";
import type { AssessmentCompletionState } from "@/lib/assessment/completion";
import { getAssessmentCompletionState, isQuestionAnswered } from "@/lib/assessment/completion";
import type { CompletedAssessmentReportState } from "@/lib/assessment/reports";
import type { CompletedAssessmentResults } from "@/lib/assessment/scoring";
import type {
  AssessmentSelectionsInput,
  AssessmentSelectionValue,
  AttemptStatus,
} from "@/lib/assessment/types";
import type { TestAnswerOption, TestQuestion } from "@/lib/assessment/tests";

type AssessmentFormProps = {
  executionMode?: "public" | "protected";
  layoutMode?: "classic" | "step";
  completionRedirectPath?: string | null;
  assessmentDisplayName?: string | null;
  participantDisplayName?: string | null;
  testId: string;
  questions: TestQuestion[];
  answerOptionsByQuestionId: Record<string, TestAnswerOption[]>;
  initialSelections: AssessmentSelectionsInput;
  initialAttemptId: string | null;
  initialAttemptStatus: AttemptStatus | null;
  initialCompletedAt: string | null;
  initialResults: CompletedAssessmentResults | null;
  initialReport: CompletedAssessmentReportState | null;
};

type SelectionState = Record<string, AssessmentSelectionValue | undefined>;
type SaveStatus = "idle" | "saving" | "saved" | "completing" | "completed" | "error";
type ProtectedCompletionUiPhase = "processing" | "redirecting" | null;

type ProtectedCompletionUiContent = {
  eyebrow: string;
  title: string;
  description: string;
  liveMessage: string;
};

function getSerializableSelections(
  selections: SelectionState,
): Record<string, AssessmentSelectionValue> {
  const entries = Object.entries(selections).filter(
    (entry): entry is [string, AssessmentSelectionValue] => entry[1] !== undefined,
  );

  return Object.fromEntries(entries);
}

function getEffectiveSelections(
  initialSelections: AssessmentSelectionsInput,
  selections: SelectionState,
): Record<string, AssessmentSelectionValue> {
  return {
    ...initialSelections,
    ...getSerializableSelections(selections),
  };
}

function resetSaveFeedback(
  setSaveStatus: (status: SaveStatus) => void,
  setSaveMessage: (message: string | null) => void,
) {
  setSaveStatus("idle");
  setSaveMessage(null);
}

function getIncompleteRequiredAnswersMessage(completionState: AssessmentCompletionState): string {
  const missingCount = completionState.missingRequiredQuestionIds.length;

  if (missingCount === 0) {
    return "";
  }

  if (missingCount === 1) {
    return "Answer the remaining required question to enable completion.";
  }

  return `Answer the remaining ${missingCount} required questions to enable completion.`;
}

function getInitialQuestionIndex(
  questions: TestQuestion[],
  selections: AssessmentSelectionsInput,
): number {
  const firstUnansweredIndex = questions.findIndex(
    (question) => !isQuestionAnswered(question.question_type, selections[question.id]),
  );

  if (firstUnansweredIndex >= 0) {
    return firstUnansweredIndex;
  }

  return Math.max(questions.length - 1, 0);
}

function getQuestionValidationMessage(question: TestQuestion): string {
  if (question.question_type === "text") {
    return "Unesite odgovor prije nastavka.";
  }

  if (question.question_type === "multiple_choice") {
    return "Odaberite najmanje jednu opciju prije nastavka.";
  }

  return "Odaberite jedan odgovor prije nastavka.";
}

function getStepCompletionValidationMessage(completionState: AssessmentCompletionState): string {
  const missingCount = completionState.missingRequiredQuestionIds.length;

  if (missingCount <= 0) {
    return "";
  }

  if (missingCount === 1) {
    return "Vratite se i odgovorite na preostalo obavezno pitanje prije završetka.";
  }

  return `Vratite se i odgovorite na preostala ${missingCount} obavezna pitanja prije završetka.`;
}

function isLikertScaleQuestion(question: TestQuestion, options: TestAnswerOption[]): boolean {
  return question.question_type === "single_choice" && options.length === 5;
}

function getProtectedCompletionUiContent(
  phase: Exclude<ProtectedCompletionUiPhase, null>,
): ProtectedCompletionUiContent {
  if (phase === "redirecting") {
    return {
      eyebrow: "Procjena završena",
      title: "Analiziramo tvoje rezultate",
      description: "Odgovori su zaprimljeni, a personalizovani izvještaj je spreman za prikaz.",
      liveMessage: "Analiza je završena. Otvaramo rezultate.",
    };
  }

  return {
    eyebrow: "Procjena završena",
    title: "Analiziramo tvoje rezultate",
    description:
      "Odgovori su uspješno zaprimljeni. Pripremamo tvoj personalizovani izvještaj.",
    liveMessage: "Procjena je završena. Analiza rezultata je u toku.",
  };
}

export function AssessmentForm({
  executionMode = "public",
  layoutMode = "classic",
  completionRedirectPath = null,
  assessmentDisplayName = null,
  participantDisplayName = null,
  testId,
  questions,
  answerOptionsByQuestionId,
  initialSelections,
  initialAttemptId,
  initialAttemptStatus,
  initialCompletedAt,
  initialResults,
  initialReport,
}: AssessmentFormProps) {
  const router = useRouter();
  const [selections, setSelections] = useState<SelectionState>(initialSelections);
  const [attemptId, setAttemptId] = useState<string | null>(initialAttemptId);
  const [attemptStatus, setAttemptStatus] = useState<AttemptStatus | null>(initialAttemptStatus);
  const [completedAt, setCompletedAt] = useState<string | null>(initialCompletedAt);
  const [results, setResults] = useState<CompletedAssessmentResults | null>(initialResults);
  const [reportState, setReportState] = useState<CompletedAssessmentReportState | null>(
    initialReport,
  );
  const [currentQuestionIndex, setCurrentQuestionIndex] = useState(() =>
    getInitialQuestionIndex(questions, initialSelections),
  );
  const [saveStatus, setSaveStatus] = useState<SaveStatus>(
    initialAttemptStatus === "completed" ? "completed" : "idle",
  );
  const [saveMessage, setSaveMessage] = useState<string | null>(
    initialAttemptStatus === "completed"
      ? "Procjena je završena. Vaši odgovori su zaključani."
      : null,
  );
  const [protectedCompletionUiPhase, setProtectedCompletionUiPhase] =
    useState<ProtectedCompletionUiPhase>(null);
  const [stepValidationMessage, setStepValidationMessage] = useState<string | null>(null);
  const requestInFlightRef = useRef(false);
  const effectiveSelections = getEffectiveSelections(initialSelections, selections);

  const isCompleted = attemptStatus === "completed";
  const isBusy = saveStatus === "saving" || saveStatus === "completing";
  const isStepLayout = layoutMode === "step";
  const completionState = getAssessmentCompletionState(questions, effectiveSelections);
  const canComplete = completionState.isComplete;
  const incompleteRequiredAnswersMessage = isCompleted
    ? null
    : getIncompleteRequiredAnswersMessage(completionState);
  const saveAction =
    executionMode === "protected" ? saveProtectedAssessmentProgress : saveAssessmentProgress;
  const completeAction =
    executionMode === "protected"
      ? completeProtectedAssessmentAttempt
      : completeAssessmentAttempt;
  const currentQuestion = questions[currentQuestionIndex] ?? null;
  const currentSelection = currentQuestion ? effectiveSelections[currentQuestion.id] : undefined;
  const progressPercent =
    questions.length > 0 ? ((currentQuestionIndex + 1) / questions.length) * 100 : 0;
  const isInteractionLocked = isBusy || requestInFlightRef.current;

  async function persistSelections(
    nextSelections: SelectionState,
    options?: {
      successMessage?: string | null;
    },
  ): Promise<boolean> {
    if (isCompleted || requestInFlightRef.current) {
      return false;
    }

    requestInFlightRef.current = true;
    setSaveStatus("saving");
    setSaveMessage(null);

    try {
      const result = await saveAction({
        attemptId,
        testId,
        selections: getEffectiveSelections(initialSelections, nextSelections),
      });

      if (!result.ok) {
        setSaveStatus("error");
        setSaveMessage(result.message);
        return false;
      }

      setAttemptId(result.attemptId);
      setAttemptStatus("in_progress");
      setCompletedAt(null);
      setResults(null);
      setReportState(null);
      setSaveStatus("saved");
      setSaveMessage(options?.successMessage ?? null);
      return true;
    } catch {
      setSaveStatus("error");
      setSaveMessage("Unable to save progress right now. Please try again.");
      return false;
    } finally {
      requestInFlightRef.current = false;
    }
  }

  async function handleSave() {
    await persistSelections(selections, {
      successMessage: "Progress saved.",
    });
  }

  async function handleComplete() {
    if (isCompleted || !canComplete || requestInFlightRef.current) {
      return;
    }

    requestInFlightRef.current = true;
    setSaveStatus("completing");
    setSaveMessage(null);
    setProtectedCompletionUiPhase(executionMode === "protected" ? "processing" : null);

    try {
      const result = await completeAction({
        attemptId,
        testId,
        selections: effectiveSelections,
      });

      if (!result.ok) {
        setSaveStatus("error");
        setSaveMessage(result.message);
        setProtectedCompletionUiPhase(null);
        return;
      }

      setAttemptId(result.attemptId);
      setAttemptStatus("completed");
      setCompletedAt(result.completedAt);
      setResults(result.results);
      setReportState(result.report);
      setSaveStatus("completed");
      setSaveMessage(result.message);

      if (executionMode === "protected" && completionRedirectPath) {
        setProtectedCompletionUiPhase("redirecting");
        router.replace(completionRedirectPath);
        return;
      }

      setProtectedCompletionUiPhase(null);
    } catch {
      setSaveStatus("error");
      setSaveMessage("Unable to complete the assessment right now. Please try again.");
      setProtectedCompletionUiPhase(null);
    } finally {
      requestInFlightRef.current = false;
    }
  }

  function updateSelection(questionId: string, value: AssessmentSelectionValue): SelectionState {
    const nextSelections = {
      ...selections,
      [questionId]: value,
    };

    setSelections(nextSelections);
    setStepValidationMessage(null);
    resetSaveFeedback(setSaveStatus, setSaveMessage);
    return nextSelections;
  }

  async function handleSingleChoiceStepSelection(optionId: string) {
    if (!currentQuestion || isInteractionLocked) {
      return;
    }

    const nextSelections = updateSelection(currentQuestion.id, optionId);
    const isLastQuestion = currentQuestionIndex === questions.length - 1;
    const didSave = await persistSelections(nextSelections);

    if (!didSave || isLastQuestion) {
      return;
    }

    setCurrentQuestionIndex((currentIndex) => Math.min(currentIndex + 1, questions.length - 1));
  }

  async function handleSingleChoiceStepConfirmation() {
    if (!currentQuestion || isInteractionLocked) {
      return;
    }

    await handleAdvance();
  }

  function handleBack() {
    if (isInteractionLocked) {
      return;
    }

    setCurrentQuestionIndex((currentIndex) => Math.max(currentIndex - 1, 0));
    setStepValidationMessage(null);
  }

  async function handleAdvance() {
    if (!currentQuestion || isInteractionLocked) {
      return;
    }

    if (!isQuestionAnswered(currentQuestion.question_type, currentSelection)) {
      setStepValidationMessage(getQuestionValidationMessage(currentQuestion));
      return;
    }

    setStepValidationMessage(null);

    if (currentQuestionIndex === questions.length - 1) {
      if (!canComplete) {
        setStepValidationMessage(getStepCompletionValidationMessage(completionState));
        return;
      }

      await handleComplete();
      return;
    }

    setCurrentQuestionIndex((currentIndex) => Math.min(currentIndex + 1, questions.length - 1));
  }

  if (questions.length === 0) {
    return <p>No questions available.</p>;
  }

  if (executionMode === "protected" && protectedCompletionUiPhase) {
    const completionUi = getProtectedCompletionUiContent(protectedCompletionUiPhase);

    return (
      <section
        aria-live="polite"
        aria-busy="true"
        className="assessment-completion-state"
        role="status"
      >
        <div className="assessment-completion-state__hero stack-sm">
          <p className="assessment-eyebrow">{completionUi.eyebrow}</p>
          <h2>{completionUi.title}</h2>
          <p className="assessment-completion-state__description">{completionUi.description}</p>
        </div>

        <div aria-hidden="true" className="assessment-completion-state__indicator">
          <span className="assessment-completion-state__indicator-orbit">
            <span className="assessment-completion-state__indicator-core" />
          </span>
          <span className="assessment-completion-state__indicator-bar" />
        </div>

        <p className="assessment-completion-state__status" aria-label={completionUi.liveMessage}>
          Rezultati će se otvoriti automatski čim obrada bude završena.
        </p>
      </section>
    );
  }

  if (isStepLayout && !isCompleted && currentQuestion) {
    const options = answerOptionsByQuestionId[currentQuestion.id] ?? [];
    const isLastQuestion = currentQuestionIndex === questions.length - 1;
    const isLikertQuestion = isLikertScaleQuestion(currentQuestion, options);
    const shouldAutoAdvance = isLikertQuestion && !isLastQuestion;
    const shouldShowContinueButton = !shouldAutoAdvance;
    const stepActionsClassName = shouldShowContinueButton
      ? "assessment-step-actions"
      : "assessment-step-actions assessment-step-actions--compact";

    return (
      <div className="assessment-run stack-md">
        <section className="assessment-run-hero">
          <div className="assessment-run-hero__content">
            <div className="assessment-run-hero__intro">
              <div className="assessment-run-hero__identity stack-sm">
                <div className="assessment-run-hero__title-group">
                  <h1>{assessmentDisplayName ?? "Procjena"}</h1>
                  {participantDisplayName ? (
                    <p className="assessment-run-hero__participant">{participantDisplayName}</p>
                  ) : null}
                </div>
              </div>

              <div className="assessment-progress" aria-label="Trenutni napredak">
                <div className="assessment-progress__summary" aria-label="Trenutni napredak">
                  <div className="assessment-progress__metric">
                    <span className="assessment-progress__metric-label">Progres</span>
                    <p className="assessment-progress__metric-value">
                      Pitanje {currentQuestionIndex + 1} od {questions.length}
                    </p>
                  </div>
                </div>

                <div className="assessment-progress__bar-region">
                  <div aria-hidden="true" className="assessment-progress__track">
                    <div
                      className="assessment-progress__fill"
                      style={{ width: `${progressPercent}%` }}
                    />
                  </div>
                </div>
              </div>
            </div>
          </div>
        </section>

        <div className="assessment-step-layout">
          <section className="assessment-step-card">
            <div className="assessment-step-card__header stack-md">
              <div
                className={`assessment-step-card__question-region stack-sm${
                  isLikertQuestion ? " assessment-step-card__question-region--stable" : ""
                }`}
              >
                <p className="assessment-step-card__kicker">Pitanje {currentQuestionIndex + 1}</p>
                <h3>{currentQuestion.text}</h3>
              </div>
            </div>

            <fieldset className="assessment-step-card__fieldset" disabled={isInteractionLocked}>
              <legend className="sr-only">{currentQuestion.text}</legend>

              {currentQuestion.question_type === "text" ? (
                <textarea
                  className="assessment-textarea"
                  value={typeof currentSelection === "string" ? currentSelection : ""}
                  onChange={(event) => {
                    updateSelection(currentQuestion.id, event.target.value);
                  }}
                  rows={4}
                />
              ) : isLikertQuestion ? (
                <div className="assessment-likert">
                  <div className="assessment-likert__scale">
                    <div className="assessment-likert__labels" aria-hidden="true">
                      <span>{options[0]?.label}</span>
                      <span>{options[options.length - 1]?.label}</span>
                    </div>

                    <ol className="assessment-likert__options">
                      {options.map((option) => {
                        const inputId = `${currentQuestion.id}-${option.option_order}`;
                        const isSelected = currentSelection === option.id;

                        return (
                          <li key={option.id}>
                            <label
                              className={`assessment-likert-option${
                                isSelected ? " assessment-likert-option--selected" : ""
                              }`}
                              htmlFor={inputId}
                            >
                              <input
                                id={inputId}
                                type="radio"
                                name={currentQuestion.id}
                                checked={isSelected}
                                onClick={() => {
                                  if (isSelected) {
                                    void handleSingleChoiceStepConfirmation();
                                  }
                                }}
                                onChange={() => {
                                  void handleSingleChoiceStepSelection(option.id);
                                }}
                              />
                              <span className="assessment-likert-option__value">
                                {option.option_order}
                              </span>
                              <span className="sr-only">{option.label}</span>
                            </label>
                          </li>
                        );
                      })}
                    </ol>
                  </div>
                </div>
              ) : options.length > 0 ? (
                <ol className="assessment-options">
                  {options.map((option) => {
                    const inputId = `${currentQuestion.id}-${option.option_order}`;

                    if (currentQuestion.question_type === "multiple_choice") {
                      const selectedOptionIds = Array.isArray(currentSelection)
                        ? currentSelection
                        : [];
                      const isSelected = selectedOptionIds.includes(option.id);

                      return (
                        <li key={option.id}>
                          <label
                            className={`assessment-option${
                              isSelected ? " assessment-option--selected" : ""
                            }`}
                            htmlFor={inputId}
                          >
                            <input
                              id={inputId}
                              type="checkbox"
                              checked={isSelected}
                              onChange={(event) => {
                                const isChecked = event.target.checked;
                                const currentValue = Array.isArray(currentSelection)
                                  ? currentSelection
                                  : [];
                                const nextOptionIds = isChecked
                                  ? [...currentValue, option.id]
                                  : currentValue.filter((optionId) => optionId !== option.id);

                                updateSelection(currentQuestion.id, nextOptionIds);
                              }}
                            />
                            <span className="assessment-option__marker">
                              {option.option_order}
                            </span>
                            <span className="assessment-option__label">{option.label}</span>
                          </label>
                        </li>
                      );
                    }

                    return (
                      <li key={option.id}>
                        <label
                          className={`assessment-option${
                            currentSelection === option.id ? " assessment-option--selected" : ""
                          }`}
                          htmlFor={inputId}
                        >
                          <input
                            id={inputId}
                            type="radio"
                            name={currentQuestion.id}
                            checked={currentSelection === option.id}
                            onClick={() => {
                              if (currentSelection === option.id) {
                                void handleSingleChoiceStepConfirmation();
                              }
                            }}
                            onChange={() => {
                              updateSelection(currentQuestion.id, option.id);
                            }}
                          />
                          <span className="assessment-option__marker">{option.option_order}</span>
                          <span className="assessment-option__label">{option.label}</span>
                        </label>
                      </li>
                    );
                  })}
                </ol>
              ) : (
                <p>No answer options available for this question.</p>
              )}
            </fieldset>

            {stepValidationMessage ? (
              <p className="assessment-inline-message assessment-inline-message--error">
                {stepValidationMessage}
              </p>
            ) : null}

            {saveMessage ? (
              <p
                className={`assessment-inline-message ${
                  saveStatus === "error"
                    ? "assessment-inline-message--error"
                    : "assessment-inline-message--success"
                }`}
              >
                {saveMessage}
              </p>
            ) : null}
          </section>

          <div className="assessment-step-layout__footer">
            {!canComplete && currentQuestionIndex === questions.length - 1 ? (
              <p className="assessment-progress-note">{incompleteRequiredAnswersMessage}</p>
            ) : null}

            <div className={stepActionsClassName}>
              <button
                className="button-secondary assessment-step-actions__button assessment-step-actions__button--ghost"
                type="button"
                onClick={handleBack}
                disabled={isInteractionLocked || currentQuestionIndex === 0}
              >
                Nazad
              </button>

              <button
                className="button-secondary assessment-step-actions__button assessment-step-actions__button--save"
                type="button"
                onClick={handleSave}
                disabled={isInteractionLocked}
              >
                {saveStatus === "saving" ? "Sačuvavanje..." : "Sačuvaj"}
              </button>

              {shouldShowContinueButton ? (
                <button
                  className="assessment-step-actions__button assessment-step-actions__button--primary"
                  type="button"
                  onClick={handleAdvance}
                  disabled={isInteractionLocked}
                >
                  {saveStatus === "completing"
                    ? "Završavanje..."
                    : isLastQuestion
                      ? "Završi procjenu"
                      : "Nastavi"}
                </button>
              ) : null}
            </div>
          </div>
        </div>
      </div>
    );
  }

  return (
    <>
      {isCompleted ? (
        <CompletedAssessmentSummary
          completedAt={completedAt}
          results={results}
          reportState={reportState}
        />
      ) : null}

      <ol>
        {questions.map((question) => {
          const options = answerOptionsByQuestionId[question.id] ?? [];
          const selection = selections[question.id];

          return (
            <li key={question.id}>
              <fieldset disabled={isCompleted}>
                <legend>{question.text}</legend>

                {question.question_type === "text" ? (
                  <textarea
                    value={typeof selection === "string" ? selection : ""}
                    onChange={(event) => {
                      updateSelection(question.id, event.target.value);
                    }}
                    rows={3}
                  />
                ) : options.length > 0 ? (
                  <ol>
                    {options.map((option) => {
                      const inputId = `${question.id}-${option.option_order}`;

                      if (question.question_type === "multiple_choice") {
                        const selectedOptionIds = Array.isArray(selection) ? selection : [];

                        return (
                          <li key={option.id}>
                            <label htmlFor={inputId}>
                              <input
                                id={inputId}
                                type="checkbox"
                                checked={selectedOptionIds.includes(option.id)}
                                onChange={(event) => {
                                  const isChecked = event.target.checked;
                                  const currentOptionIds = Array.isArray(selection)
                                    ? selection
                                    : [];
                                  const nextOptionIds = isChecked
                                    ? [...currentOptionIds, option.id]
                                    : currentOptionIds.filter(
                                        (optionId) => optionId !== option.id,
                                      );

                                  updateSelection(question.id, nextOptionIds);
                                }}
                              />
                              {option.label}
                            </label>
                          </li>
                        );
                      }

                      return (
                        <li key={option.id}>
                          <label htmlFor={inputId}>
                            <input
                              id={inputId}
                              type="radio"
                              name={question.id}
                              checked={selection === option.id}
                              onChange={() => {
                                updateSelection(question.id, option.id);
                              }}
                            />
                            {option.label}
                          </label>
                        </li>
                      );
                    })}
                  </ol>
                ) : (
                  <p>No answer options available for this question.</p>
                )}
              </fieldset>
            </li>
          );
        })}
      </ol>

      {!isCompleted ? (
        <>
          <button type="button" onClick={handleSave} disabled={isBusy}>
            {saveStatus === "saving" ? "Saving..." : "Save progress"}
          </button>

          <button type="button" onClick={handleComplete} disabled={isBusy || !canComplete}>
            {saveStatus === "completing" ? "Completing..." : "Complete assessment"}
          </button>

          {incompleteRequiredAnswersMessage ? <p>{incompleteRequiredAnswersMessage}</p> : null}
        </>
      ) : null}

      {saveMessage ? <p>{saveMessage}</p> : null}
    </>
  );
}
</file>

</files>
